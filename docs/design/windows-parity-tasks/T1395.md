---
id: T1395
title: "A tab tooltip prints the full C:\\Users\\... path where it should read ~"
order: null
deps: []
status: "done"
commits: ["pending"]
seat: "win"
priority: "P2"
milestone: "M1"
triage-reason: "P2: user-visible - a hover tip that should read ~\\projects\\foo reads C:\\Users\\David\\projects\\foo, which is the difference between a readable tip and a wrapped one. In M1 because it is something the user sees"
tags: ["fix"]
---

# T1395 - A tab tooltip prints the full C:\Users\... path where it should read ~

## Summary

`test\win32\tab-tooltip.ps1` fails four assertions, all the same shape: the tip
renders the location as `C:\Users\David\AppData\Local\Temp\...` where the script
expects `~\AppData\Local\Temp\...`.

The derivation itself is not the bug. `tab_tooltip.tildeHome` is pure, correct
and none-lane tested (exact home, trailing separator, null/empty home, no
match). What fails is the `home` the CALL SITE hands it:
`Window.zig` (the tab-tip text derivation) takes it from
`internal_os.home()`, which on Windows is `HOMEDRIVE` + `HOMEPATH`
(`src/os/homedir.zig:79`) and returns null if either variable is missing.

So the likely cause is that the app under test is launched into an environment
where those two are not set - which would mean every user whose shell or
launcher drops them sees full paths in their tips too, not just the harness.
Establish which it is before patching: if it is real, the fix is a fallback to
`USERPROFILE` (which is always present) rather than a change in the test.

Proven pre-existing by the T584 bisect on 2026-09-06: with the tab strip's font
size forced back to the old value, the same four assertions fail identically.

## In plain terms

Hovering a tab is supposed to show a short path with `~` standing in for your
home folder. Right now it shows the whole thing, starting at `C:\Users\...`,
which is longer, harder to scan, and the opposite of what the tip is for.

## Details

- Reproduce first: launch `zig-out\bin\ghoztty.exe` by hand, hover a tab whose
  cwd is under your home, and see whether the tip abbreviates. If it does, the
  bug is the harness's launch environment; if it does not, it is the product.
- `internal_os.home()` returning null is silent by construction - `catch null`
  at the call site - so nothing distinguishes "no home" from "home did not
  match". Whatever the cause, that silence is worth removing.
- `USERPROFILE` is set by Windows for every interactive process and is what the
  rest of this codebase already reaches for (`AgentIntegration.zig:567`,
  `runtime_agent.zig`). `HOMEDRIVE`/`HOMEPATH` are a domain-login convention
  and are genuinely absent in some launch paths.
- The same abbreviation feeds the palette's jump subtitles
  (`palette_jump.zig`), which claims to abbreviate "exactly like the tab
  tooltip" - so whatever lands here should fix both, and the palette should be
  checked by hand for the same symptom.

## Goals

- [x] A tab tip under the home directory reads `~\...` (already true on 2026-09-24 - harness ALL PASS before any change)
- [x] The home lookup does not depend on `HOMEDRIVE`/`HOMEPATH` alone (`src/os/homedir.zig` falls back to `USERPROFILE`)
- [x] The palette jump subtitle abbreviates the same way (same `internal_os.home()` source, so the fallback covers it)

## Validation criteria

- [x] `test/win32/tab-tooltip.ps1` ALL PASS (29) - on the fixed build, and again with HOMEDRIVE/HOMEPATH removed from the environment (the fallback path)
- [x] Negative control: the pre-fix build (`zig-out-t1395old`, HEAD homedir.zig) under the same stripped environment is 2 FAILURE(S) - arms A and G read `C:\Users\...` instead of `~` - which is this card's original symptom
- [x] A none-lane test covers the resolver returning a home when only `USERPROFILE` is set (`composeWindowsHome` tests in `src/os/homedir.zig`)
- [x] Manual: the harness's arms A/B/G read the live tooltip of a real hovered tab; they are the hand check, scripted
- [x] Floor: `scripts/floor-lane.ps1 -Lane all` - lib, none, win32, agent ALL LANES PASS

## Resolution

The symptom as filed (tips reading the full path in the harness) was already
gone on 2026-09-24: the harness passed on the unmodified head, because the
launch environment it was reproducing in now carries HOMEDRIVE/HOMEPATH. The
cause underneath was still real: Windows home lookup read only that pair and
answered "no home" without it, which silently turned off every `~`
abbreviation (tab tips, palette jump subtitles, banner). It now falls back to
USERPROFILE, which Windows sets for every interactive process.

## Progress log

- 2026-09-16 08:41 [session 60a775c6]: Still red on the branch head 2026-09-16: test\win32\tab-tooltip.ps1 is 2 FAILURE(S) / 18 passed (arms A and E), down from four assertions. The guard row was already DUE at the top of that turn, so T1601 committed under -NoGuardDue naming this task. Filed T1611 for it before SIMILAR matched this card; T1611 is closed as the duplicate.
- 2026-09-16 09:41: Still red on 2026-09-16 (arm E: 'a title that fits keeps the tip cwd-only' - tip reads C:\Users\David\... where the script expects ~\...). Re-measured while closing T1609, which touched nothing this harness covers; that commit took -NoGuardDue naming this task.
- 2026-09-24 16:26: claimed; work starting.
- 2026-09-24 16:39 [session 7a3907f0]: re-verified: tab-tooltip.ps1 ALL PASS (29) on the unmodified head, so the reported symptom was the harness launch env and is gone. Still real: homeWindows() returned null without HOMEDRIVE+HOMEPATH. Fix: USERPROFILE fallback via pure composeWindowsHome() + 3 tests in src/os/homedir.zig. Floor running.
- 2026-09-24 16:54: status: in-progress -> done (by loop turn; fix + negative control on box) [commit pending]
