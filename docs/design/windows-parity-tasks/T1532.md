---
id: T1532
title: "A dragged pane can land on another tab or in another window"
order: null
deps: ["T1531"]
status: "skipped(split -\u003e T1537, T1538, T1539)"
commits: []
seat: "win"
priority: "P1"
tags: ["feature"]
---

# T1532 - A dragged pane can land on another tab or in another window

## Summary

Cross-tab and cross-window drops: the tab strip makes a new tab at the index under the pointer, long-hovering a tab button for 500ms switches to it so the drag can continue into its layout, dropping over another window's panes lands there, and releasing over nothing makes a new window. Session safety is the sharp edge - a relocated pane must not be read as closed and lose its agent session (Mac's PaneMoveCoordinator.finishRelocation).

## Inherited from T1530

The pane header's POP-OUT button is already drawn, measured and hit-tested, and
ships disabled behind one constant, `Window.pane_pop_out_supported` — because
what it would do is the cross-window pane relocation this task owns. Flipping
that constant, and filling in the empty `.button` arm in `Window.wndProc`'s
`WM_LBUTTONDOWN`, is part of this task: a live button must land with the
session-safety rule below it, never before it.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-09-13 11:56: claimed; work starting.
- 2026-09-13 11:58: status: in-progress -> skipped(split -> T1537, T1538, T1539) (by four separate drops in one card; split so each lands with its own validation)
