---
id: T703
title: "The agent never sets attached_elsewhere, so the documented steal handshake is dead code on both sides"
order: null
deps: ["T657"]
status: "done"
seat: "win"
priority: "P2"
triage-reason: "Not a live user symptom - the agent's unconditional re-bind is arguably the right behavior. It is a spec/code divergence with dead code and untestable branches on both sides, which is how a future change to either half quietly breaks the other."
tags: ["fix"]
commits: ["3210584f3"]
---

# T703 - The agent never sets attached_elsewhere, so the documented steal handshake is dead code on both sides

## In plain terms

The design says that when a second window tries to open a terminal session
someone else already has open, the first one is asked politely and the second
one has to say "no really, give it to me". That conversation is written down in
three places and implemented in none: the background service just hands the
session over without ever objecting. Nothing is broken for a user today — the
handover works — but a chunk of the app is written to handle an answer it can
never receive, so nobody would notice if it stopped working.

## Summary

Found while landing T657. `protocol.Attached.attached_elsewhere` is documented
as §5.3's steal signal ("Set when the session already had an attached bridge;
the client may retry with `force = true` to steal"), and `protocol.Attach.force`
is the retry. Both halves of the CLIENT exist and are tested:

- `Connection.attachChannelCancellable` returns no pane when
  `attached_elsewhere and !force` (`connection.zig`, the `keep` condition).
- `Remote.threadEnter` detects that case and re-attaches with `force = true`.
- `connection.zig`'s test "attachChannel: steal — attached_elsewhere without
  force, then force succeeds (§5.3)" covers it.

But the only producer is `LifecycleAgent.attached_elsewhere_first`, the test
fake. Grepping `src/remote/agent/` finds no write to the field at all:
`Server.handleAttach` calls `bindLocked(s)` unconditionally on a live session,
which silently repoints the bridge at the new connection and never reports that
anyone else held it. `Attach.force` is likewise read by nobody on the agent side.

So today: every ATTACH steals, immediately and silently; the client's retry
branch is unreachable against a real agent; and one of the reason tokens T657's
notice renders (`attached_elsewhere`) can only ever come from a status the real
agent does not produce.

## Approach

Decide which way the divergence resolves — the code or the spec — and make the
other match. Do NOT implement the handshake just because it is written down;
"attach always wins" may well be the better behavior, and the reconnect swap
(WP-D1) plus restore (WP-D2) both DEPEND on a re-attach succeeding for a session
their own superseded connection still holds. The steal-with-force dance would
cost every one of those an extra round trip to reach the same place.

Two candidate resolutions:

1. **Spec follows code** (likely correct): document that a live ATTACH always
   re-binds, retire `attached_elsewhere` and `force` as no-ops kept for wire
   compatibility (never removed — an older client still sends `force`), and
   delete or re-point the client's retry branch. Keep the notice token: a
   FUTURE agent could produce it, and rendering a generic sentence for a token
   this build cannot see is exactly what the mapping is built for.
2. **Code follows spec**: have `handleAttach` report `attached_elsewhere` when
   `s.bound` and the bridge belongs to another connection, gated on a
   capability so an older client (which would then get no pane and no retry)
   is not regressed. This is the more invasive option and needs the reconnect
   and restore paths measured first — they are the common case, not the
   exception.

Either way the acceptance evidence is the same: the branch must be reachable
from a real agent, or gone.

## Validation criteria

- [x] `attached_elsewhere` is documented as never produced — `protocol.zig`
      (both fields now say RESERVED and name the capability rule a future
      refusal must follow), `handleAttach` (why the unconditional re-bind IS
      the contract), `docs/claude/sessions.md`, `docs/design/remote-machines.md`
      §5.3 (an "as built" note kept beside the design) and
      `docs/design/session-persistence.md` — and the client's dead branches are
      gone: `connection.zig` no longer withholds the pane and
      `termio/Remote.zig` no longer retries with `force = true`.
- [x] The `connection.zig` test is re-labelled "an ATTACHED that sets
      attached_elsewhere still yields a pane (wire shape, T703)" and asserts the
      pass-through rather than a handshake; the fake that produces the field
      says it is the only producer. Green in the `none` lane.
- [x] A REAL-server test now covers the contended case:
      `server.zig` "T703: a second viewer's ATTACH takes a still-bound session,
      silently" — viewer 2 attaches over viewer 1's live bind and gets
      `alive` + `attached_elsewhere == false`, the bridge really moves (live
      output lands on viewer 2), and viewer 1 takes it back with
      `force = false`. Green in `zig build test-agent`.
- [x] The reconnect swap and launch restore still re-attach, with no added round
      trip — `test\win32\session-persistence.ps1` ALL PASS,
      `remote-reconnect-relay.ps1` ALL PASS (12), `remote-reconnect-fresh.ps1`
      ALL PASS (12); measured, not assumed: the relay run's `app.err` shows five
      `attach: session=` lines and no session attached twice, so every re-attach
      is one round trip.
- [x] No capability was introduced (that is the point of the resolution), so
      there is no skew pair to assert. The rule for a future one is written into
      `protocol.Attach.force`.

## Progress log

- 2026-09-12 14:52: claimed; work starting.
- 2026-09-12 14:54: Re-verified on today's code: handleAttach still calls bindLocked unconditionally, never writes Attached.attached_elsewhere and never reads Attach.force; the only producer is the connection.zig test fake. Taking resolution 1 (spec follows code) - it is a mechanism call, not an experience one, so no decision filed: every shipped client already auto-retries with force, so implementing the agent half would change nothing a user can see while costing the reconnect/restore paths a round trip, and redefining a wire field's meaning is not an additive protocol change. Plan: agent behavior documented as always-rebind, the two wire fields declared reserved (a future handshake must be capability-gated), the client's withholding + force retry deleted, and a real-server agent test pinning that a second ATTACH over a live bridge rebinds with attached_elsewhere=false.
- 2026-09-12 15:48: Validated: floor-lane -Lane all ALL LANES PASS (lib/none/win32/agent); the new real-server agent test and the relabelled connection test are green in their lanes. Acceptance: sessions-running-cmd 16, session-relaunch-notify 131, pane-ingest-lag 11, pane-ingest-ab 13, chooser-restore-all-remote 47, agent-relay-session-e2e 18, docs-routing 20, thread-join 24, test-reach 14, msg-timer-ids 15, printclient-audit 8 - all ALL PASS and re-stamped. Criterion 3 measured on the relay reconnect run's app.err: five attach: session= lines, no session attached twice, so no retry round trip was added or lost. Filed T1506 for the other half of 5.3 (no attach epoch, no DETACHED frame: the evicted viewer is never told and can still type into the shell).
- 2026-09-12 15:49: status: in-progress -> done [commit 3210584f3]
