---
id: T1623
title: "Tab tooltip sometimes ends the folder line with a stray trailing backslash"
order: null
deps: []
status: "done"
commits: ["5f2f8554a"]
seat: "win"
priority: "P2"
milestone: "M1"
triage-reason: "a stray backslash at the end of the folder line reads as a broken path"
tags: ["polish"]
---

# T1623 - Tab tooltip sometimes ends the folder line with a stray trailing backslash

## Summary

The tooltip's folder line is the location verbatim bar the ~, so whether it ends in a backslash depends on which source answered: the OS-read live cwd can carry a trailing separator where the OSC-7 cache and a viewer location do not. Seen in tab-tooltip.ps1 arm C's oracle, where the same directory reads '~\AppData\...\tipB555\' one hover and '~\AppData\...\tipB555' the next. Nothing is WRONG, it just spells the same place two ways in a hover a user reads often. The fix is not a blanket trim: a root path is exactly the case where the separator is load-bearing ('C:\' must not become 'C:'), so trim only when a component precedes it. Where: tab_tooltip.zig tipText (pure, none-lane testable); the harness's arm C/E oracles use -like so they need no change, but the none lane should pin both the trimmed and the root case. Found while fixing T1622.

## In plain terms

Hovering a tab shows the folder that tab is in. Sometimes that folder was shown with an extra backslash on the end and sometimes without, for the very same folder, which looks like a broken path. Now it is always shown without the extra backslash, except for a drive's top folder like `C:\`, where the backslash is part of the name.

## Goals

- [x] the folder line never ends in a stray separator after a folder name
- [x] a root (`C:\`, `/`) keeps its separator; a website address is left exactly as it is

## Validation criteria

- [x] `C:\Users\David\git\x\` and `...\git\x` both render `~\git\x`; home-with-slash renders `~`; a UNC share trims — none-lane test "tipText: a trailing separator is dropped after a component (T1623)" (`zig test src\apprt\win32\tab_tooltip.zig`, 29/29).
- [x] `C:\`, `c:/`, `/`, `\` are unchanged — test "tipText: a root keeps its load-bearing separator (T1623)".
- [x] a viewer URL with a trailing `/` is untouched — the existing "tipText: a viewer URL passes through untouched" test is still green.
- [x] on-box: `test\win32\tab-tooltip.ps1` arms B and C now assert the probed tip has no trailing separator — ALL PASS (31 assertions), stamped.

## Progress log

- 2026-09-25 14:46 [session 161077a8-b780-46cf-b374-4a187e7550c9]: claimed; work starting.
- 2026-09-25 14:47 [session 161077a8-b780-46cf-b374-4a187e7550c9]: re-verified still real (tipText passes the location verbatim bar ~). Fix: trimLocationSep in tab_tooltip.zig tipText drops trailing separators only after a component; roots (C:\, c:/, /, \) and URLs keep theirs. None-lane tests pin both; updated the T1622 test that pinned the old verbatim trailing sep; tab-tooltip.ps1 arms B+C now assert no trailing separator.
- 2026-09-25 15:10: status: in-progress -> done (by cli: David, from powershell, in ghoztty)
