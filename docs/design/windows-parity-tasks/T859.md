---
id: T859
title: "Chooser roster: a stale pooled connection could retry on a fresh dial now that T510 bounded every dial phase"
order: null
deps: ["T510"]
status: "done"
commits: ["fe6e4b9f0"]
seat: "win"
priority: "P2"
triage-reason: "Reliability polish, not a defect: the pooled design (T461) already works; this adds a recovery path that was previously forbidden because a retry dial could wedge the worker. Not user-blocking, so P2 and out of M1 until triage says otherwise."
tags: ["polish", "fix"]
---

# T859 - Chooser roster: a stale pooled connection could retry on a fresh dial now that T510 bounded every dial phase

## In plain terms

When the machine chooser lists the sessions on a remote machine, it borrows a
shared connection to ask. If that shared connection has silently died, the
chooser shows a stale or empty list until the pool notices and reconnects on
its own. A careful "try once more over a brand-new connection" would fix that
moment — it used to be forbidden because a new connection attempt could hang
the chooser permanently, and that hang is now impossible (T510).

## Goals

- [x] A roster fetch that discovers its pooled connection is gone says so,
      instead of failing quietly and leaving the stale list up
- [x] The pool condemns that exact connection and dials a fresh one at once,
      without waiting out the redial cooldown it applies to automatic sweeps
- [x] The roster comes back on its own - nobody clicks anything
- [x] One dial per proven-dead connection, and a slow agent's timeout never
      condemns a healthy shared connection
- [x] The recovery is demonstrated failing: section G goes red against a build
      with the wiring removed

## Summary

T328's history: a retry-on-fresh-dial in the SessionRoster worker wedged the
worker thread permanently and was reverted; the root cause was
`relay_dial.dial`'s unbounded upgrade read, fixed by T510 (commit 75ac7fefb).
T461 then removed the per-fetch dial cycle in favor of a pooled connection,
which stands on its own merits — but the pooled design has a known soft spot:
a pooled connection that died silently serves one failed RPC before the pool
heals, and the chooser shows a stale roster for that beat.

Now that every phase of a dial is bounded (connect → TLS+upgrade → HELLO), a
single bounded retry over a fresh dial is safe to reintroduce where a pooled
borrow fails with a connection-shaped error. Scope: the SessionRoster worker
first (`src/apprt/win32/SessionRoster.zig` — see the T328/T510 comment block
there); assess whether the same pattern pays for other pool borrowers
(RemoteReconnect, MachineConnectionPool consumers) or whether the pool's own
healing is enough there.

## Validation criteria

- [x] A roster fetch whose pooled connection is dead retries once over a fresh
      bounded dial and lands the roster (acceptance: section G of
      `test\win32\chooser-conn-pool.ps1` - the pool's own suite, which already
      owns the relay+agent fixture and counts dials on the far side of the wire;
      `FakeRelay.ps1 -DropBridgesFile` drops the live bridge without touching
      the agent, so the machine is fine and only the socket is gone)
      — VERIFIED: `chooser-conn-pool.ps1` ALL PASS (26 assertions), all 7 new
      section-G assertions green, and the negative control (the one
      `retryDeadPool` call disabled) turns 4 of those 7 red.
- [x] The retry provably cannot wedge the worker: the dial path is bounded end
      to end (T510 tests) and the retry is single-shot with the failure
      surfaced as today's stale-roster behavior
      — VERIFIED: the worker dials nothing at all (it reports `dead_entry`);
      the dial is the pool's bounded one, and the two new `machine_pool.zig`
      unit tests pin the single-shot rule (`redial` refuses anything but the
      current `ready` connection) in the `none` and `win32` lanes.
- [x] One dial per proven-dead connection, never a storm (the pool's own dial
      count for the machine moves by exactly one across the recovery)
      — VERIFIED: section G's dial-count oracle counts on the far side of the
      wire and moves by exactly one; it is one of the four assertions the
      negative control turns red.
- [x] `chooser-conn-pool.ps1` and `chooser-sessions-remote.ps1` (incl.
      `-NegativeControl`) still ALL PASS
      — VERIFIED standalone on this box: `chooser-conn-pool` ALL PASS (26),
      `chooser-sessions-remote` ALL PASS (20), `chooser-sessions-remote
      -NegativeControl` ALL PASS (13). Also green, because `FakeRelay.ps1` is
      in their guard coverage: `chooser-resume-remote` (40),
      `activity-monitor-dialed` (44), `remote-reconnect-relay` (12),
      `chooser-controls` (54), `chooser-modeless` (22),
      `chooser-orphan-badge` (17), `chooser-resume` (29),
      `chooser-selection` (35), `window-active-audit` (16).

## Progress log

- 2026-09-18 17:01: claimed; work starting.
- 2026-09-18 17:29 [session 0b528a3b]: Re-verified the defect against the code first, and it is bigger than 'polish': the pool only condemns a connection when the link FSM reaches .dead, but a dropped transport goes EOF -> onTransportError -> .reconnecting (connection.zig:3639) and only reaches .dead after the heartbeat backoff runs out. So a pooled connection whose socket died stays INSTALLED, and every roster fetch borrows it and fails, for minutes. Design (mechanism fork, my call per go.md 5b): the worker does NOT dial - the pool owns dialing, and a connection made in the worker would heal one fetch while leaving the dead entry installed for the next, re-paying a relay upgrade per fetch (the cost T461 removed). Instead the fetch REPORTS what it disproved (Result.dead_entry = the pooled Entry.id) and the GUI thread calls MachineConnectionPool.redialNow, which condemns that exact entry and starts the pool's own bounded dial (T510) immediately, bypassing the 5s redial cooldown; the refetch then rides the ordinary lease notification. Single-shot per loaded roster (SessionRoster.redialed) so it cannot become a dial storm.
- 2026-09-18 18:32 [session 0b528a3b]: RESUME STATE (uncommitted, keep it - the work is validated, only the guard sweep is unfinished). Code complete in src/apprt/win32/{machine_pool,MachineConnectionPool,SessionRoster,MachineChooser}.zig plus test/win32/{chooser-conn-pool.ps1,lib/FakeRelay.ps1} and this task file. PROVEN: floor-lane -Lane all ALL LANES PASS (lib/none/win32/agent, log C:\Users\David\AppData\Local\Temp\t859-floor2.log); chooser-conn-pool.ps1 ALL PASS (26 assertions) with all 7 new section-G assertions green; NEGATIVE CONTROL done - with the one-line retryDeadPool call disabled, 4 of the 7 go red (condemn, pool dial, roster-came-back, dial-count), and the relay-saw-a-socket one passes at delta 1 on the remote window's own reconnect, which is why it is a witness and not the discriminator. STILL OWED: (1) the acceptance batch started at 18:29 was still running at reset - read its verdicts from C:\Users\David\AppData\Local\Temp\t859-accept-summary.txt (BATCH DONE is its last line; it covers chooser-sessions-remote incl. -NegativeControl, remote-reconnect-relay, chooser-controls/modeless/orphan-badge/resume/resume-remote, activity-monitor-dialed - all due because FakeRelay.ps1 is in their coverage); (2) floor-lane -Lane harness (due: test/win32 sources changed); (3) parity-tasks validate, then commit via git-commit-guard with the message drafted at C:\Users\David\AppData\Local\Temp\claude\D--git-ghoztty\0b528a3b-a911-4732-a922-cdafc9d563c9\scratchpad\msg-t859.txt and the log entry drafted at scratchpad\t859-log.md (replace EVIDENCE_PLACEHOLDER/FILED_PLACEHOLDER).
- 2026-09-18 18:42 [session f714ddb1]: RESUME (turn 2): keeping the uncommitted work per the progress log. Read the interrupted acceptance batch summary: 7 of 9 scripts recorded, BATCH DONE never printed (reset killed it). chooser-conn-pool ALL PASS (26), remote-reconnect-relay ALL PASS (12), chooser-controls/modeless/orphan-badge/resume ALL PASS, chooser-sessions-remote -NegativeControl ALL PASS (13) -- but chooser-sessions-remote itself came back 2 FAILURE(S) (18 passed). Investigating that first; then chooser-resume-remote + activity-monitor-dialed, then -Lane harness, validate, commit.
- 2026-09-18 19:36 [session f714ddb1]: Re-ran the three scripts the interrupted batch had scored red, standalone and with nothing else on the box: chooser-sessions-remote ALL PASS (20), chooser-resume-remote ALL PASS (40, stamped), activity-monitor-dialed ALL PASS (44, stamped). The batch's reds were overlap, not defects - it was still running under the dead session while this turn started its own scripts; filed T1658 so a turn boundary can SEE a live acceptance run instead of discovering it through red verdicts. Harness floor: floor-lane -Lane harness hit its own 1800s wall-clock cap after every audit had reported (filed T1657); re-running scripts\harness-floor.ps1 directly, which is the guard row's command and the path that stamps.
- 2026-09-18 20:47: status: in-progress -> done [commit fe6e4b9f0]
