---
id: T1742
title: "A viewer opened from the command palette in a pane with no known folder inherits an empty origin folder"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P3"
triage-reason: "Rare state (pane folder unknown) and the effect of an empty origin is unverified; behind everything user-reported."
tags: ["fix"]
---

# T1742 - A viewer opened from the command palette in a pane with no known folder inherits an empty origin folder

## Summary

T1586 follow-up: Surface.zig:3240 (palette viewer split) passes self.pwd straight through as origin_directory, and a pane with no known folder caches the EMPTY string there (IpcHandlers +list seed / Surface.zig:967). viewer_worktree.fallbackPlan then plans .directory='' rather than .none. Same fix as T1586's banner site: treat an empty cached pwd as absent. Verify what a '' origin actually does in the worktree/git probes first; add a unit test where the decision is made.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
