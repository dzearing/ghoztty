---
id: T1765
title: "Mac: confirm a Windows-agent pane viewed from a Mac window no longer flashes blank when resized (T1763 hold)"
order: null
deps: ["T1763"]
status: "todo"
commits: []
seat: "mac"
priority: "P2"
triage-reason: "P2: verification of shared-core behaviour on the Mac seat; the Windows half is validated on box by conpty-sync-hold.ps1."
tags: ["test"]
---

# T1765 - Mac: confirm a Windows-agent pane viewed from a Mac window no longer flashes blank when resized (T1763 hold)

## Summary

T1763 added a shared-core hold (src/termio/conpty_sync_hold.zig) that keeps a ConPTY child's clear-and-redraw off screen until conhost's paint lands. It keys on the pane's CHILD pty flavour, so it also applies to a Mac window attached to a Windows ghoztty-agent (flavour conpty from the HELLO), and never to a local Mac pane (posix). Validate on the Mac seat: resize a remote Windows pane running Claude Code and confirm no blank flash; and confirm a local Mac pane's sync brackets are unaffected (no added latency). The debug-build oracle line 'conpty sync shown how=.. text_rows=..' is logged in both cases where it applies.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
