---
id: T767
title: "tab-strip-autohide.ps1 aims its caption hit tests with the pre-T496 button arithmetic, so the \u0027...\u0027 probe lands on minimize"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
triage-reason: "a test script aims at pre-T496 button positions, so its probe measures the wrong control"
tags: ["test"]
---

# T767 - tab-strip-autohide.ps1 aims its caption hit tests with the pre-T496 button arithmetic, so the '...' probe lands on minimize

## Summary

Section 4/5 of test/win32/tab-strip-autohide.ps1 derives the caption button x's as a 28 DIP square plus pad_sm per step (\ = \ + \; \ = \.Width - \ - \). T496 made the system trio three NATIVE 46 DIP slabs flush to the client's right edge with zero gaps, which caption-bar.ps1 and CaptionGeom now use. The stale arithmetic puts the '...' probe one slab to the right, so WM_NCHITTEST answers HTMINBUTTON (8) and the assertion 'the ... button hit-tests as HTSYSMENU' fails - against a build whose caption is correct (caption-bar.ps1 asserts HTSYSMENU at the properly derived point, and passes). Found red during T263's regression sweep; the script contains no Send-TestMouse call at all, so it is untouched by that change and this was already failing. Fix: derive the points from Get-TestChromeMetrics the way caption-bar.ps1's CaptionGeom does, or share that helper.

## In plain terms

Nothing a user of the terminal sees is wrong here - this is one of our own test
scripts measuring the wrong thing. It checks that the "..." button in the window's
title bar really is the window menu, but it works out where that button sits using
the old title-bar layout from before the caption buttons were rebuilt at Windows'
native size. So it aimed at minimize instead, and reported a perfectly good build
as broken. When this lands the script asks the shared layout helper where the
buttons are, the way our other title-bar test already does, and it goes green
against the caption the product actually paints.

## Goals

- [x] the "..." probe lands on the overflow button, not on minimize
- [x] the maximize and close probes are aimed by the same published numbers rather than passing by luck
- [x] no second hand-written copy of the caption arithmetic is left in the test tree
- [x] tab-strip-autohide.ps1 is green, and its negative control still goes red

## Validation criteria

- [x] `test/win32/tab-strip-autohide.ps1` exits 0 with ALL PASS - verified on this box: 15/15, where the same script was `1 FAILURE(S) (14 passed)` on the same build minutes earlier.
- [x] the "..." assertion reports HTSYSMENU (3), not HTMINBUTTON (8) - verified in the run above.
- [x] the script derives no caption button x of its own; `CaptionCloseLeft`/`CaptionMaxLeft`/`CaptionMinLeft`/`CaptionOverflowLeft` come off `Get-TestChromeMetrics` - verified by reading the diff.
- [x] the script can still fail: `-NegativeControl` exits 1 - verified on this box.
- [x] standing floor green - `scripts/floor-lane.ps1 -Lane all`: lib/none/win32/agent all PASS.
- [x] harness floor green - `scripts/floor-lane.ps1 -Lane harness`.

## Progress log

- 2026-08-11 18:00: T264 turn re-measured this, and it is still red on b23200944: 1 FAILURE(S) (14 passed), 'the ... button hit-tests as HTSYSMENU ... (got 8)'. Numbers at 125 percent: the script models closeL=W-40 / maxL=W-80 / minL=W-120 / overL=W-165, while the real slabs are close W-58..W, max W-116..W-58, min W-174..W-116. Its '...' probe lands at W-148, inside MINIMIZE. The other three probes pass BY LUCK - their centers happen to fall in the correct slabs - so fixing only the failing one would leave three assertions still aimed by stale arithmetic. Fix source is now proven: T264 added section 2b to caption-bar.ps1 asserting Get-TestChromeMetrics' CaptionCloseLeft/CaptionMaxLeft/CaptionMinLeft/CaptionOverflowLeft against the same numbers section 2 measures against painted pixels, so reading them off the module is safe. See also T770 (two more scripts restating the same datum, correct today).
- 2026-09-03 16:03: T404 already-fixed sweep re-verified this on 6161bc18e: STILL REAL. test/win32/tab-strip-autohide.ps1 lines 158-162 still derive the caption buttons by hand (step = btn + padSm; closeL = cli.Width - padSm - btn; overL = minL - padMd - btn) instead of reading CaptionCloseLeft/CaptionMaxLeft/CaptionMinLeft/CaptionOverflowLeft off Get-TestChromeMetrics, which the script already calls at line 134 for other metrics. The only commit to touch the file since (4dc012073, per-run terminal endpoint) did not go near the arithmetic. No action needed beyond the fix the card already describes.
- 2026-09-09 06:21 [session 328a946b-27c3-4646-a45d-2061c8883c9d]: T676 sweep 2026-09-09: STILL REAL, re-verified at HEAD. test/win32/tab-strip-autohide.ps1:159-163 still derives the caption buttons privately (step = btn + padSm; closeL = cli.Width - padSm - btn) instead of asking Get-TestChromeMetrics/CaptionGeom, so the pre-T496 arithmetic the card describes is unchanged. The only commit to touch the file since filing (4dc012073, per-run terminal endpoint) did not go near this.
- 2026-09-15 09:13: claimed; work starting.
- 2026-09-15 09:48 [session cb2247e8-9d1a-4ffa-95e4-24c06b663db9]: root cause confirmed by running it: on this build tab-strip-autohide.ps1 was 1 FAILURE(S) (14 passed), the '...' probe answering HTMINBUTTON (8). Fix: section 4/5 now reads CaptionCloseLeft/CaptionMaxLeft/CaptionMinLeft/CaptionOverflowLeft off Get-TestChromeMetrics (it already called it at line 134) and centers the system-trio probes on CapBtnW rather than BtnPaint, so the three that passed by luck are now aimed too. No restatement kept here: caption-bar.ps1's CaptionGeom is the deliberate hand derivation and its section 2b is the oracle (T264); a second uncompared copy would be the T257 shape. Re-run: ALL PASS (15 assertions), '...' now answers HTSYSMENU (3); -NegativeControl still exits 1. T770 covers the other two scripts and is untouched.
