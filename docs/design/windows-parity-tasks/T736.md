---
id: T736
title: "src/build/wasm_patch_growable_table.zig\u0027s tests run in no build step"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
tags: ["test", "infra"]
triage-reason: "dead coverage: the tests exist but no build step runs them"
---

# T736 - src/build/wasm_patch_growable_table.zig's tests run in no build step

## In plain terms

There is a file of checks in the build tooling that nobody ever runs. It looks
like it is tested - the checks are written and sitting right next to the code -
but no part of `zig build test` reaches them, so if that code broke tomorrow
every lane would stay green. Wiring them in is a few lines now that there is a
place to wire them into.

## Goals

- [x] `wasm_patch_growable_table.zig`'s seven tests run in `zig build test`
- [x] Breaking one of them turns a lane red
- [x] The aggregator is checked, not remembered: an orphaned `test` block
      under `src/build/` fails the test step by name
- [x] A helper that genuinely cannot live under the aggregator has a way to
      say so, in the file itself

## Summary

Found while landing **T243**. The main test binary roots at `src/main.zig` and
therefore reaches **none** of `src/build/`, so a `test` block written next to a
build helper runs in no step at all. T243 added
`src/build/build_test.zig`, an aggregator wired into `test_step` for exactly
this, and put `drive_check.zig` behind it.

`src/build/wasm_patch_growable_table.zig` is still orphaned. Orphaned
assertions are worse than no assertions: they read as coverage in review and
cannot fail.

**Evidence refreshed at HEAD 2026-09-09 (T676 sweep).** When this was filed,
that file was the *one* other file under `src/build/` with `test` blocks.
Today `grep -rln '^test "' src/build/` finds **three** — `drive_check.zig`,
`TestFilterGuard.zig` and `wasm_patch_growable_table.zig`. `TestFilterGuard.zig`
landed after filing and was wired into the aggregator correctly
(`src/build/build_test.zig:13`), so the gap did not widen; but the aggregator
was *remembered* that time rather than enforced, which is the argument for the
sweep the last paragraph below floats.

The likely one-liner is adding `_ = @import("wasm_patch_growable_table.zig");`
to the aggregator. What needs checking first is whether it compiles under the
aggregator's module root and target (`config.baselineTarget()`) - it patches
wasm binaries, so it may pull in imports or assume a wasm target, in which case
it needs its own test artifact rather than a line in the aggregator.

While in there, consider whether the aggregator should be *enforced* rather
than remembered - a sweep that fails when a `src/build/*.zig` carries a `test`
block no step imports would stop the next one rotting the same way. That is the
same shape as the `ExitCodeAudit` / `SkipAudit` / `VerdictExitAudit` analyzers
already in `test\win32\lib\`.

## Validation criteria

- [x] `zig build test -Dapp-runtime=none --summary all` shows the
      `wasm_patch_growable_table` assertions running (a test count that grows by
      its block count), on both lanes - `ghoztty-build-helpers-test` went
      `14 passed` to `21 passed`, and `GHOZTTY_TEST_FILTER_DUMP=1` with
      `-Dtest-filter=wasm_patch_growable_table` lists all seven by name
- [x] Deliberately breaking one of its assertions turns the lane red (teeth
      check), and reverting it turns it green again - flipping the
      `readLeb128 single byte` expectation to 6 gave
      `20/21 passed, 1 failed ... expected 6, found 5`; reverted, green
- [x] No `src/build/*.zig` is left carrying a `test` block that no step
      imports - either wired in, or the gap is named with a reason - and this is
      now ENFORCED rather than remembered by `src/build/BuildTestSweep.zig`,
      which `build.zig` fails `test_step` on. Its own teeth were proven both
      ways: an unimported `test` file under `src/build/` turned the lane red
      with the file named, and `//! build-test-exempt: <reason>` in that same
      file turned it green

## Progress log

- 2026-09-09 06:21 [session 328a946b-27c3-4646-a45d-2061c8883c9d]: T676 sweep 2026-09-09: STILL REAL, but the card's grep evidence is out of date and is corrected in the Summary. 'src/build/wasm_patch_growable_table.zig is the ONE other file under src/build/ with test blocks' was true when filed; today grep -rln '^test "' src/build/ finds THREE - drive_check.zig, TestFilterGuard.zig and wasm_patch_growable_table.zig. TestFilterGuard.zig arrived after filing and was wired into the aggregator correctly (src/build/build_test.zig:13), so the gap did not widen - wasm_patch_growable_table.zig is still the sole orphan, and build_test.zig still imports only the other two. That third file is also the argument FOR the enforcing sweep the card's last paragraph floats: the aggregator was remembered once and would have been forgotten once.
- 2026-09-14 19:20: claimed; work starting.
- 2026-09-14 19:33 [session 93199c65-9f44-4dfb-a53a-d3baf89334f6]: Wired src/build/wasm_patch_growable_table.zig into the src/build aggregator (src/build/build_test.zig). It compiles under the aggregator's module root and baselineTarget with no changes - it imports only std. Evidence: the none lane's ghoztty-build-helpers-test binary went 14 -> 21 passed, and GHOZTTY_TEST_FILTER_DUMP=1 with -Dtest-filter=wasm_patch_growable_table lists all seven of its named tests. Teeth check: flipping the 'readLeb128 single byte' expectation to 6 turned the lane red ('20/21 passed, 1 failed ... expected 6, found 5'); reverted and green again. Remaining: the enforcing sweep so the aggregator is checked rather than remembered.
- 2026-09-14 19:44 [session 93199c65-9f44-4dfb-a53a-d3baf89334f6]: Enforcement landed: src/build/BuildTestSweep.zig walks src/build/ at configure time, finds every file with a top-level test block, and checks it is reachable from build_test.zig through sibling imports. build.zig hangs a b.addFail() step off test_step for anything that is not. Escape hatch is self-documenting - '//! build-test-exempt: <reason>' in the file's own doc comment. Its parsing half is pure and asserted by the aggregator it polices (8 new tests, incl. two tmpDir end-to-end sweeps). Teeth proven both ways: dropping an unimported 'test "x" {}' file into src/build/ turned the none lane red with the file named and both remedies printed; adding the exempt marker to that same file turned it green; probe deleted.
- 2026-09-14 20:01 [session 93199c65-9f44-4dfb-a53a-d3baf89334f6]: Validated: floor-lane -Lane all => lib#1=PASS none#1=PASS win32#1=PASS agent#1=PASS (ALL LANES PASS). test\win32\docs-routing.ps1 ALL PASS (20 assertions) for the docs/claude/build.md note, guard re-stamped. Filed T1575 for the same dead-coverage shape outside src/build/.
