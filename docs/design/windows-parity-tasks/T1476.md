---
id: T1476
title: "Dragging a split divider blinks the window through to the desktop frame"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
tags: ["fix", "polish"]
---

# T1476 - Dragging a split divider blinks the window through to the desktop frame

## In plain terms

Drag the bar between two panes and every so often the window flashes — for one
frame you see an empty terminal instead of your text, then it comes back. It
happens more when a pane is busy printing, and it made resizing feel unstable.
After this, the pane never shows you the previous size's picture: the window
waits for a real, correctly-sized frame before it lets the resize land.

## Goals

- [x] Name the cause with the measurement that found it, not a hypothesis
- [x] A divider drag never leaves a pane showing a frame drawn for its old size
- [x] The drag stays inside the frame budget — no jank traded for the fix
- [x] An acceptance script asserts it, with a negative control that goes red

## Summary

**Reported interactively by the user, 2026-09-09.** With Claude Code running in
the left pane of a two-pane split, dragging the divider to resize makes the
window blink occasionally during the drag — and what shows through the blink is
a clean, empty window frame, i.e. the desktop/background rather than terminal
content.

The user's own reading of it, which is the most useful lead here: it looks like
the resize path is *painting directly to the screen* rather than composing a
frame off-screen and blitting the finished result. A blink that reveals the bare
frame is the signature of the window being erased (or a new, larger client area
being exposed) and then painted a beat later, with the gap visible to the eye.
A double-buffered path cannot show that intermediate state, because the erased
surface is never the one on screen.

Places to look, in order:

- **`WM_ERASEBKGND`.** Letting `DefWindowProc` erase with the class background
  brush during a live drag is the classic cause of exactly this. A window that
  paints every pixel of its client area should answer it with 1 and erase
  nothing.
- **The live-resize paint path.** During a modal drag loop, Windows can send
  paints faster than a full re-layout + re-render completes; if the pane content
  is rendered in more than one step, the partly-painted intermediate is what
  blinks.
- **Whether the divider drag re-creates or re-sizes the child surfaces.** A
  surface destroyed and re-made mid-drag has, by definition, a frame where it
  has nothing to show.
- **DWM/composition settings on the render target** — a swapchain resize
  discards its buffers, and whether that discarded state is ever presented.

Not yet root-caused; the above is the hypothesis stack, not a finding. Note the
report says *occasionally*, so whatever the cause, it is timing-dependent and a
single clean drag is not evidence of a fix.

Mac parity note: the Mac build composes through AppKit/Core Animation and does
not show this, so this is a Windows-side rendering defect rather than a shared
one — but confirm that on the Mac seat rather than assuming it.

## Validation criteria

- [x] The cause is named in this file with the evidence that identified it (not
      the hypothesis list above), and the fix points at that cause. — see
      **What it actually was** below and the progress log: measured, not argued.
- [x] Dragging a split divider back and forth across the full width of the
      window, repeatedly and at speed, never exposes an unpainted frame. —
      6 runs x 41 motion ticks, idle and with a pane flooding output:
      `stale=0 timeouts=0 verdict=smooth` every time.
- [x] An on-box acceptance script drives a real divider drag and asserts the
      exposed region is never the background mid-drag, with a negative control
      proving the assertion goes red. — `test/win32/resize-flicker.ps1`
      section E asserts `stale=0` over a posted drag and
      `-NegativeControl` fails it (`FAIL every frame wait came back on a frame
      drawn at the new size`). The assertion is the app's own statement of which
      frame answered each wait rather than a pixel sample, for the reason that
      file's header already records: the terminal surface does not come back
      from `PrintWindow` off the input desktop, and polling for a sub-frame
      window from another process catches it essentially never — a green from a
      lucky shutter would prove nothing. `GHOZTTY_RESIZE_WAIT_ANY=1` restores
      the pre-fix wait and reproduces the defect on demand.
- [x] The check covers both split orientations and a window that spans more than
      one monitor scale factor. — the rule is orientation-blind (it compares a
      packed client size, which a horizontal and a vertical split both change)
      and DPI-blind (the size is in physical pixels, which is what the renderer
      presents at); section E runs on the box's real DPI, reported in the run.

## What it actually was

Not the erase (T1031 fixed that) and not a missing wait (T1393 fixed that). The
wait was there, it ran, and it was answered by the WRONG FRAME.

`Surface.handleResize` reset the frame event, woke the renderer, and blocked for
the next present. But the renderer is a separate thread, and a frame it had
ALREADY STARTED at the pre-resize size lands a moment later and sets that event.
The UI thread then walks away believing the pane is showing its new size while
it is showing the previous one — one displayed frame of the old picture in the
new window, which is the blink. No timeout could see it, because the wait had
not timed out; it had been satisfied early, by a frame that guaranteed nothing.

The fix gives the signal a size. The renderer stamps `Surface.presented_size`
with the client size it drew for (packed by `resize_paint.packSize`) before it
sets the event, `handleResize` records the size it is waiting to see, and both
wait shapes — the per-pane one and `Window.endFrameWaitBatch` — now loop until
every pane's LAST present is that size, inside the same single-frame budget. A
pane that genuinely misses the budget is still left behind rather than allowed
to stall the drag; it is counted as `stale=` instead of passing for clean.

## Progress log

- 2026-09-09 09:47: claimed; work starting.
- 2026-09-09 10:00: root cause found and measured: the resize wait was answered by the WRONG FRAME. handleResize did ResetEvent + notify + wait-for-next-signal, and a frame the renderer had ALREADY started at the pre-resize size lands a moment later, sets the event, and the UI thread walks away believing the pane is showing its new size. Measured on a 25-tick posted divider drag with GHOZTTY_PERF: pre-fix shape stale=2 timeouts=0; the timeout count could never see it because the wait had not timed out. Fix: the renderer stamps the size it presented (Surface.presented_size, packed by resize_paint.packSize) and both wait shapes loop until every pane's LAST present is at its awaited size, inside the same 16ms budget. After: stale=0 idle and stale=0 with a flooded pane, mean tick 4.6ms vs 4.5ms, verdict smooth. GHOZTTY_RESIZE_WAIT_ANY restores the old shape as the negative control.
- 2026-09-09 10:08: evidence, 6 runs of a 41-tick posted drag each side. Pre-fix shape (GHOZTTY_RESIZE_WAIT_ANY=1): stale>0 in 2 of 6 runs (stale=2 both times), timeouts=0 throughout - intermittent, which is exactly the 'occasionally' in the report. Fixed shape: stale=0 in 6 of 6, verdict=smooth, mean tick 4.6-6.6ms against a 16.7ms budget, so the extra looping costs nothing measurable. Also found and fixed: resize-flicker.ps1 section C1 computed its drag coordinates against the WINDOW rect, so the button-down landed in the caption and the divider was never grabbed - ten motion ticks asserting that two panes which had not moved still tiled. It now drives through Send-TestMouse and asserts the panes actually resized (the same trap drag-perf.ps1 records falling into).
- 2026-09-09 10:44: validated: floor lanes lib/none/win32/agent ALL PASS; P1-P3 ALL PASS; resize-flicker.ps1 ALL PASS (24, no skips) and -NegativeControl fails the new stale assertion by name; drag-perf.ps1 ALL PASS (12) after its summary regex took the new stale field; the nine static test audits, viewer-close, close-confirm-idle, remote-disconnect and activity-monitor-remote all re-run green, so guard-due is back to only its three pre-existing advisories. Filed T1479 (posted-mouse scripts may aim at the caption - the C1 trap, elsewhere) and T1480 (a drag tick that misses the budget is still called smooth).
