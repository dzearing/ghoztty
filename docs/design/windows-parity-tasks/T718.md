---
id: T718
title: "The ghoztty agent skill does not mention ghoztty:// links, so generated documents never carry one"
order: null
deps: ["T695"]
status: "done"
commits: ["be4e9a410"]
seat: "win"
priority: "P2"
tags: ["docs"]
triage-reason: "Found while landing T695. The scheme exists to make generated documents clickable, and the thing that generates those documents is an agent reading the skill - which does not know the scheme exists."
---

# T718 - The ghoztty agent skill does not mention ghoztty:// links, so generated documents never carry one

## Summary

T695 (Windows) and main's `c36456863` (macOS) give a document the ability to
say "jump to that terminal" with `ghoztty://focus/<target>`. The documents in
question — a worktree dashboard, a build report, a status page — are written by
an agent following the `ghoztty` skill, and the skill says nothing about the
scheme, so nothing emits one.

main updated its bundled copy (`macos/Resources/Ghoztty/skills/ghoztty/
SKILL.md`, +116 lines in `c36456863`). **That file does not exist on this
branch**, and the skill this box actually loads lives in the
dzearing-claude-marketplace repo (plus its plugin cache) — so this is a change
in a different repo, which is why it is filed rather than done inline.

What it should teach: the one form, that `<target>` is anything `--target`
accepts (so `$GHOZTTY_PANE_ID` works), that focus is the ONLY verb and why, and
that a banner link or a rendered markdown link is where such a link is usually
put.

## In plain terms

Ghoztty can now be linked to, but nothing that writes documents knows that yet.
Until the skill says so, the feature is available and unused.

Since this was filed the teaching has arrived — the skill moved into this repo
and came with it. What was still missing is that nothing CHECKED it: the text
could be re-vendored away tomorrow and no run would notice.

## Goals

- [x] Confirm the shipped skill teaches the scheme (it does, since T866)
- [x] A harness fails if that teaching is ever dropped
- [x] The stale copy an agent can still load is refreshed

## Validation criteria

- [x] The `ghoztty` skill documents `ghoztty://focus/<target>`, its one verb,
      and where such a link is usefully placed — read in
      `src/apprt/win32/assets/ghoztty/skills/ghoztty/SKILL.md` (and the
      byte-identical Mac bundle resource), section "Focus links: the
      `ghoztty://` URL scheme"; landed by 59388143f (T866)
- [x] The teaching cannot silently disappear — section A of
      `test\win32\hook-json.ps1` now asserts the five phrases that carry it
      (the one form, `focus` as the only verb, `<target>` taking anything
      `--target` takes, the pane-id row, the banner-link example)
- [x] Patched in the active plugin cache — the `ghoztty:ghoztty` copy under
      `.claude\plugins\cache\dzearing-claude-marketplace\ghoztty\0.10.0` was a
      711-line pre-scheme document and is now byte-identical to the repo asset.
      The marketplace source repo no longer ships a ghoztty plugin at all;
      the repo asset is the source of truth, and the app installs it.
      The rest of that stale snapshot is T1559.
- [x] The link an agent would emit actually works — the scheme's on-box
      acceptance is `test\win32\url-scheme.ps1` (guard CURRENT), and a
      rendered-markdown/banner link is exactly the shape it covers

## Progress log

- 2026-09-14 04:13: claimed; work starting.
- 2026-09-14 04:15: CHECK FIRST: criterion 1 is already satisfied. The skill's source of truth moved INTO this repo at 59388143f (T866, 2026-08-15) - macos/Resources/Ghoztty/skills/ghoztty/SKILL.md and src/apprt/win32/assets/ghoztty/skills/ghoztty/SKILL.md both carry the '## Focus links: the ghoztty:// URL scheme' section (one form, target table incl. $GHOZTTY_PANE_ID, focus-is-the-only-verb, banner/markdown placement). The marketplace source repo no longer ships a ghoztty plugin at all. Two things are left: (a) the ACTIVE plugin cache copy (~/.claude/plugins/cache/dzearing-claude-marketplace/ghoztty/0.10.0) is a 711-line pre-scheme document still offered to every session as ghoztty:ghoztty, and (b) nothing tests that the shipped skill teaches the scheme - that is the validation this card was owed.
- 2026-09-14 04:38: Validated: hook-json.ps1 ALL PASS (53 assertions, up from 48) and it re-stamped the guard; negative control run by hand - deleting the 'focus is the only verb' sentence flips that assertion false. All four zig lanes PASS; ipc-p1/p2/p3 ALL PASS. Plugin cache copy refreshed and byte-identical to the repo asset. Filed T1559 for the rest of that stale snapshot.
- 2026-09-14 04:45: status: in-progress -> done
