---
id: T1653
title: "Re-run the harness floor, lane-releasesafe and build-cache against T848's commit to re-stamp their guards"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
tags: ["test", "infra"]
---

# T1653 - Re-run the harness floor, lane-releasesafe and build-cache against T848's commit to re-stamp their guards

## Summary

T848 committed under `-NoGuardDue`. This card is the debt that hatch creates,
and it should be paid by the next turn that has the box to itself.

What made them due is mechanical rather than suspicious: T848 added one file
under `test\win32\` and edited `scripts\floor-lane.ps1` and
`scripts\guard-due.ps1`. The harness-floor family are SWEEPS over every script
in `test\win32\`, so a single new file makes all of them due at once;
`lane-releasesafe` and `build-cache` cover the two edited scripts.

What is already known about the code, so this is a re-stamp and not an
investigation:

- The harness floor was run in full on this change (`-Lane harness`, 18m 53s):
  **26 of 29 green**. The three that were not are `skip-visibility.ps1` (PENDING,
  T1123), `asserted-nothing.ps1` (PENDING, T1568) and `test-reach-audit.ps1`,
  which passed when re-run alone - the T1645 flake.
- `skip-visibility.ps1` DID name the new file on the first run (a `-NoNegativeControl`
  switch let the run print ALL PASS with a section skipped). That was fixed by
  removing the switch, and re-running the audit no longer names it. Only T1123's
  own pre-existing failure remains.
- The new guard row `lane-stall-sampling` is stamped green.

## In plain terms

A batch of test sweeps need re-running so their "last known good" stamps point
at the newest code. Nothing is known to be broken; the sweeps just take about
twenty minutes each and the previous run was cut short.

## Goals

- [ ] `harness-floor` re-stamped green
- [ ] `lane-releasesafe` re-stamped green
- [ ] `build-cache` re-stamped green
- [ ] `guard-due.ps1 check` reports no non-advisory DUE row

## Details

```
powershell -NoProfile -File scripts\floor-lane.ps1 -Lane harness
powershell -NoProfile -File test\win32\floor-lane-releasesafe.ps1
powershell -NoProfile -File test\win32\build-cache.ps1
powershell -NoProfile -File scripts\guard-due.ps1 check
```

Run them sequentially and with the box otherwise idle - `test-reach-audit.ps1`
hosts a live WebView2 controller and is the member T1645 keeps catching under
load. If it goes red, re-run it alone before treating it as a real failure, and
add the occurrence to T1645 rather than filing a new card.

## Validation criteria

- [ ] `scripts\guard-due.ps1 check` exits 0 for every non-advisory row (the two
      standing advisories, `release-artifacts-packaging` and `rdp-session`, plus
      `install-walkthrough`, are out of scope here).
- [ ] The harness floor's own verdict line is quoted in this task, with the
      PENDING members named so a future reader can tell a pending red from a new
      one.

## Progress log

- 2026-09-18 11:01 [session edb3aec5]: Next claim will report 14 dirty *.stamp.json files under test\win32\ (argv-hazard, body-complete, cleanslate, command-resolve, desktop-launch, isolation-meta, job-teardown, launch-preflight, persistence-flag, seam-audit, stderr-capture, stderr-launch-capture, unroll-count, verdict-exit) as STRANDED WORK. They are machine-written stamps from the two interrupted harness-floor runs of the T848 turn, and they are NOT evidence to keep: most were written before floor-lane-stall-sampling.ps1 was edited to drop its -NoNegativeControl switch, and every one of those sweeps covers all of test\win32\, so committing them would assert coverage that was never proven over the final code. Right remedy: revert them (git checkout -- on those paths, which is user-gated and was therefore left for a turn that can ask), then do this task's re-run, which rewrites them honestly.
