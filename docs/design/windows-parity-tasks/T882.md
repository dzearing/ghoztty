---
id: T882
title: "chooser-resume.ps1\u0027s orphan fixture rotted: T194 agent-blob restore re-attaches the sessions the kill+drop-manifest trick needs orphaned"
order: null
deps: []
status: "done"
commits: ["622cdf9e1"]
seat: "win"
priority: "P2"
triage-reason: "Test-harness rot, no user-facing behavior at stake; P2 so it does not outrank feature parity work."
tags: ["test", "fix"]
---

# T882 - chooser-resume.ps1's orphan fixture rotted: T194 agent-blob restore re-attaches the sessions the kill+drop-manifest trick needs orphaned

## Summary

`test/win32/chooser-resume.ps1` (T320) manufactures its "orphaned live
session" fixture the pre-T194 way: run panes under the agent, hard-kill the
APP only, delete `session-layout-debug.json`, relaunch — then expects the
relaunched app NOT to re-attach those sessions. Since T194
(`App.restoreSessionLayout` unions the local manifest with the layout blobs
the AGENT itself holds, served from its in-memory store), that relaunch
recovers the windows and re-attaches every session even with the manifest
gone. The fixture yields zero orphans, so the script's step-1 setup assertions
("the roster has the orphans plus the new pane", "a live row with no viewer is
rendered") cannot hold.

Evidence (2026-08-16, T520 turn): a new acceptance script that copied this
exact fixture measured, after kill+manifest-drop+relaunch, 2 rendered sessions
with BOTH attached — the T194 recovery in action — and had to be rewritten.
The working replacement shape (already proven in `session-reattach.ps1` F9d
and now `chooser-orphan-badge.ps1`): OPEN a session directly against the agent
with `remote-test-client --pipe=<agent pipe> --hold=1`; the client detaches
and the session stays alive with no pane, orphaned by construction, immune to
launch restore.

The script is not in the guard-due coverage table, so nothing flags the rot —
it stays red (or silently mis-fixtured) until someone runs it by hand. Fix the
fixture; while in there, consider whether the chooser scripts deserve a
guard-due row.

## In plain terms

One of our automated tests sets up its scenario using a trick that a later
feature deliberately made impossible (the app now recovers windows on its own
after a crash — good for users, fatal for the trick). The test can no longer
create the situation it wants to test, so it fails at setup. It needs the
newer, supported way of creating that situation.

## Validation criteria

- [x] `test\win32\chooser-resume.ps1` builds its orphan via an equivalent
      that survives T194 - the relaunch sets `GHOZTTY_RESTORE_SKIP` and the
      script ASSERTS the seam engaged - and runs ALL PASS on the box
      (2026-09-19: `ALL PASS (29 assertions)`, exit 0, debug win32 build).
- [x] The dead-tombstone and positive-control sections still exercise what
      they exercised before: section 3 ("a session whose program has exited is
      not offered at all", 7 assertions incl. the agent still holding its
      tombstones for launch-time restore) and section 4 ("positive control:
      the same keys DO act on a live row", 4 assertions) both passed in that
      run.

## Progress log

- 2026-09-19 14:54: claimed; work starting.
- 2026-09-19 14:54 [session ba7cd531]: CHECK FIRST: already fixed. test\win32\chooser-resume.ps1 no longer relies on the kill+drop-manifest trick - 622cdf9e1 (T620, 2026-08-16, the same day T882 was filed) taught the relaunch to set GHOZTTY_RESTORE_SKIP and to ASSERT the seam engaged ('session-restore: skipped entirely by GHOZTTY_RESTORE_SKIP'), which survives T194's agent-side layout store; the manifest delete stayed only as fixture honesty. The second half of the ask is done too: scripts\guard-due.ps1 carries a 'chooser-resume' row (and a separate 'chooser-resume-remote' one), and this turn's claim reported it GUARD CURRENT. Remaining deliverable per go.md's already-fixed path: run the harness green on the box and close against 622cdf9e1.
- 2026-09-19 15:17 [session ba7cd531]: Validated on the box: test\win32\chooser-resume.ps1 ALL PASS (29 assertions), exit 0, against a fresh -Doptimize=Debug win32 build. Standing floor green in the same turn: floor-lane -Lane all => lib PASS, none PASS, win32 PASS, agent PASS. Both validation criteria ticked from that run. Filed T1668 for the three false 'GUI POSTMORTEM ... CRASHED - 0xFFFFFFFF' blocks that print under the ALL PASS verdict (Stop-Process -Force exits -1, whose high bit ConvertTo-GuiExitVerdict reads as an unhandled exception; verified empirically on the box).
- 2026-09-19 15:17: status: in-progress -> done (by already fixed by 622cdf9e1 (T620); this turn ran the harness green, which is the deliverable on that shape) [commit 622cdf9e1]
