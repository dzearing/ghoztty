---
id: T1600
title: "Give the keybind and input acceptance scripts a guard row so a red one cannot sit unnoticed"
order: null
deps: ["T1599"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "Without a guard row a red keybind lane can sit unnoticed, which is exactly what T1599 was."
tags: ["infra", "test"]
---

# T1600 - Give the keybind and input acceptance scripts a guard row so a red one cannot sit unnoticed

## Summary

T782 found kb-actions.ps1 red (T1599) only because it edited the file and re-ran it. Nothing else would have: the script has no row in scripts\guard-due.ps1, so no code change makes it DUE and no floor runs it. The same is true of several input/keybind scripts (keybinds-t01.ps1, window-title.ps1, tab-strip.ps1 have rows only in part). Add rows keyed on the win32 input/keybind sources they cover, so touching src\apprt\win32\ input handling makes the matching acceptance script due - which is the whole T783 arrangement.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-09-16 00:03: PARTIAL: T1599 landed the kb-actions half - scripts\guard-due.ps1 now carries a 'kb-actions' row (Surface.zig, RenameDialog.zig, termio\Termio.zig, the script) and test\win32\kb-actions.ps1 stamps itself on a clean green run. Remaining for this task: keybinds-t01.ps1, window-title.ps1 and tab-strip.ps1 still have NO row at all (verified by grep against the table). Also worth reading T1599's comment on why src\apprt\win32\App.zig is deliberately left out of the kb-actions row - a row that is due every day is a row nobody runs.
