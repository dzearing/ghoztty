---
id: T780
title: "The input-desktop audit does not sweep test\\win32\\lib\\, so a harness helper can make every consumer input-desktop-only for free"
order: null
deps: ["T276"]
status: "done"
commits: []
seat: "win"
priority: "P2"
triage-reason: "same miss shape T272/T276 closed, one level up; measured zero instances today, so a hole to close before it is used rather than a live defect"
tags: ["test", "infra"]
---

# T780 - The input-desktop audit does not sweep test\win32\lib\, so a harness helper can make every consumer input-desktop-only for free

## Summary

T276 widened the audit to a second family (screen-DC reads) but not its SCOPE: Get-ForegroundAuditFindings sweeps test\win32\*.ps1 non-recursively, and lib\ is excluded on the grounds that TestDesktop.ps1 legitimately owns a SendInput for its -Interactive hatch. That reasoning covers the foreground family and does not transfer to the screen-DC one: a new lib\ helper that reads the composited screen would make EVERY script that calls it input-desktop-only, and not one of them would be flagged, because the site is not in a swept file. Same miss shape T272 and T276 both closed, one level up - a mechanism the sweep cannot see. No known instance today (measured 2026-08-11: zero screen-DC sites under lib\), which is why this is P2 rather than a defect. Likely shape: sweep lib\ too, with a per-file stated-intent marker for the harness's own hatch instead of a blanket directory exclusion, so the exemption is one line a reader can find rather than an absence.

## In plain terms

The test suite has a rule that keeps it able to run in the background, without
hijacking the screen while somebody is working: any test that truly needs the
live screen has to say so, on a line anyone can find. The checker enforcing that
rule only looked at the tests themselves, never at the shared helper code they
all call - so a helper that quietly needed the live screen would have dragged
every test that used it into the same corner, and nothing would have said a
word. Nobody has written such a helper yet; this closes the door before someone
does. Nothing a user of the terminal sees changes; what changes is that a whole
afternoon of "why does the test suite steal my focus again" cannot come back
through this particular gap.

## Goals

- [x] the audit reads `test\win32\lib\` as well as the top level
- [x] each existing helper that holds these APIs says why, on one findable line
- [x] a new unmarked helper is a finding, proven by planting one
- [x] an acceptance script still cannot exempt itself with the helper marker

## Validation criteria

- [x] `test\win32\foreground-audit.ps1` ALL PASS (56 assertions) with the live
      sweep now covering 50 `lib\` helpers as well as the 380-odd top-level
      scripts - section C, assertions C2b/C2c.
- [x] `-TeethCheck` goes red on a planted unmarked helper in `lib\`
      (assertion C1c), alongside the two pre-existing planted violators - run on
      box, ALL PASS (58 assertions).
- [x] the three real `lib\` files that hold these APIs each state their
      exemption and the mechanism is asserted per file (E12): `TestDesktop.ps1`
      and `DesktopCapability.ps1` by `# input-desktop-helper:`,
      `ForegroundAudit.ps1` by `# foreground-audit:` (it names them, calls none).
- [x] the marker is not a second door for an acceptance script: a top-level file
      carrying it is still a finding, and the finding says so (E9/E10).
- [x] a marker with no reason does not exempt (E4); a marker outliving its site
      is a stale finding (E5) - the same rule as a stale declaration.
- [x] guards re-stamped green on box: `test-desktop-harness.ps1` (77),
      `desktop-launch-audit.ps1` (29), `command-resolve-audit.ps1` (15),
      `floor-lane.ps1 -Lane harness`.

## Progress log

- 2026-09-15 19:43: claimed; work starting.
- 2026-09-15 20:15: widened Get-ForegroundAuditFindings to sweep test\win32\lib\ as well as the top level; helpers state intent with a new '# input-desktop-helper: <reason>' marker (TestDesktop, DesktopCapability), ForegroundAudit itself carries the existing named-not-called marker; files are now keyed by path relative to test\win32 so a helper can also be DECLARED as lib\<name>.ps1. Found a latent parser bug on the way: a one-element [string[]] unrolled through an if and was indexed per character, so a header with a single declaration parsed as zero.
- 2026-09-15 20:39: floor green on box: foreground-audit ALL PASS (56) and -TeethCheck ALL PASS (58) incl. the planted lib\ helper; harness-floor PASS + re-stamped; floor-lane -Lane all ALL LANES PASS; ipc-p1/p2/p3 26/20/16. Filed T1597 (the one-element unroll in the sibling parsers) and T1598 (the same scope question for the sibling audits).
- 2026-09-15 20:39: status: in-progress -> done
