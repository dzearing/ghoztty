---
id: T841
title: "The T443 hunt only gets a handful of runs per turn - soak it unattended between turns instead of spending turns on it"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
tags: ["test", "infra"]
---

# T841 - The T443 hunt only gets a handful of runs per turn - soak it unattended between turns instead of spending turns on it

## Summary

T443 is the top P0 and it is starved of RUNS, not of instruments. A turn can
afford about five lane runs (a compile-loaded agent lane is ~190-340s each), the
signal appears in ~19% of runs on the days it appears at all, and it has now
gone quiet for 28 consecutive build-runner runs across three different box
conditions. Turns are the scarce resource; wall-clock between turns is not —
the box sits idle whenever a turn is thinking, and completely idle whenever the
loop is between tasks.

Make the soak run itself: an unattended, low-priority, INTERRUPTIBLE T443 soak
that accumulates runs in the background and gets out of the way the moment a
turn needs the box.

## In plain terms

We are waiting for a rare crash to show up again, and we can only afford to look
for it a few minutes at a time. The machine is idle most of the day. This is
about letting the search run in that idle time, so the odds accumulate without
costing us working sessions.

## Details

Constraints that make this non-trivial, and are why it is a task rather than a
one-liner:

- **It must never be the thing that wedges a turn.** An acceptance script takes
  the per-user pipe and kills agents; a lane run starved by a background soak
  wedges (T401), which is the exact failure the soak is supposed to measure
  around. So the background soak has to be killable on demand and must not hold
  the lane's zig cache — the isolation `-LoadKind build` already composes for
  its load workers is the pattern.
- **Its results must land somewhere durable**, appended as data points (runs,
  crashes, condition) rather than lost with the process — a crash it catches is
  the whole point, and T443's unblock rule expects a named occurrence.
- **A crash must actually be captured**, not merely counted: the run needs the
  crash-catch path armed so the next occurrence yields a dump and the sweep's
  panic text naming the corrupting test.
- Candidate hosts: the go-loop watchdog (already a per-user scheduled task that
  ticks every 10 minutes), or a separate scheduled task gated on the box being
  idle and no lane running.

## Validation criteria

- [x] Runs accumulate without a turn spending time on them, and the count is
      visible somewhere a turn can read in one command -
      `soak-daemon.ps1 status` (state, rounds, pass/crash/fail/yielded/stall,
      the ledger path and the last five rounds). Installed on this box
      2026-09-18 00:23; the first real round - an isolated
      `zig build test-agent` - was in flight a minute later with no turn
      waiting on it.
- [x] A turn that starts while the soak is running is not slowed or wedged by it
      (the soak yields; demonstrated, not asserted) - `test\win32\soak-daemon.ps1`
      sections D and E put a round IN FLIGHT and then make the box busy two
      ways: an explicit `pause`, and a live command line that names foreground
      work. Both kill the round's process tree within a poll, record it as
      `yielded` with the reason, and leave the foreground process untouched
      (E3). Section F is the other half of the same rule: a process carrying
      the soak marker is NOT foreground work, or the daemon would yield to
      itself for ever and accumulate nothing while looking healthy.
- [x] A crash the background soak catches produces the same evidence a
      foreground one would (dump + panic text + lane log) - the round IS the
      foreground instrument (`test-binary-soak.ps1`, build-runner mode, crash
      catch armed), so the evidence path is identical by construction; the
      daemon reads its exit code, records `crash` with the crash line and keeps
      the round log (section C). T443's log gains the data point when one
      lands: `status` says so in as many words, naming the ARMED-WATCH LOG.

## Progress log

- 2026-09-17 21:22: claimed; work starting.
- 2026-09-17 21:26: design: a separate per-user scheduled task hosts an interruptible soak daemon (scripts/soak-daemon.ps1) rather than the watchdog tick - a round is minutes long and the watchdog tick must stay instant. Round = test-binary-soak.ps1 build-runner over an ISOLATED zig cache (T401: sharing the repo cache manifest lock is the thing that wedges a turn's lane). Yield = explicit pause flag OR auto-detected foreground box work (floor-lane/suite-run/test win32/zig build command lines that do not carry the soak marker); the round's process tree dies within one poll. Ledger = jsonl + state.json under LOCALAPPDATA, read by 'soak-daemon.ps1 status'.
- 2026-09-17 22:25: built scripts/soak-daemon.ps1 (start|stop|status|pause|resume|busy|dry-run|tick|install) + test/win32/soak-daemon.ps1 (34 assertions, ALL PASS) + guard row soak-daemon. Yield is demonstrated, not asserted: a round in flight dies within a poll of an explicit pause AND of a foreground command line, and the foreground process is untouched. Two traps paid for on the way: Start-Process joins -ArgumentList without quoting (a fixture command with a space started a daemon that died instantly in a hidden window), and the harness's own 'test\win32\' command line reads as foreground work, which is what -IgnorePids exists for.
- 2026-09-17 23:43: floor: lib PASS, none PASS, agent PASS; win32 FAILED in the all-run on the ViewerPane live WebView2 host-floor test (untripped point=read) and PASSED alone on the same tree - the known T1645/T678 flake, not this change. The Deadline self-test flake that was reddening the win32 lane and test-reach-audit's lane arm all day WAS this turn's to fix: filed T1647 and sized its numbers against the scheduler (100ms steps under a 300ms budget, 5 of them, so the progress() property still has teeth).
