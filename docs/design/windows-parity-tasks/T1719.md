---
id: T1719
title: "Group the 15 files that joined the upstream-pull risk set since 2026-08-16 into the pull plan"
order: null
deps: ["T960"]
status: "todo"
commits: []
seat: "win"
priority: "P3"
triage-reason: "Only matters once the user calls an upstream pull (D80); the monthly delta already keeps the gap visible."
tags: ["docs", "infra"]
---

# T1719 - Group the 15 files that joined the upstream-pull risk set since 2026-08-16 into the pull plan

## Summary

T960's first monthly divergence run (2026-09-24) grew the risk set from 131 to 145 files: 15 joined (src/apprt.zig, src/apprt/structs.zig, src/apprt/surface.zig, src/terminal/formatter.zig, src/renderer/OpenGL.zig, macos TerminalWindow.swift, and others - listed under 'Joined the risk set' in docs/design/windows-parity-divergence.md) and CLAUDE.md left. docs/design/windows-parity-upstream-pull-plan.md groups only the original 131, so the new files have no group, merge direction, or gate. Assign each to a G1-G10 group (or a new one), recompute the per-stage intersections, and drop CLAUDE.md. Only worth doing before the user calls an upstream pull (D80); until then the monthly delta keeps the gap visible.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
