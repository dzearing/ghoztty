---
id: T805
title: "ghoztty +version prints two binaries\u0027 provenance and only one of them says \u0027commit\u0027"
order: null
deps: []
status: "done"
commits: ["PENDING", "34b51816d"]
seat: "win"
priority: "P2"
triage-reason: "diagnosis polish: +version prints two binaries and only one names its commit, which slows down reading a delivery back"
tags: ["polish"]
---

# T805 - ghoztty +version prints two binaries' provenance and only one of them says 'commit'

## Summary

The only line labelled 'commit' in '+version' describes the RUNNING app, not the binary that was asked - which cost a P1 misfile (T773). Make the document self-disambiguating on both platforms.

## In plain terms

Asking Ghoztty for its version prints two answers on one page: the version of
the copy you asked, and the version of the copy that happens to be running on
the machine. Those are usually different — that is the whole reason the second
one is printed. But only the running copy's answer has the word "commit" next
to it, so that is the line a reader's eye lands on, and they walk away with the
wrong binary's identity. It has already happened once and produced a P1 bug
report about a defect that did not exist.

## Details

`src/cli/version.zig` prints a `Version` block for **this** binary
(`- version: 1.4.0-…-+<sha>`, the sha buried in semver build metadata) and then
a `Running Instance` block for the app it reached over IPC (T52), whose fields
include the only `- commit  : <sha>` line in the document.

Measured on the box 2026-08-12 — one command, two commits:

```
  - version: 1.4.0-users-dzearing-windows-amd64-+66ad87d45   <- this binary
Running Instance
  - commit  : 445780e49                                      <- the running app
```

The reading is not helped by the fact that the probed binary's sha has no
`commit` label at all, while the other binary's does.

Candidate shapes (pick one in the task, don't assume this list is the answer):
give the `Version` block its own `- commit :` line so the label is scoped by
its block rather than owned by one of them; and/or name the blocks for what
they describe (this binary vs the running app) instead of `Version` vs
`Running Instance`.

`src/cli/version.zig` is **shared core**, so whatever lands here lands on macOS
too — that is the point, not a hazard. What must not break: the delivery gates
parse this output (`Get-CommitFromVersionText` in `scripts/delivery-version.ps1`,
anchored on `- version:`) and so does `test/win32/lib/BuildMode.ps1` (anchored
on `- build mode`), both of which already have decoy arms for the two-block
trap (`upgrade-staleness.ps1` A31–A35, `build-mode-guard.ps1` B2).

## Validation criteria

- [x] A reader who greps the output for `commit` can tell, from the line alone
      or its immediate context, which binary it describes. Both commit lines
      now end in `(this binary)` / `(the running app)`, and both headings name
      their binary. Verified live against a real two-block run at HEAD
      73ac4c238 and by the five `T805 ...` arms in `ipc-version.ps1`.
- [x] The same change is present on the macOS seat — the edit is in shared core
      (`src/cli/version.zig`), so it compiles into the Mac build and the `lib`
      lane (which compiles the shared core and the libghostty C API for this
      box) is green. READING the Mac output is a Mac-seat act and is filed as
      **T1628** rather than assumed here.
- [x] `test\win32\upgrade-staleness.ps1` (A31-A35), `build-mode-guard.ps1` and
      `ipc-version.ps1` stay ALL PASS; the fixtures in them are updated to the
      new shape rather than the shape being chosen to fit old fixtures.
      upgrade-staleness ALL PASS (161), build-mode-guard ALL PASS (55),
      ipc-version ALL PASS (54). The pre-T805 shape is kept as its OWN fixture
      (A1b, A31b) because a delivery still probes binaries built before this.
- [x] An arm asserts the new labelling against a real `+version` run with an
      instance actually running, so the two blocks are both populated —
      `ipc-version.ps1` section 1, five arms, against the fixture instance the
      script launches. They would have failed before this change: the old
      output carried exactly ONE `commit` line and it was unlabelled.

## Progress log

- 2026-09-16 21:03: claimed; work starting.
- 2026-09-16 21:18: Reproduced at HEAD 73ac4c238: '+version' printed 'Version' / 'Running Instance' with the ONLY 'commit' line belonging to the running app (82b87b0d2) while this binary's sha was buried in semver build metadata. Fix in src/cli/version.zig (shared core, so macOS gets it too): both headings now name their binary ('Version (this binary)', 'Running Instance (the app running now, not this binary)') and BOTH commit lines carry the same parenthetical, with a new '- commit: <sha> (this binary)' line under Version. Parsers untouched - Get-CommitFromVersionText still anchors on '- version:' and BuildMode on '- build mode', and a pre-T805 binary still parses (new A1b/A31b arms).
- 2026-09-16 21:57: status: in-progress -> done [commit PENDING]
- 2026-09-16 22:00: Landed as 34b51816d. Also hit a commit-guard false positive worth its own card: -Paths with a WILDCARD ('test\win32\*.stamp.json') stages correctly but the readback compares the literal pathspec against the commit's file list, so all 18 matched files read as SWALLOWED WORK and the commit was refused (exit 5). Re-committed with the 26 paths spelled out. Filing it.
