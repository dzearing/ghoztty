---
id: T1657
title: "floor-lane -Lane harness cannot finish inside its own wall-clock cap, so the wrapper reports TIMEOUT over a suite that completed"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
tags: ["infra", "test"]
---

# T1657 - floor-lane -Lane harness cannot finish inside its own wall-clock cap, so the wrapper reports TIMEOUT over a suite that completed

## Summary

scripts\floor-lane.ps1 -Lane harness caps the lane at 1800s. On 2026-09-18 the 29-audit floor took about 28 minutes of sweeping (test-reach-audit alone 10m) and was then cut during suite-run's confirm-pass re-run, so the lane printed WALL-CLOCK CAP / FLOOR NOT GREEN over a run in which every audit had already produced a verdict and the only two reds were the named pending exceptions (T1123, T1568). go.md step 3 points turns at -Lane harness, so the documented path cannot go green; the guard row's own command (scripts\harness-floor.ps1, no cap) still runs and stamps. Fix the cap for this lane (it is source sweeps, not a build), or make the wrapper score the summary it already has when the cap hits.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
