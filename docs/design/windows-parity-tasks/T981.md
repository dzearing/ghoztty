---
id: T981
title: "The agent-adopt harness cannot clear its own guard - a green run leaves the row due"
order: null
deps: []
status: "done"
commits: ["32fc8187c"]
seat: "win"
priority: "P2"
triage-reason: "A harness with a guard row but no stamping tail can never clear its own guard, so every commit after it needs a manual override - the T883/T898 shape."
tags: ["infra", "test"]
---

# T981 - The agent-adopt harness cannot clear its own guard - a green run leaves the row due

## Summary

test\win32\agent-adopt.ps1 has a guard-due row (src/remote/agent/adopt.zig + the harness) but no stamping tail, so a clean green run does not re-stamp it and the row can only be satisfied by hand with 'guard-due.ps1 update -Guard agent-adopt'. That is the same shape T883 fixed for lane-leak-sweep and T898 for release-artifacts: a permanently-due row trains the loop to reach for the -NoGuardDue hatch, which is how the hatch stops meaning anything. Wanted: the standard tail (stamp only when there are zero failures AND zero skips, so a run that skipped a section never vouches for the half it did not look at), plus a sweep for any other row whose harness never stamps.

## In plain terms

Every test script has a "last run green" receipt that keeps the pre-commit gate quiet. One script (the agent-adoption test) could never write its own receipt, so it looked overdue forever and the loop had to override the gate by hand. It was fixed in passing in August; this task tightens it and adds a check so no other test script can ship with the same hole.

## Goals

- [x] agent-adopt stamps its own row on a clean run (already landed in 39b03d125, T1042)
- [x] a run that skipped section R does not stamp
- [x] a standing sweep: every guard row's harness writes its own stamp

## Validation criteria

- [x] a clean green run re-stamps agent-adopt - `test/win32/agent-adopt.ps1` ALL PASS (35), `STAMPED agent-adopt`
- [x] no other row lacks a stamping tail - one-off sweep of 219 rows found none; now held by section P of `test\win32\guard-due.ps1` (P2)
- [x] the sweep can say no - P3 flags the pre-T1042 agent-adopt.ps1 read from git history, P4 rejects a longer-name match; guard-due.ps1 ALL PASS (118)

## Progress log

- 2026-09-26 14:26: claimed; work starting.
- 2026-09-26 14:32: re-verified: stamp tail already landed in 39b03d125 (T1042); left: skip rule + sweep. Added realSkipped==0 to agent-adopt tail and section P (P1-P5) to guard-due harness; both green
- 2026-09-26 14:52: status: in-progress -> done (by cli: David, from powershell, in ghoztty) [commit 32fc8187c]
