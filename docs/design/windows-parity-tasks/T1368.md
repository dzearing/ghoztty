---
id: T1368
title: "Nobody has watched Repair and Cancel answered against a real MSI over a real install"
order: null
deps: ["T1367"]
status: "done"
commits: ["7d7e712cf"]
seat: "win"
priority: "P2"
milestone: "M1"
triage-reason: "user-facing: the Repair/Cancel question is the last installer path still asserted only at its source, and the last two installer defects reached the user because nothing ran the package"
tags: ["test", "fix"]
---

# T1368 - Nobody has watched Repair and Cancel answered against a real MSI over a real install

## In plain terms

When you run the installer for a version you already have, Ghoztty asks
whether to repair it or cancel. Both answers have been checked in every way
short of the real one: the dialog is driven live, and the package's tables are
read back at build time, but nobody has yet double-clicked an actual installer
over an actual install and watched Repair repair and Cancel leave the machine
alone. That last step is what the previous two installer bugs needed and did
not have.

## Summary

T1367 found the Repair/Cancel question sequenced at 6605 — five past
`InstallFinalize` — so it was being asked once the install had already run.
Every existing assertion missed it because they all read the *source* or the
*compiled tables*, and the number that gave it away only appeared when somebody
printed it.

The fix is landed and the tables now assert the whole band. What is still owed
is the walk: `test\win32\install-walkthrough.ps1` act D already installs a
re-identified copy of a PUBLISHED package into
`%LOCALAPPDATA%\Programs\GhozttyT1299Test`, re-runs it, and asserts the dialog
appears and Cancel ends at 1602. Two gaps in it:

- It only answers **Cancel**. Repair — the answer that actually re-runs the
  install — is never pressed, so `REINSTALL=ALL` being armed correctly is still
  unobserved outside the table check.
- Its subject is the newest published release, which until the release carrying
  T1367 goes out is a package whose prompt is sequenced at 6605. The act would
  pass against it, because a same-version reinstall of an identical payload
  looks "untouched" either way — which is exactly the blind spot T1367 was.

fork-ci builds an MSI on every push but uploads no artifact, so the package
that carries the fix arrives through the daily publish. This task is therefore
gated on that release existing.

## Goals

- [x] Run `install-walkthrough.ps1` against the first published release that
      carries T1367 and record act D's outcome (win-v1.36.37 — T1731 first,
      then this turn: ALL PASS 47)
- [x] Extend act D to answer **Repair** as well as Cancel, asserting the
      install is re-written (not merely left alone) and msiexec ends at 0
      (Repair was added by T1302 at D5/D6; this turn's D7 proves it rewrites)
- [x] Assert the thing T1367 could not: that the question is asked BEFORE the
      files move (D4b/D9b snapshot the install directory while the question
      is on screen; D4c/D9 read the action order from the msiexec log)
- [x] Green run stamps the `install-walkthrough` guard

## Validation criteria

- [x] `powershell -NoProfile -File test\win32\install-walkthrough.ps1` is green
      on this box, act D included, against a package carrying T1367 —
      published win-v1.36.37: `install-walkthrough: ALL PASS (47 assertions)`.
- [x] Act D asserts both answers, and the Repair arm fails if `REINSTALL` is
      not armed — `-TeethUnarmedRepair` moves the two arming rows after
      InstallFinalize: `2 FAILURE(S)`, exactly D7 and D8, everything else green.
- [x] `scripts\guard-due.ps1 check` no longer reports `install-walkthrough` as
      unstamped — the green run wrote `install-walkthrough.stamp.json`.

## Outcome

Most of the task had already landed in passing: T1302 walked Repair and Cancel
under a real msiexec (and found Cancel broken, T1730), and T1731 walked the
first published release carrying both fixes. What was still unobserved is what
this turn added to act D:

- **D4b / D9b** — the install directory, read while the question is still on
  screen, is identical to before the run. T1367's 6605 sequencing would have
  moved files first.
- **D4c / D9** — the msiexec log agrees: a Cancel runs nothing that installs;
  a Repair runs InstallFiles only after MaintenancePrompt.
- **D7** — Repair rewrites a file that is present at the same version
  (back-dated to 2001 first). Deleting a file was tried first and is NOT a
  test: Windows Installer restores a missing file of an installed component on
  any same-version `/i`, armed or not — the negative control showed it.
- **D8** — REINSTALL=ALL is added before the prompt starts, from the log.
- `-TeethUnarmedRepair` is the negative control. Getting it to work exposed
  the wixl string-pool under-count a fourth time: editing a Condition freed
  the string MaintenancePrompt still used, so it prompted inside `/qn`
  upgrades and uninstalls. The control writes only the integer Sequence
  column and reads the prompt's condition back; fixing the pool at the source
  is **T1732**.
- `Invoke-IdentityRewrite` now deletes its output first — a failed rewrite had
  passed A1 on the previous run's package.

## Notes

Filed from T1367's turn, which fixed the sequencing and closed the table-level
hole but could not walk a real package: the only wixl on this box is inside
Docker, and starting Docker is the user's call.

## Progress log

- 2026-09-24 15:02: claimed; work starting.
- 2026-09-24 15:33: Most of the task landed in passing (T1302 walked Repair+Cancel under real msiexec; T1731 walked win-v1.36.37 = first release carrying the T1367/T1730 fixes). Left: Repair proven to repair, and prompt-before-files. Added D4b-D4d (install-dir snapshot at the prompt == before; Cancel log has no PrepareInstallDir/InstallValidate/InstallFiles) and D7-D9b (remove ghoztty.com, Repair restores it; REINSTALL=ALL in log; MaintenancePrompt before InstallFiles; snapshot unchanged at the Repair prompt). Green vs published win-v1.36.37: ALL PASS (47). Added -TeethUnarmedRepair negative control; first attempt exposed rewriter refcount defect -> filed T1732. Also fixed Invoke-IdentityRewrite passing A1 on a stale previous-run package.
- 2026-09-24 16:20: Deletion test was vacuous (MSI restores a missing file on any same-version /i); switched D7 to a back-dated present file, which only REINSTALLMODE=amus rewrites. D8 now orders REINSTALL=ALL before the prompt. Teeth (-TeethUnarmedRepair, arming moved to 6700): exactly D7+D8 red. Green vs win-v1.36.37 ALL PASS (47), stamped. harness floor PASS, stamped. Filed T1732 (wixl string pool, 4th occurrence).
- 2026-09-24 16:20: status: in-progress -> done (by cli: David, from powershell, in ghoztty) [commit 7d7e712cf]
