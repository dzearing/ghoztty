---
id: T900
title: "ipc-p2 can fail 16 of 18 assertions on its first run after another harness, then pass twice in a row - the first-run flake has no diagnostics"
order: null
deps: []
seat: "win"
status: "done"
commits: ["PENDING", "c5840f4b7"]
priority: "P2"
triage-reason: "Not user-facing; a test-infra flake observed once with no evidence captured. Needs diagnostics before it can be root-caused."
tags: ["test", "infra"]
---

# T900 - ipc-p2 can fail 16 of 18 assertions on its first run after another harness, then pass twice in a row - the first-run flake has no diagnostics

## In plain terms

While checking other work, the P2 test suite reported almost everything broken,
and then reported everything fine twice in a row minutes later without any code
change in between. Nothing is actually broken for a user - but a test that
cries wolf like this trains people to re-run instead of read, and the one time
it is telling the truth it will be ignored.

## Summary

Observed 2026-08-16 during T623 validation, running the standing floor
sequentially (never overlapped): `release-artifacts.ps1` finished green, then
the first `ipc-p2.ps1` run exited 1 with `P2 ACCEPTANCE: 16 FAILURE(S) (2
assertions passed)`. Two immediate re-runs were `ALL PASS (20 assertions)`.
The 16-of-18 shape smells like the app (or its pipe) never came up for the
whole run - the same family as T755's cold-launch waits - but the run's output
was filtered to the verdict line, so there is no evidence to reason from.

The task is therefore evidence-first:

- Make a red `ipc-p2` run leave its full transcript somewhere durable
  (`%TEMP%` with a stable name, printed in the verdict), the way the
  cross-lineage harness preserves artifacts on failure, so the NEXT flake is
  diagnosable rather than a shrug.
- If the transcript shows a launch/pipe race with the immediately preceding
  harness's teardown, fix that (e.g. wait for the prior instance's pipe to
  actually vanish before starting).

## Validation criteria

- [x] A deliberately-failed ipc-p2 run names a preserved transcript path in its
      final output, and the file holds every assertion line - `test\win32\fail-transcript.ps1`
      section D runs a throwaway harness of the same shape red, reads the path
      out of its LAST line, and reads every assertion line back out of the file
      (D1-D4); section B asserts the naming (B1-B3). ALL PASS (42 assertions).
- [x] Root cause of the first-run flake identified from a captured transcript,
      or the capture shipped and the task split so the fix has evidence to
      stand on - BOTH. The root cause was already read off this run's
      transcript by T1285 ("the app under test stopped answering IPC - the
      CLI's own 5-second waiting notice is in the transcript") and the cascade
      it caused is fixed there; the capture is what shipped here, so the NEXT
      flake survives the re-run.

## Progress log

- 2026-09-20 02:19: claimed; work starting.
- 2026-09-20 02:19: CHECK FIRST outcome: the 16-of-18 CASCADE half was already fixed by T1285 (lib\FloorFixture.ps1 quotes this exact run and stops the body on an unreachable fixture), and its header records the root cause read off the transcript - the app stopped answering IPC. What is still unshipped is criterion 1: the transcript lives at a FIXED path (ghoztty-<name>-last.log) that the next re-run overwrites, and 5 of the 7 floor scripts never name it. So the 2026-08-16 evidence died at the first green re-run. Fix going into a shared lib\Transcript.ps1: preserve a RED run's transcript under a timestamped name later runs cannot clobber, and name that path IN the verdict line so a '| Select-Object -Last 1' reader sees it.
- 2026-09-20 03:52: Shipped: lib\Transcript.ps1 (New-TestTranscript / Save-FailedTranscript / Complete-TestTranscript) and all 7 teed harnesses wired to it. A red run copies its transcript to ghoztty-<name>-fail-<stamp>-<pid>.log and the verdict line names it. Wiring turned up two harnesses (T332, T135) that counted failures but never passes - the T271 shape - fixed in the same change. lib\VerdictExitAudit.ps1 learned the new scorer entry point (A15-A19 demonstrate both directions). Validation: fail-transcript.ps1 ALL PASS (42), four zig lanes PASS, harness floor PASS (29 audits), P1 26 / P2 20 / P3 16, ipc-floor-setup 19, cli-argv-fidelity 35, T332 8, T135 12. Filed T1678 (preserved transcripts are UTF-16).
- 2026-09-20 03:54: status: in-progress -> done (by cli: David, from powershell, in ghoztty) [commit PENDING]
