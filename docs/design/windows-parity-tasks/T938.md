---
id: T938
title: "A guard that is known-blocked invites a fresh duplicate task from every turn that meets it"
order: null
deps: ["T898"]
status: "done"
seat: "win"
priority: "P2"
triage-reason: "P2: no user-visible effect, but four tasks now exist for one gap and each cost a turn's filing; the backlog convergence target counts them"
commits: []
tags: ["infra", "docs"]
---

# T938 - A guard that is known-blocked invites a fresh duplicate task from every turn that meets it

## In plain terms

One safety check on this box cannot pass right now for a reason everybody
already knows and has already written down. Every session that runs into it
sees a red line with no hint that it is a known problem, does the responsible
thing, and files a fresh report for it. There are four reports of the same
thing. The check should say "this one is already known, it is T898" so the
next session spends its time elsewhere.

## Summary

`scripts\guard-due.ps1 check` reports `GUARD DUE <name>` with the changed files
and the command to run. That is exactly right for a guard that is due because
somebody edited its code. It is misleading for a guard that CANNOT be cleared
on this box — and there is one: `release-artifacts` only stamps on a zero-skip
run, two of its sections need Docker, and Docker is deliberately kept down
here. T898 has recorded that since 2026-08-16, with two designed remedies.

Since then the same gap has been filed three more times by three more turns —
T914, T920, T932 — all now closed as duplicates of T898. Each cost a turn the
work of writing it up, and each one landed in the M1 denominator before being
closed again. The report is doing this: it presents a known, tracked,
deliberately-accepted condition identically to a fresh regression, and step 5
of the turn ("every gap the work turned up becomes a NEW task") then does
precisely what it is told.

## Details

Wanted shape: a guard row can name an open task that explains why it is
blocked, e.g. `KnownBlockedBy = 'T898'`, and the report then reads

```
GUARD DUE release-artifacts: ... (known-blocked by T898 -- do not re-file;
    run it anyway if you can, then commit with validate -NoGuardDue)
```

so the escape hatch is still loud but the duplicate-filing reflex is answered
in the same line. The pointer must be VERIFIED rather than trusted: if the
named task is closed, the annotation disappears and the guard reads as an
ordinary due guard again, otherwise the note outlives the reason for it and
becomes the next false all-clear.

This is deliberately not a fix for the underlying block — that stays T898's
job, and this task depends on it only in the sense that it must not paper over
it. Both can be true: the guard is honestly due, AND the next turn should not
spend itself re-discovering why.

## Goals

- [x] A guard row can say "this is already tracked by T<id>" and the report says so
- [x] The note verifies itself against the task file and vanishes once the task closes
- [x] The one row that needs it today (`rdp-session`, blocked on T1256) carries it

## Validation criteria

- [x] A guard row can name a known-blocking task, and `guard-due check`
      prints that pointer beside the DUE line — `test\win32\guard-due.ps1`
      O1 (fixture), and the live claim prints it for `rdp-session` -> T1256
- [x] The pointer is dropped automatically once the named task is closed
      (negative control: close it in a fixture, the annotation goes away) —
      O5/O5b (done, text and -Json), O6 (skipped), O7 (task file missing)
- [x] A guard that is due for ordinary reasons is unchanged — no annotation,
      same wording, same exit code — O2 (blocked row still exits 1), O3
      (plain row), O3b (blocked row's report minus the two added lines is
      byte-identical to the plain row's)
- [x] `test\win32\go-loop-guard.ps1` (or the guard-due harness) pins all three
      — section O of `test\win32\guard-due.ps1`, ALL PASS (113 assertions)

## Progress log

- 2026-08-18 00:38: Recurrence, turn 90 (T877): claim reported release-artifacts DUE; ran it, got ALL PASS (2 skipped) because B1/B2 need Docker, which is deliberately not started on this box; guard-due still DUE afterwards. Did NOT file a seventh duplicate - T898/T939/T945/T949/T951/T953 already say it. Used validate -NoGuardDue.
- 2026-08-18 04:41 [session 20260818]: T898 (this task's dep) landed 2026-08-18 and removed the concrete instance: the release-artifacts guard row was split so an edit to a WIRING file (workflows, shared artifact script, on-box publish script, the harness) is cleared by an ordinary Docker-less run, and only build-msi.sh / build-portable-zip.sh remain behind the zero-skip Docker bar. There is no permanently-unclearable guard on this box today, so the duplicate-filing pressure that produced twelve reports is gone. The KnownBlockedBy annotation is still worth having as a general mechanism for the NEXT guard that becomes environmentally blocked, but its urgency has dropped - re-triage accordingly.
- 2026-09-23 22:17: claimed; work starting.
- 2026-09-23 22:25: re-verified still real (guard-due had Advisory since T1189 but no way to name a tracking task). Built: KnownBlockedBy row field, verified against the task file each check (open => 'known-blocked by T<id>' line after the DUE block + -Json fields; done/skipped/missing => dropped). rdp-session row now names T1256. test\win32\guard-due.ps1 section O: ALL PASS (113).
- 2026-09-23 22:43: status: in-progress -> done (by guard-due section O ALL PASS (113); harness floor PASS)
