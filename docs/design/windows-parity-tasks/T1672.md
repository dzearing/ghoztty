---
id: T1672
title: "Verify +reload --config on the Mac seat: the Swift half of T893 has never been compiled or run"
order: null
deps: ["T893"]
status: "todo"
commits: []
seat: "mac"
priority: "P2"
triage-reason: "P2: the Mac half of T893 is unverified rather than known-broken; the seat that can compile it owns the check"
tags: ["test", "infra"]
---

# T1672 - Verify +reload --config on the Mac seat: the Swift half of T893 has never been compiled or run

## Summary

T893 added the app-wide config-reload trigger to both servers in one change, but the Windows seat cannot build Swift, so IPCServer.handleReload's new --config branch (NSApp.delegate as? AppDelegate -> reloadConfig(nil) on the main queue) is unbuilt and unrun. Build the mac app, run 'ghoztty +reload --config' against it, confirm the config is re-read (a changed setting takes effect), and confirm '+reload --config --target=x' answers '--config cannot be combined with --target' at exit 1 - byte-identical to the win32 server's refusal.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
