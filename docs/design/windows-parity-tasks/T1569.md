---
id: T1569
title: "Both portable install locations hold a Debug ghoztty.exe beside a release ghoztty.com"
order: null
deps: ["T727"]
status: "done"
commits: ["70c6d28a8"]
seat: "win"
priority: "P2"
milestone: "M1"
tags: ["infra", "fix"]
triage-reason: "the exact 2026-08-10 state, found live by T727's first audit run; anybody who downloads the portable zip or runs the Desktop copy gets a console-window build from a commit the .com beside it disagrees with"
---

# T1569 - Both portable install locations hold a Debug ghoztty.exe beside a release ghoztty.com

## In plain terms

The portable copy of Ghoztty on the Desktop, and the one on the network share,
are both currently broken in the same way: the main program is a debug build
(it opens a console window) and the little launcher next to it is from a
different, older build. Anybody who runs those copies gets something nobody
meant to ship. Nothing has noticed for weeks.

## Goals

- [x] Find what keeps putting a Debug build into the portables, and stop it
- [x] Put a real release build into both portables and the share
- [x] Prove the audit and the health line agree they are right

## Summary

**Root cause (2026-09-25).** The Debug exe was not left over from 2026-08-10; it
kept coming back. `test\win32\upgrade-no-fork.ps1` runs
`scripts\upgrade-ghoztty-windows.ps1` against a sandbox staging prefix built from
`zig-out` (a Debug `ghoztty.exe` plus the agent, no `.com`) and did NOT pass
`-NoExtraInstalls`, so the script's default mirror list - the REAL Desktop and
share portables - received that Debug exe and agent on every run. The evidence
is the shape: on 2026-09-23 15:50 both portables got `ghoztty.exe` +6eccc6868
(the HEAD of 15:46) and a fresh agent, while `ghoztty.com` stayed at its
2026-08-31 release copy, because the sandbox stages no `.com`; that run's green
stamp is dated 16:48 the same afternoon.

**Fix, two layers.** The test now passes `-NoExtraInstalls`. And
`upgrade-ghoztty-windows.ps1` refuses to mirror a staging `ghoztty.exe` whose PE
subsystem is not GUI (`NOT PROPAGATED - staging ghoztty.exe has PE subsystem 3`),
so no caller - a test, a hand run, a future script - can put a Debug build into
a portable again. Acceptance: `test\win32\upgrade-staleness.ps1` D18-D23 (a
sandbox mirror named with `-ExtraInstallDirs`; the positive control flips the
subsystem WORD to 2 and it IS mirrored).

### Original filing


T727's audit was run for the first time on 2026-09-14 and immediately scored the
state it was written for:

```
D:\Users\David\Desktop\Ghoztty-portable-x64\Ghoztty\ghoztty.exe has PE subsystem 3, expected 2
D:\Users\David\Desktop\Ghoztty-portable-x64\Ghoztty ships ghoztty.exe +f1d06293e beside ghoztty.com +db68549df
\\homeassistant\share\ghoztty-windows\Ghoztty-portable-x64\Ghoztty\ghoztty.exe has PE subsystem 3, expected 2
\\homeassistant\share\ghoztty-windows\Ghoztty-portable-x64\Ghoztty ships ghoztty.exe +f1d06293e beside ghoztty.com +db68549df
\\homeassistant\share\ghoztty-windows\ghoztty-agent.exe reports 20260810-60bb1e9c9 but the portables have 20260908-fc2e17ac4
```

PE subsystem 3 is console: a Debug build. So both portable locations carry a
debug `ghoztty.exe` (+f1d06293e) beside a `ghoztty.com` from +db68549df, which
is precisely the 2026-08-10 shape, and the share's loose agent is a month older
than the one in the portable directories beside it.

The fix is a delivery, not an edit: build a real release staging prefix and run
`scripts\deliver-windows-build.ps1` against it, which refuses a Debug staging
prefix up front and verifies every file it writes. Nothing here touches the
user's installed Ghoztty (D85) — these are install locations 2 and 3, which the
loop owns.

Reproduce, at any time, with:

```
powershell -NoProfile -File scripts\deliver-windows-build.ps1 -VerifyOnly
```

## Validation criteria

- [x] A release staging prefix exists and `deliver-windows-build.ps1` accepts it
      (a Debug one is refused at the door). - ReleaseFast `zig-out-release` at
      d82c23109; `DELIVER OK: 16 file(s) verified across 2 location(s)` (with
      `-AcceptZipShape`: 11 legitimate added entries - release notes and the
      viewer composer/links files).
- [x] `deliver-windows-build.ps1 -VerifyOnly` exits 0 afterwards, with every
      location agreeing on one commit. - `AUDIT OK: 3 location(s) agree on
      +d82c23109`, exe subsystem 2, com subsystem 3; re-checked after the
      upgrade-no-fork run.
- [x] The share's loose `ghoztty-agent.exe` matches the agent in the portable
      directories. - `ghoztty-agent.exe 20260925-d82c23109 in 3 location(s)`.
- [x] `go-loop-health.ps1` reports `deliver=ok`. - after
      `deliver-audit.ps1 -Force` (the daily watermark does not follow a
      delivery - filed T1739).
- [x] Nothing can mirror a Debug build again. - `upgrade-staleness.ps1` ALL
      PASS (167); negative control with the refusal disabled: 2 FAILURES
      (D20, D21). `upgrade-no-fork.ps1` ALL PASS (131),
      `install-ownership.ps1` ALL PASS (42).

## Progress log

- 2026-09-14: filed from T727's first audit run.
- 2026-09-20 08:32: still live on 2026-09-20, six days after filing, and now worse: deliver-windows-build.ps1 -VerifyOnly scores 7 problems (was 5). The exe/com skew moved to +cb9c0b2ee exe beside +db68549df com, the Desktop portable's agent is now 20260915-83b4d19ba against the share's 20260810-60bb1e9c9, and BOTH ghoztty.com copies are now flagged individually as reporting the wrong commit. The claim-time DELIVER AUDIT reports this every turn and never refuses, so nothing escalates it on its own. Noted from T913's turn; T1682 was filed as a duplicate and closed.
- 2026-09-25 10:05: claimed; work starting.
- 2026-09-25 10:09: root cause: test\win32\upgrade-no-fork.ps1 ran upgrade-ghoztty-windows.ps1 against a zig-out (Debug) staging prefix without -NoExtraInstalls, so the mirror step copied the Debug exe + agent into BOTH real portables (09-23 15:50, exe+agent written, .com untouched because the sandbox stages none). Fix: the test passes -NoExtraInstalls; the upgrade script refuses to mirror a console-subsystem staging exe (upgrade-staleness D18-D23, negative control 2 red). Delivery of a real release next.
- 2026-09-25 10:26: status: in-progress -> done (by cli: David, from powershell, in ghoztty) [commit 70c6d28a8]
