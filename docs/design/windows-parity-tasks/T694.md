---
id: T694
title: "Acceptance scripts still bind fixed TCP ports, and several collide with each other"
order: null
deps: []
status: "done"
commits: ["488c510f8"]
seat: "win"
priority: "P2"
triage-reason: "Port collisions between acceptance scripts: harness hygiene, fixed by allocation rather than by any product change."
tags: ["test", "infra"]
---

# T694 - Acceptance scripts still bind fixed TCP ports, and several collide with each other

## Summary

T171 removed the fixed 47921/47922/47912 ports from `relay-account.ps1` (a
port the OS just handed out, asserted free before it is bound, and a readiness
check that only the run's own nonce satisfies). Eleven other acceptance scripts
still guess a number, and some guess the SAME number as each other:

| Port | Scripts |
|---|---|
| 47931 | `chooser-menu.ps1`, `ipc-machine-chooser.ps1` |
| 47941 / 47942 | `host-settings.ps1`, `chooser-sessions-remote.ps1` |
| 47911 | `ipc-relay.ps1`, `remote-inherit.ps1` |

Plus singletons: `chooser-restore-all-remote.ps1` (47951/47952),
`close-confirm-idle.ps1` (47741), `ipc-remote.ps1` (47901),
`remote-pill.ps1` (47913).

Two failure shapes, both of which read as a product bug rather than a harness
one. A back-to-back run of the same script can meet its own previous listener
still dying in the port (a TCP connect then "succeeds" against a socket that
will never answer). Two DIFFERENT scripts sharing a number cannot run near each
other at all, which is a standing constraint nobody has written down.

## In plain terms

Several tests reserve a network port by picking a number and hoping it is free.
Sometimes it is not -- the previous test is still letting go of it, or another
test in the suite wants the very same one -- and the test then fails in a way
that looks like the terminal is broken. This makes each test take a port the
system just handed it, so tests stop colliding with each other and with
themselves.

## Goals

- [x] One shared helper, so no script writes its own `Get-FreePort` again.
- [x] Every fixed port in `test\win32` drawn instead of guessed.
- [x] A sweep that fails when a script goes back to guessing one.

## Details

The pattern to copy is in `test/win32/relay-account.ps1` (T171):

- `Get-FreePort` (bind :0, read the port, release it) and a `0` default on the
  parameter so a caller can still pin one.
- `Test-PortFree` asserted immediately before each bind.
- A readiness probe the run's OWN server answers with a per-run nonce, instead
  of a bare `TcpClient.Connect`.

`test/win32/lib/FakeRelay.ps1` and `lib/PipeBridge.ps1` take the port from
their caller, so fixing the callers covers them.

## Validation criteria

- [x] No `test/win32/*.ps1` carries a fixed non-zero default TCP port. The check
      is no longer a grep: `lib\FixedPortAudit.ps1` names the three violating
      shapes and section C of `test\win32\fixed-ports.ps1` sweeps the whole
      directory. 32 violations before, 0 after; `-TeethCheck` proves that
      section can still go red.
- [x] Every converted script prints the ports it drew and asserts each free
      before binding - `Resolve-TestPort` does both, and throws rather than
      falling back when a port a caller PINNED is held (sections A5-A8).
- [x] Each converted script passes on its own: the 26 ran serially through
      `scripts\suite-run.ps1` (`temp\suite-runs\t694`) and 25 were green; the
      one red, `ipc-machine-chooser.ps1`, was ALL PASS on the runner`s alone
      re-run and is filed as T1497. The ex-collision groups passed back to
      back in that order: 47941/2 (host-settings, chooser-sessions-remote),
      47911 (ipc-relay, remote-inherit), 47951/2 (chooser-conn-pool,
      chooser-restore-all-remote), 47961/2 (chooser-resume-remote,
      remote-reconnect-relay) and the 47913 trio (activity-monitor-remote,
      remote-disconnect, remote-pill).

## Progress log

- 2026-09-12 09:22: claimed; work starting.
- 2026-09-12 09:30: the defect still reproduces: 32 fixed-port sites across 26 scripts (6 shared a number). Added lib\FreePort.ps1 (Get-FreePort/Test-PortFree/Resolve-TestPort) + lib\FixedPortAudit.ps1, converted every site, and filed test\win32\fixed-ports.ps1 (A helpers, B analyzer fixtures, C sweep, -TeethCheck). Sweep is now 0 and the harness is ALL PASS (16).
- 2026-09-12 10:16: floor lanes ALL PASS (lib/none/win32/agent). Acceptance sweep over the 26 converted scripts at 18/26: 17 pass, 1 fail (ipc-machine-chooser: 4 chooser-row PIXEL assertions, 73 passing incl. every fake-relay/port one - unrelated to ports, needs its own task). All four ex-collision groups pass back to back: 47941/2, 47911, 47951/2, 47961/2, and the 47913 trio.
- 2026-09-12 10:35: sweep finished: 26 scripts, 25 green; ipc-machine-chooser red in-order but ALL PASS alone -> harness order-dependence, filed T1497. The nine meta-audits that sweep every test script all ALL PASS. Guards re-stamped by the green runs.
- 2026-09-12 10:37: status: in-progress -> done [commit 488c510f8]
