---
id: T1543
title: "Resting a dragged pane on another window's tab button opens that tab"
order: null
deps: ["T1538"]
status: "in-progress"
commits: []
seat: "win"
milestone: "M1"
priority: "P2"
triage-reason: "rearrange gap: resting over another window\u0027s tab button does not open it"
tags: ["feature"]
---

# T1543 - Resting a dragged pane on another window's tab button opens that tab

## In plain terms

You can now drag a pane into another window, and you can drop it on that
window's tab strip to make it a tab there. What you cannot do is put it into a
tab that window is not currently showing: hovering over one of its tab buttons
does nothing, so the only way to get a pane into a background tab of another
window is to move it twice.

## Summary

T1537 gave the drag a DWELL: resting on a tab button for 500ms switches to that
tab, so the drag continues against the layout you are aiming at. T1538 left it
deliberately scoped to the window the drag started in, because switching a
background WINDOW's tab reorders something the user is not looking at and
nothing had established what that should feel like. The reachable half is
there - a drop on another window's strip makes a tab - so this is the missing
precision rather than a missing capability.

What the work is: `updateRearrangeDrag` already resolves `pane_drop.hoveredTab`
against a candidate SET and passes it only this window's candidate; the dwell
state (`RearrangeDrag.dwell_tab`) would have to name a window as well as an
index, and `onTabDwell` would have to select the tab on that window and keep
the capture here. Mac's counterpart is `PaneDragController`'s hover timer.

**Landed:** the dwell resolves against every window's strip (`hoveredTab`
over the full candidate set, with the same z-order ownership `resolve` uses, so
a strip hidden behind another window's panes arms nothing),
`RearrangeDrag.dwell_window` names the window, and `onTabDwell` switches that
window's tab through `showTabIndex(idx, focus = false)` - focusing a pane in
another top-level would activate it while this window holds the capture. The
drop needs no change: `commitCrossWindowDrop` already lands in the
destination's ACTIVE tab, which is now the one the dwell opened.

## Goals

- [x] Resting on another window's tab button opens that tab there, and the
      drag carries on against the layout it reveals.
- [x] That window is not activated or focused mid-drag - only its tab changes.
- [x] A tab button hidden behind another window's panes never arms the dwell.

## Validation criteria

- [x] Resting a dragged pane on a BACKGROUND window's tab button for the dwell
      brings that tab up in that window, and the drag continues against it -
      claim E of `test\win32\rearrange-window-drop.ps1` (the pane on screen is
      that tab's own HWND, the strip marks it selected, and a drop onto it is
      previewed).
- [x] Releasing then lands in that tab's layout, with the pane's HWND identity
      and session intact - claim E: the dragged HWND is a child of the other
      window beside the revealed pane, and that window still has two tabs.
- [x] The tab the drag STARTED in is unaffected until the drop commits - claim
      E reads the source mid-dwell: both panes present, same selected tab.
- [x] `test\win32\rearrange-window-drop.ps1` grows the claim, and the guard row
      still stamps - ALL PASS (58 assertions), stamp rewritten;
      `-NegativeControl` is red on A and on E (E inverted to the pre-T1543
      "the other window's tab never changes").
- [x] Occlusion is unit tested: "the front window's PANES beat a strip behind
      them" in `pane_drop.zig` now also asserts a covered strip names no dwell
      tab and an uncovered one names the BACK window (none + win32 lanes via
      `floor-lane.ps1 -Lane all`).

## Progress log

- 2026-09-25 08:08: claimed; work starting.
- 2026-09-25 08:13: fix: dwell resolves against every window's strip (pane_drop.hoveredTab now uses resolve's one-window-owns-the-point rule so a covered strip arms nothing); RearrangeDrag.dwell_window names the window; onTabDwell switches the other window's tab via showTabIndex(focus=false) so it is not activated mid-drag. rearrange-window-drop claim E ALL PASS 58; -NegativeControl red on A and E. Floor lanes next.
