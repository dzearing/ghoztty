---
id: T1498
title: "Fifty-two acceptance launches never say whether they want the last run\u0027s panes back, and nothing runs the sweep that would notice"
order: null
deps: []
status: "skipped(duplicate of T1012 - same red sweep, filed before reading the SIMILAR block through)"
commits: []
seat: "win"
priority: "P2"
tags: ["test", "infra"]
---

# T1498 - Fifty-two acceptance launches never say whether they want the last run's panes back, and nothing runs the sweep that would notice

## Summary

The T158 persistence sweep is RED: 52 of 289 launch sites in test\win32 declare nothing about session persistence, so each of them may be asserting against panes the previous section restored. It has been red silently because persistence-flag.ps1 is NOT in the coverage table in scripts\guard-due.ps1 - every other sweep of this shape (stderr-launch-capture, cleanslate, verdict-exit) is, so a new script that forgets the declaration is caught the same turn, while this one drifted from 0 to 52. Two halves: declare all 52 sites (flag, splat, helper callers, or a '# persistence: <reason>' marker - since T697 one marker on a helper's header covers every launch inside it), then add a persistence-flag row to guard-due covering test\win32\*.ps1 + lib\PersistenceSweep.ps1 so it can never drift again. Register the guard only once the sweep is green, or validate wedges the loop on a red that has no owner.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-09-12 10:54: status: todo -> skipped(duplicate of T1012 - same red sweep, filed before reading the SIMILAR block through) (by T697 turn)
