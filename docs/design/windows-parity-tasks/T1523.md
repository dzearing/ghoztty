---
id: T1523
title: "Work off the guard-due backlog the T1012/T1517 sweeps left (21 rows)"
order: null
deps: ["T1012"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "the guard-due hatch is being pressed every turn, which makes the gate informationless"
tags: ["test", "infra"]
---

# T1523 - Work off the guard-due backlog the T1012/T1517 sweeps left (21 rows)

## Summary

The 2026-09-12/13 harness sweeps (T1012's persistence declarations, T1517's job teardown) edited ~27 acceptance scripts and the code under several agent/install/session rows, and the guard stamps for those rows were never refreshed - so every turn since has opened with 20+ GUARD DUE rows and closed with -NoGuardDue. A hatch pressed every turn carries no information, which is the exact failure mode the advisory tier was invented for. The rows still due: agent-autostart, agent-handoff, agent-recovery-tabs, holder-adopt, holder-durable, holder-volume, session-vanished, relay-throughput, upgrade-staleness, install-restart, install-maintenance, startup-failure, update-real-msi, sessions-running-cmd, window-name-env, session-layout-preserve, restore-late-agent, chooser-session-sort, plus the three advisories (release-artifacts-packaging, rdp-session, install-walkthrough). Wanted: run each one, fix whatever it catches, and let it stamp - or, where a row genuinely cannot answer on this box, say so and move it to the advisory tier with the reason. Done when a clean validate needs no -NoGuardDue.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
