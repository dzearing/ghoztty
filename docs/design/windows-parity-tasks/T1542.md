---
id: T1542
title: "Dragging a tab\u0027s only pane onto the strip does nothing instead of moving its tab"
order: null
deps: ["T1537"]
status: "in-progress"
commits: []
seat: "win"
milestone: "M1"
priority: "P2"
triage-reason: "rearrange gap: a tab\u0027s only pane cannot be dropped on the strip, so the gesture dead-ends"
tags: ["feature"]
---

# T1542 - Dragging a tab's only pane onto the strip does nothing instead of moving its tab

## In plain terms

In rearrange mode you can drag a pane up onto the tab bar to give it a tab of
its own. If the pane is the only one in its tab it is already a tab of its own,
so Ghoztty refuses - correctly, since there is nothing to split off. But the
gesture the user just made was "put this tab here", and the obvious thing to
do with it is MOVE the tab to where they pointed. Today it does nothing at all,
which reads as the drag being broken rather than as the pane already being
where it was asked to go.

## Summary

T1537's `Window.canNewTabDrop` refuses a new-tab drop when the dragged pane is
its tab's whole tree, and `drop_highlight.forTarget` reads the same answer so
no caret is drawn. That is the right refusal for "make a new tab" and the wrong
answer for the gesture: the strip already reorders tabs by dragging their
BUTTONS (`Window.drag_tab`), so the destination is meaningful - it is just that
the pane drag does not reach it.

The change is to resolve the drop to a tab REORDER instead of a refusal when
the dragged pane is its tab's only leaf: preview the same caret, and commit by
moving the source tab's slot to that index rather than by opening a new one.
`removeTabSlot` and `insertPaneAsTabAt` are already the two halves of that move.

## Goals

- [x] Dragging a lone pane onto the strip previews the seam the way a new-tab
      drop does.
- [x] Releasing MOVES that pane's tab to the index under the pointer.
- [x] Dropping it back where it already is changes nothing and is not an error.
- [x] A tab moved this way keeps its title, its colour, its pin and its
      session.

## Validation criteria

- [x] The seam arithmetic (insertion seam -> final index, the two "right
      here" seams answering no-op, clamping, a stale source refused) is unit
      tested in `pane_relocate.zig` (`tabReorderIndex`, four tests, none + win32
      lanes via `floor-lane.ps1 -Lane all`).
- [x] Dropping a lone pane on the seam where its tab already is previews
      nothing and changes nothing - claim C of `test\win32
earrange-tab-drop.ps1`
      (tab count 2, selected index still 1, same pane).
- [x] Dropping it on the seam before tab 0 draws the caret on that seam and
      MOVES the tab there: still two tabs, the carried tab is index 0 and on
      screen, holding the same pane HWND - claim C2 of the same script, ALL
      PASS (73 assertions).
- [x] `-NegativeControl` scores red on C2 ("a lone pane previews nothing",
      the pre-T1542 behavior) as well as on A.
- [x] Title / colour / pin / session travel with the tab: the commit is
      `moveTabTo`, the same whole-slot move the tab-button drag uses, which
      carries every per-tab array; the pane HWND identity in C2 is the session
      half.

## Progress log
- 2026-09-25 07:29: claimed; work starting.
- 2026-09-25 07:43: root cause: canNewTabDrop refused any strip drop for a lone pane. Fix: pane_relocate.tabReorderIndex maps the seam to a final index (null = where it already is); canNewTabDrop now takes the seam; commitNewTabDrop on at==.root selects the source tab if a dwell moved off it, then moveTabTo. Built; rearrange-tab-drop ALL PASS 73 with new claim C2; -NegativeControl red on C2. Floor lanes running.
