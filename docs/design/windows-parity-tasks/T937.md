---
id: T937
title: "Retire the composer\u0027s RichEdit machinery once the web composer owns the text"
order: null
deps: ["T934", "T935", "T936"]
status: "skipped(split -\u003e T1702, T1703, T1704)"
commits: []
seat: "win"
priority: "P2"
triage-reason: "split out of T830 as its cleanup tail; nothing a user can see changes, so it stays out of M1 while its three siblings carry the parent's milestone"
tags: ["infra"]
---

# T937 - Retire the composer's RichEdit machinery once the web composer owns the text

## Summary

The tail of the rebuild: with the web composer carrying the text, the quotes and the chips, the RichEdit subclass, richedit_tom.zig, the TOM undo suspension, the UTF-16 caret arithmetic and the derive-quotes-from-text matching in viewer_feedback_doc.zig are all dead weight, and dead weight that still compiles is dead weight nobody notices is wrong. Delete them, re-point the viewer-feedback guard-due Covers row at the files that now carry the behaviour, and rewrite the harness arms that drove the control directly.

Split out of T830; read its `## Design` section first.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-19 02:04: T934 kept the RichEdit as a runtime FALLBACK rather than deleting it: it is created hidden, shown only when the pane has no WebView2 environment or the controller is refused, and forced by GHOZTTY_COMPOSER_SURFACE=richedit. test\win32\viewer-feedback.ps1 pins itself to that fallback (window messages cannot drive a Chromium window off the input desktop) while test\win32\viewer-composer.ps1 covers the web surface. This task removes the fallback, the env override, the ~700 lines of RichEdit machinery in ViewerFeedbackBar.zig, and re-points viewer-feedback.ps1's editing arms - or retires them in favour of in-process coverage.
- 2026-08-22 10:05 [session ba55183b]: T1102 added four more scripts to the re-point list: test\win32\viewer-feedback-capture.ps1, -images.ps1, -carousel.ps1 and -utf16.ps1 now call Set-ComposerSurface 'richedit' from test\win32\lib\ComposerSurface.ps1 and assert the surface they got. When this task deletes the fallback and GHOZTTY_COMPOSER_SURFACE, those four (plus viewer-feedback.ps1) are exactly the callers to re-point; grep for Set-ComposerSurface to find them.
- 2026-09-23 14:53: claimed; work starting.
- 2026-09-23 14:54: re-verified: the fallback is still live (ViewerFeedbackBar.zig openComposer, forcedRichEdit) and six harnesses pin to it. Too big for one context: split into T1702 (a DevTools-protocol driver so scripts can type into the web composer off the input desktop; feasibility risk noted - webview2.zig bypasses WebView2Loader so the ADDITIONAL_BROWSER_ARGUMENTS env var may be ignored), T1703 (re-point the six harnesses), T1704 (the deletion).
- 2026-09-23 14:54: status: in-progress -> skipped(split -> T1702, T1703, T1704) (by cli: David, from powershell, in ghoztty)
