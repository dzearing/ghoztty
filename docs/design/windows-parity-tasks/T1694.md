---
id: T1694
title: "Mac: confirm a +new-window issued during local-agent startup still gets session persistence"
order: null
deps: []
status: "todo"
commits: []
seat: "mac"
priority: "P2"
tags: ["test"]
---

# T1694 - Mac: confirm a +new-window issued during local-agent startup still gets session persistence

## Summary

Windows T1688: a +new-window served while the app was still resolving its local agent opened without persistence (session_id null), 2 runs in 5. The Windows fix holds the request until the resolve finishes. Check whether the Mac LocalAgentManager path has the same window (IPC served while the first agent connect is in flight) and, if so, port the hold; if not, record why. Repro shape: cold launch + immediate ghoztty +new-window, then +list --json session_id.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
