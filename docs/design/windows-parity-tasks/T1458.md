---
id: T1458
title: "Claude Code in a Ghoztty pane lags versus native conhost on a streamed desktop"
order: null
deps: ["T1460", "T1463", "T1464", "T1465", "T1466"]
status: "done"
commits: []
seat: "win"
priority: "P0"
user-report: true
triage-reason: "reported interactively by the user on 2026-09-08; flagging so closing it asks for the release that carries the fix"
tags: [perf, renderer, opengl, conpty]
---

# T1458 - Claude Code in a Ghoztty pane lags versus native conhost on a streamed desktop

## Summary

Running Claude Code inside a Ghoztty pane feels **laggy**; the same workload in
a native `cmd` / conhost window on the same machine feels markedly faster.
Reported interactively 2026-09-08 on the installed release (build 2026-09-05),
Windows 11 26100.9278.

P0 because this is the user's primary terminal and the defect is in the
interaction they spend all day in, not in a corner of the chrome.

## In plain terms

Type into Claude Code inside Ghoztty and the screen does not keep up the way a
plain `cmd` window does - text arrives in visible lurches instead of flowing.
Nothing is broken and nothing is lost; it just feels heavy, and it feels lighter
in the terminal Windows ships with. When this lands, a Ghoztty pane should feel
at least as immediate as conhost for the same output.

## What was already RULED OUT - do not re-investigate

The reporter's own hypothesis was that Ghoztty had fallen back to software
OpenGL. **It has not.** Measured on the live process (pid 24300) 2026-09-08:

```
opengl32.dll   C:\WINDOWS\SYSTEM32\opengl32.dll                  10.0.26100.9278
nvoglv64.dll   ...DriverStore\...\nv_dispi.inf_amd64...\nvoglv64.dll  32.0.15.9649
```

- The loaded GL is the **NVIDIA hardware driver**, reached through System32's
  real `opengl32.dll`.
- The shipped Mesa fallback (`<install>\gl\opengl32.dll`, 17 MB, from T1252) is
  present on disk but **is not loaded into the process**.
- The box has an RTX 4090. `gl_loader`'s fallback path (T1251) did not trigger.

So this is NOT llvmpipe, and any work that starts from "we are on the software
rasterizer" is starting from a false premise. Method worth reusing: the loaded
module list is the decisive oracle here, and it is one PowerShell call -
`(Get-Process ghoztty).Modules | Where-Object ModuleName -match 'opengl|nvoglv'`.
It beats reading the log, which on this build carries
`renderer=renderer.generic.Renderer(renderer.OpenGL)` but no vendor/renderer
string.

## The environment, which is probably load-bearing

`Get-CimInstance Win32_VideoController` on the reporting box returns THREE
adapters:

```
Virtual Desktop Monitor           10.54.50.446
Parsec Virtual Display Adapter    0.45.0.0
NVIDIA GeForce RTX 4090           32.0.15.9649
```

`SESSIONNAME=Console` and no `mstsc`/`msrdc`/`rdpclip` is running, so this is
not classic RDP - it is a **streamed console session** (Parsec and/or Virtual
Desktop). That matters because it changes what "presenting a frame" costs:

- An OpenGL window renders on the NVIDIA adapter and then has to reach a
  **virtual display** owned by a different driver. If the compositor is doing a
  cross-adapter copy per frame, every frame pays a GPU→CPU→GPU trip that a
  native desktop never pays.
- The streamer then has to **capture and encode** whatever changed. A
  GL-composited window and a GDI/DirectWrite window (conhost) are not equally
  cheap to capture, and conhost's damage rectangles are far smaller than a
  full-surface GL swap.

That is a hypothesis, not a finding. **Establish it or kill it first**, because
it decides whether the fix is in Ghoztty's frame pacing or in how Ghoztty
presents on a virtual display.

## Investigation order

Measure before changing anything. The instrumentation already exists.

1. **Get the numbers with `GHOZTTY_PERF` set.** `renderer/OpenGL.zig`'s `perf`
   struct (T40/T48/T1147) logs, once per second per pane:
   `fps`, the longest inter-frame gap, and the **max SwapBuffers-to-SwapBuffers
   stall**, each tagged `pane=<uuid>`. Group by pane and read only the pane the
   load is in - the median across panes is meaningless when idle panes sit at
   1 fps (that mistake is documented in the same comment block).
   `renderer/Thread.zig` adds `wakeups_per_s`.
   **A long SwapBuffers stall is the direct signature of the present being the
   bottleneck**, which is what the virtual-display theory predicts.
2. **Compare against conhost on the same desktop**, same output, same window
   size. Without that control the numbers say nothing - the reporter's whole
   claim is relative.
3. **Find out which monitor the pane is on.** Move the Ghoztty window to the
   physical 4090-attached display (if one is attached) and to the virtual
   display, and take the same measurement on each. If the stall follows the
   virtual display, the theory above is confirmed and the work is about
   presentation, not about the terminal.
4. **Characterise Claude Code's output shape.** It is not a `cat` of a big
   file: it is a spinner ticking several times a second, streaming tokens, and
   frequent full-width repaints of a status area. That is many small ConPTY
   reads, each of which can wake the renderer. Count `wakeups_per_s` against
   frames actually presented - if wakeups vastly exceed useful frames, the
   renderer is being asked to draw far more often than the display can show,
   and coalescing is the lever.
5. **Only then look at the ANSI/parse path.** `termio/Exec.zig`'s `ReadFile`
   loop (around lines 1558-1585) already does a fill-then-parse; confirm with
   numbers that parsing is or is not on the critical path before touching it.
   The reporter named "ansi support that claude code uses" as a suspect, and it
   may well be innocent - do not optimise it on suspicion.

## Fix directions, in the order the evidence would justify them

None of these should be started before step 1-3 above name a cause.

- **If the present is stalling on a virtual display:** consider whether the
  win32 surface should present differently when the window is on an adapter
  that is not the rendering one - fewer, larger presents; or an explicit
  damage-limited blit rather than a full-surface swap.
- **If wakeups vastly exceed presented frames:** coalesce. The renderer thread
  already distinguishes `wakeup` (coalescing) from `draw_now` (not) - see the
  comment at `renderer/Thread.zig:66`. Make sure a burst of small ConPTY reads
  rides the coalescing path and cannot force a present each.
- **If frames are cheap but latency is high:** the problem is scheduling, not
  throughput. Look at how a PTY read gets from `termio` to a present, and how
  much of that is a thread hop.

## MEASURED 2026-09-08 - what the numbers say

All numbers from an isolated Debug build on the reporting box's own streamed
console session (Parsec + Virtual Desktop adapters present, `SESSIONNAME=Console`,
two 3072x1728 screens). Workload: `scratchpad\workload.ps1`, a Claude-Code-shaped
stream - streaming token text, a spinner, and a full-width CR status repaint every
7th write - run byte for byte identically in both terminals, timing ITSELF so the
number does not depend on how the harness launched it. Artifacts under
`%TEMP%	1458\`.

### The A/B, which reproduces the report

| | conhost | Ghoztty pane |
|---|---|---|
| 4,000 chunks | 76 ms | 57 ms |
| 400,000 chunks (~8 MB) | **6,087 ms** | **9,088 / 9,104 ms** (two runs) |

At small sizes Ghoztty is FASTER - both terminals just absorb the burst into the
pipe. Under sustained load Ghoztty takes 1.50x as long on the writer's clock, and
that clock flatters it badly: the writer was released at 9.1 s while the pane was
still parsing the backlog **20+ seconds later**, at a sustained ~50 KB/s. conhost
finished the same 8 MB in 6.1 s (~1.3 MB/s). The lived difference is far larger
than 1.5x.

### The virtual-display hypothesis is DEAD

`swapBuffers` was not instrumented before this task - `max_gap_ms` cannot tell
"idle, nothing to draw" from "the present blocked", so the number the theory
needed did not exist. It does now (`swap_max_ms`, `swap_avg_us`):

- `swap_avg_us` = **15-194 us** (median 28-162 depending on run)
- `swap_max_ms` = **0 in every one of 773 + 467 samples**

The present costs microseconds. There is no cross-adapter stall, no per-frame
GPU->CPU->GPU trip. Investigation step 3 (move the window to the physical display)
is moot: a present that costs 30 us cannot be the cause of a 26x throughput
deficit. **Do not spend another turn on presentation.**

### The real data path had NO throughput telemetry at all

`reads_per_s` / `kb_per_s` came back `NO SAMPLES`, and the log carries no
`io_exec` scope whatsoever. The reason is structural: this pane's PTY is held by
`ghoztty-agent`, so bytes arrive through `termio/Remote.zig`, not through
`Exec.zig`'s ConPTY reader. Every existing "is the terminal keeping up?" number
instruments a path the shipped build does not take. T1458 added the analog
(`perf agent_feed ...`) to `Remote.feedSliced`.

### Where the second actually goes

Sustained seconds of the 400k run, from `perf agent_feed`:

```
kb_per_s=58 slices_per_s=14 feeds_per_s=3 parse_ms_per_s=560 yield_ms_per_s=0
kb_per_s=60 slices_per_s=15 feeds_per_s=3 parse_ms_per_s=521 yield_ms_per_s=0
kb_per_s=50 slices_per_s=12 feeds_per_s=3 parse_ms_per_s=460 yield_ms_per_s=0
```

with the renderer, same seconds: `fps` median **12**, `swap_avg_us` 128-194.

- **`yield_ms_per_s` = 0 everywhere.** The T111b priority handoff between 4 KiB
  slices costs nothing. It is not the problem and does not need revisiting.
- **`parse_ms_per_s` ~= 500.** The pane's io thread is inside
  `processOutputTracked` for roughly half of every wall-clock second while
  moving only ~50 KB.

50 KB/s while spending 500 ms/s "parsing" is ~10 ms per KB. No terminal parser is
that slow, so that time is almost certainly not parsing. `processOutputTracked`
SELF-LOCKS the renderer mutex, so the clock added here covers lock wait plus
parse, and the arithmetic points hard at the first: the renderer ran at 12 fps in
those same seconds, and **12 frames x ~40 ms of held mutex = ~480 ms/s**, which
matches `parse_ms_per_s` almost exactly. The io thread looks starved by the
renderer holding the pane mutex across a slow `drawFrame` - not by parsing, and
not by the present, which is 0.03 ms of that frame.

**That is a hypothesis with a strong quantitative match, not a finding**, because
the instrument conflates lock wait with work. Splitting the two is the next
measurement and is filed as its own task; `IpcHandlers.zig` already does exactly
that split (T111b) and is the pattern to copy.

### The split came back, and the hypothesis above is WRONG (T1460, 2026-09-08)

`perf agent_feed` now reports `lock_ms_per_s` and `work_ms_per_s` beside
`parse_ms_per_s`. Same workload, same box. Across the 28 sustained seconds:
**lock wait 12 ms total (0.08%), work under the lock 14,450 ms (99.83%)**.

```
kb_per_s=61 slices_per_s=15 feeds_per_s=3 parse_ms_per_s=605 lock_ms_per_s=1 work_ms_per_s=604 yield_ms_per_s=0
```

The io thread is **not** starved by the renderer holding the pane mutex. The
"12 fps x ~40 ms = ~480 ms/s" match was a coincidence of two numbers that happen
to be about half a second. The drain really is inside `processOutputLocked`
doing work: ~40 ms per 4 KiB slice, ~10 us per byte. So of the three candidates
this card opened with - present, wakeups, parse - **it is the parse path**, and
T1461 is pointed there.

**Caveat carried forward:** every number in this section, including the A/B table
above, is off a **Debug** build whose own log says performance will be very poor.
The lock-vs-work *ratio* does not care; the absolute rates and the 26x gap do.
T1463 re-measures on an optimized build and blocks T1461 for that reason.

### The optimized re-measurement changed the answer (T1463, 2026-09-08)

Every rate in the two sections above is **Debug-only and does not describe the
build the reporter runs.** On a ReleaseFast build of the same tree, in the
release sandbox, same workload and same desktop:

- The parse path costs **0.06-0.07 us/byte** rather than ~9.7, and
  `parse_ms_per_s` is **41-43** rather than ~500-600. The pane no longer trails
  the writer by 20 seconds; it finishes with it. **The parser is not the defect**
  and T1461 was closed as superseded for that reason.
- Lock wait is 2.6-2.8% of the drain (0.08% in Debug) - a different number, the
  same verdict as T1460.
- The deficit the report describes is still real, and it is **the agent relay**:
  the same optimized build runs the same workload in **6.1-6.5 s with a local
  ConPTY** and **12.7-12.9 s when `ghoztty-agent` holds the PTY**, against
  conhost's 7.9-8.5 s. Shipped panes are the agent-held kind. Filed as **T1464**,
  which this card now waits on.
- Burst presents (T1462) are **2,905-4,192 fps** optimized, not 421-514.

Full table and method: T1463.

### Burst behaviour, separately

On the SHORT run the loaded pane hit **421-514 fps** with `wakeups_per_s` tracking
it one-for-one. Under sustained load it settles to 12, so over-draw is not the
sustained defect - but a burst presenting 500 frames a second is wasted work
everywhere, and on a streamed desktop it also forces the capture encoder to
re-grab the window 500 times. Filed separately rather than folded in here.

## WHERE THIS ENDED UP (2026-09-09, the umbrella closing)

All five sub-tasks are resolved, and the answer is not the one this card opened
with. In order, each step killed the previous step's leading theory:

| Suspect | Verdict | Number that settled it |
|---|---|---|
| Software OpenGL | dead | NVIDIA `nvoglv64.dll` loaded, Mesa fallback not |
| The present / virtual display | dead | `swapBuffers` 15-194 us, `swap_max_ms` 0 in 1,240 samples |
| Renderer-mutex starvation | dead (T1460) | lock wait 0.08% of the drain, work 99.83% |
| The ANSI parser | dead (T1463) | 0.06-0.07 us/byte optimized, ~4% of a second |
| The relay's framing / message count | dead (T1464) | every leg <7 us of pipe syscalls, all at the same rate |
| **The pty holder's CPU placement** | **THE CAUSE (T1465)** | shell->conhost round trip 122 us on an E-core vs 55 us on a P-core |

**What shipped:** `src/os/power.zig` derives the performance-core set from
`GetSystemCpuSetInformation`'s `EfficiencyClass` (a no-op on a non-hybrid CPU),
and the pty holder pins itself to it before creating the ConPTY, so the conhost
behind every persisted pane runs where its round trip is cheap. The user's own
shell is handed the full system mask back, per **D94**. Also landed on the way:
one pipe write per OUTPUT frame instead of three, DATA coalescing in the agent's
writer, and 64 KiB / 200 ms ACK batching (T1464) - all backlog-only, so no
keystroke echo pays for any of it.

**What it bought**, optimized build, release sandbox, same 7.7 MB burst, `A` a
local-ConPTY pane, `B` the shipped agent-held pane, `D` conhost:

```
BEFORE   B = 11588 - 11964 ms    B/A = 1.84 - 2.02
AFTER    B =  8990 -  9909 ms    B/A = 1.51 - 1.58     A = 5800-6269   D = 6318-6372
```

So the shipped pane went from **~1.9x conhost to ~1.45x**, and
`pane-ingest-ab.ps1`'s `$relayRatioBound` came down 2.50 -> **1.75**, which is
above every measurement of the fixed path and below every measurement of the
broken one - the arm now fails if this fix stops working, not only if something
new breaks.

**What is deliberately left on the table.** Closing the last of the gap needs the
user's own shell subtree pinned to the performance cores too, which would cost a
build inside a persisted pane half the machine. That was put to the user as
**D94** and they chose to leave commands on the whole machine, so **1.75 is the
permanent bound**, T1466 is closed as `skipped`, and this is as far as the ratio
goes by design rather than by exhaustion. The one open follow-up is **T1462**
(a short burst presenting 500 frames a second) - real waste, but measured NOT to
be the sustained defect, so it stands on its own.

**The last word belongs to the reporter.** Every number here is off the box's
background test desktop, where the streamed-display condition the report was made
under does not exist. Closing this files the publish request (it is a
`user-report`), so the fix reaches their installed terminal and the confirmation
that matters is them typing in it.

## Parity note

Standing symmetry rule. The macOS build uses Metal and has no equivalent of
either the WGL present path or a Parsec virtual display, so a fix here is
expected to be win-only. **Confirm rather than assume**, and if the Mac seat
shows the same lag under screen sharing, file its half.

## Validation criteria

- [x] A `GHOZTTY_PERF` capture of the laggy workload, grouped by the loaded
      pane, recording fps, max inter-frame gap, max SwapBuffers stall and
      `wakeups_per_s` - **before** any change. **Done 2026-09-08**; the
      SwapBuffers stall did not exist as an instrument and was added here
      (`swap_max_ms` / `swap_avg_us` in `renderer/OpenGL.zig`).
- [x] The same capture for the same workload in conhost, as the control the
      whole report rests on. **Done**: 6,087 ms vs 9,088 ms for 400k chunks,
      same desktop, byte-identical workload.
- [x] The cause is **named with numbers**, not narrated: which of present,
      wakeup rate, or parse is on the critical path, and by how much.
      **Done, and the answer moved twice before it settled.** T1460 ruled out
      renderer-mutex starvation (lock wait 0.08% of the drain) and pointed at
      the parser; T1463 re-measured on an OPTIMIZED build and the parser came
      back at 0.06-0.07 us/byte, ~4% of a second, so that was a Debug artifact;
      T1464 ruled out the relay's framing (every leg under 7 us of pipe
      syscalls); **T1465 named the cause with numbers** - the pty holder's
      process tree ran on the CPU's efficiency cores, where the shell->conhost
      round trip behind every ~73-byte chunk costs ~122 us against ~55 us on a
      performance core. Present, wakeups, the T111b handoff, the parser and the
      renderer mutex are all measured out.
- [x] The virtual-display hypothesis is explicitly confirmed or killed, with
      the on-physical-display measurement that decides it. **KILLED** by a
      cheaper and stronger measurement than the one proposed: the present
      itself costs 15-194 us and `swap_max_ms` is 0 across 1,240 samples, so
      no per-display comparison can rescue the theory.
- [~] After the fix, the same capture shows the improvement, and the reporter
      confirms by eye that a Ghoztty pane no longer feels heavier than conhost.
      **Half met, and the other half is the user's.** The capture is in *Where
      this ended up*: the shipped agent-held pane went from 11,588-11,964 ms to
      8,990-9,909 ms on the same 7.7 MB burst, ~1.9x conhost down to ~1.45x, and
      `pane-ingest-ab.ps1` now arms at 1.75 so a regression fails a harness. The
      by-eye confirmation cannot be produced here - the box runs acceptance
      scripts on a background desktop where the streamed-display condition does
      not exist - so closing this files the publish request that puts the fix in
      the reporter's own terminal.
- [x] No regression in the resize paint work (T1031, T1392, T1393) - those
      share the present path; re-run `test\win32\resize-flicker.ps1`.
      **Done 2026-09-09**: ALL PASS (21 assertions).
- [x] `powershell -NoProfile -File scripts\floor-lane.ps1 -Lane all` green, and
      `test\win32\ipc-p1/p2/p3.ps1` green.
      **Done 2026-09-09**: lib/none/win32/agent all PASS, P1 ALL PASS (25),
      P3 ALL PASS (16), P2 ALL PASS (20) - P2 scored `1 FAILURE(S) (0
      assertions passed)` on its first run of the day, i.e. it died in
      preflight before any check, and then passed twice on the same code
      including an explicit p1-then-p2 sequence. Filed as **T1475** rather
      than waved past.

## Progress log

- 2026-09-08: filed from an interactive report. Software-GL hypothesis measured
  and DISPROVED (NVIDIA `nvoglv64.dll` loaded, Mesa fallback not); streamed
  console session with Parsec/Virtual Desktop virtual display adapters
  identified as the leading hypothesis to test first.
- 2026-09-08 11:14: claimed; work starting.
- 2026-09-08 11:33 [session 56816bf90d8c2a8db0a84af2d4ac9fd7]: Instrumented the present path: OpenGL.zig's perf struct now times swapBuffers itself and logs swap_max_ms + swap_avg_us beside fps/max_gap_ms. Reason: max_gap_ms cannot tell 'idle, nothing to draw' from 'the present blocked', so the existing telemetry could not answer step 1's question at all (the task file's claim that a SwapBuffers stall was already logged was wrong). Debug win32 build green. HARNESS TRAP HIT AND FILED: the first measurement script did not dot-source test\win32\lib\Isolation.ps1, so its CLI calls inherited GHOZTTY_IPC_SOCKET from this pane and the +split landed in the USER'S installed release, splitting the live go-loop window. Cleaned up (+close --target, stray debug pid killed); rewrote the script on Set-GhozttyTestIsolation/Reset-GhozttyTestState.
- 2026-09-08 12:29: user-report: true (closing this task now asks for the release that carries the fix)
- 2026-09-08 12:49 [session 56816bf90d8c2a8db0a84af2d4ac9fd7]: MEASURED. Present hypothesis KILLED: swapBuffers costs 15-194us, swap_max_ms=0 in all 1240 samples. A/B reproduces the report: 400k-chunk workload 9088/9104ms in a Ghoztty pane vs 6087ms in conhost, and the pane was still draining 20s after the writer was released, at ~50KB/s vs conhost's ~1.3MB/s. Found the shipped data path (agent-held ConPTY -> termio/Remote.zig) had NO throughput telemetry - Exec.zig's reads_per_s instruments a path this build never takes - and added perf agent_feed (kb/s, slices/s, feeds/s, parse_ms/s, yield_ms/s). yield_ms_per_s=0 everywhere, so the T111b handoff is exonerated. ~500ms of every second is inside processOutputTracked while moving 50KB; that call self-locks, and 12 fps x ~40ms held mutex = ~480ms/s matches it almost exactly, so renderer-mutex starvation is the leading reading - filed as T1460 (split lock wait from work) rather than claimed. Filed T1461 (the throughput deficit) and T1462 (burst presents at 421-514 fps). Floor all 4 lanes PASS, ipc-p1/p2/p3 ALL PASS, resize-flicker ALL PASS, pane-ingest-lag and session-relaunch guards run green and re-stamped.
- 2026-09-08 12:53: status: in-progress -> todo (by measurement phase complete and committed b5ea07168; the actionable work is T1460 then T1461, so the umbrella waits on them instead of staying claimed)
- 2026-09-09 07:40: claimed; work starting.
- 2026-09-09 07:40 [session 7af3d725]: All five deps are resolved (T1460/T1463/T1464/T1465 done, T1461 superseded, T1466 skipped per D94). Nothing left to build here: the fix that moved the number shipped under T1465 (performance-core placement for the pty holder), and D94 fixed the remaining gap as accepted. This turn is the umbrella wrap-up - re-run the validation the card still owes and consolidate the result.
- 2026-09-09 08:04: status: in-progress -> done (by all five deps resolved; the fix shipped under T1465 and D94 fixed the remaining gap as accepted; floor + ipc + resize-flicker green this turn)
- 2026-09-09 08:04: publish request filed: T1458: Claude Code in a Ghoztty pane lags versus native conhost on a streamed desktop
