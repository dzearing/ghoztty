---
id: T1494
title: "Convert the session-* acceptance suites onto their own agent lineage (T691 tranche 2)"
order: null
deps: ["T691"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
tags: ["test", "infra"]
---

# T1494 - Convert the session-* acceptance suites onto their own agent lineage (T691 tranche 2)

## In plain terms

Our automated tests still shut down the background helper that keeps the
developer's own terminal sessions alive, just to run. T691 built the way out
and converted two scripts as the worked example; this is the next batch - the
session and layout restore suites. When it lands, none of them interrupts real
work, and any two of them can run at the same time.

## Details

The conversion is three lines per script, and T691 landed the machinery plus
`test\win32\session-open.ps1` and `test\win32\session-close.ps1` as the worked
examples - diff either one to see the whole shape:

1. `[void](Set-GhozttyTestAgentLineage -Tag '<suite>')` immediately after the
   `lib\CleanSlate.ps1` dot-source, with `lib\TestDesktop.ps1` dot-sourced
   beside it (the scoped kill asks it which `ghoztty.exe` pids are ours, and
   REFUSES rather than widening if it cannot be asked).
2. `-ScopeToLineage` on every `Stop-RepoGhoztty` / `Reset-GhozttyTestState`.
3. `Get-GhozttyAgentStateDir -Root $tmp` in place of every hardcoded
   `ghoztty\local-agent-debug`, which no longer names this run's dir.

Read each converted suite's result rather than accepting it: a suite that was
silently non-persistent all along (no agent could start, so the app fell back to
exec panes) starts actually exercising persistence here, and its assertions may
change. That is the point. `session-open.ps1`'s explicit "the pane really is
agent-backed (port.json written)" assert is the shape a converted suite carries.

Scripts in scope (18):

- `test\win32\session-crash-recover.ps1`
- `test\win32\session-layout-preserve.ps1`
- `test\win32\session-persistence.ps1`
- `test\win32\session-reattach-zombie.ps1`
- `test\win32\session-reattach.ps1`
- `test\win32\session-relaunch-notify.ps1`
- `test\win32\session-relaunch.ps1`
- `test\win32\session-resume-offset.ps1`
- `test\win32\session-snapshot-reattach.ps1`
- `test\win32\restore-late-agent.ps1`
- `test\win32\layout-blobs.ps1`
- `test\win32\layout-blob-cross-lineage.ps1`
- `test\win32\layout-capture-cost.ps1`
- `test\win32\persistence-flag.ps1`
- `test\win32\rearrange-session-drop.ps1`
- `test\win32\window-name-restore.ps1`
- `test\win32\window-name-env.ps1`
- `test\win32\viewer-restore.ps1`

## Validation criteria

- [ ] Each converted suite passes STANDALONE
- [ ] Each converted suite passes while another converted suite runs
- [ ] `test\win32\agent-lineage-suites.ps1` still ALL PASS
- [ ] No script in this tranche kills an agent it did not start
