---
id: T1611
title: "The tab tooltip shows the full home path where it should abbreviate to a tilde (tab-tooltip guard is red)"
order: null
deps: []
status: "skipped(duplicate of T1395)"
commits: []
seat: "win"
priority: "P2"
tags: ["fix", "test"]
---

# T1611 - The tab tooltip shows the full home path where it should abbreviate to a tilde (tab-tooltip guard is red)

## Summary

test\win32\tab-tooltip.ps1 is 2 FAILURE(S) / 18 passed on the branch head, and its guard row was already DUE at the top of the 2026-09-16 turn (the script itself had been edited and never run green since), so nothing in flight caused it. Arms A and E both read the tooltip as the full path where they expect a leading tilde: `C:\Users\David\AppData\Local\Temp\ghoztty-tabtip-<pid>\tipA<n>\n~\AppData\Local\Temp\ghoztty-tabtip-<pid>\tipA<n>`. The captured text is two lines - a full path and a tilde-abbreviated one - so the open question is whether the app stopped abbreviating the FIRST line, or whether the arm's matching was written for a one-line tip and the second line is new. Establish which, then fix the side that is wrong and re-stamp the guard, so `parity-tasks.ps1 validate` stops needing `-NoGuardDue` for this row.


## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-09-16 08:41: status: todo -> skipped(duplicate of T1395) (by filed and closed in the same turn: SIMILAR named T1395, which is the same tilde-abbreviation defect (now 2 assertions rather than 4))
