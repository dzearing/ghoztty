---
id: T1418
title: "win32 machine pill names a relay machine by device id, not the friendly name the chooser shows"
order: null
deps: ["T610"]
status: "done"
commits: ["ccef8a1a1"]
seat: "win"
priority: "P2"
milestone: "M1"
triage-reason: "User-facing parity: Mac's pill shows the machine's friendly name; on a relay machine ours shows an id nobody chose. P2 because the chip is correct and useful today for the tcp case and merely unfriendly for the relay one."
tags: ["fix", "polish"]
---

# T1418 - win32 machine pill names a relay machine by device id, not the friendly name the chooser shows

## In plain terms

A remote window's titlebar now says which machine it is running on. When you
reached that machine by hostname it says something you recognise
(`build-agent-1`). When you reached it through the relay it says the device id
— a long stable string nobody picked — even though the machine chooser you
opened it from listed it as "MaximusHome" two seconds earlier. The same box
should have the same name everywhere in the app.

## Goals

- [x] A relay window's pill, tooltip and close confirmation name the machine
      the way the chooser lists it
- [x] Windows that never saw a listing (restored, CLI-opened) are named from
      the remembered machine list
- [x] A rename on the relay reaches windows that are already open
- [x] A machine nobody has named falls back to its own hostname, then its id

## Summary

Filed out of T610 (2026-09-07). `Window.machineDisplayName` returns
`RemoteMachine.displayName()`, which is the hostname for `.tcp` and the DEVICE
ID for `.relay`. That is the app's single naming derivation (T1390) and the
close confirmation and the pill tooltip speak it too — so this is one fix in
one place, not three.

The friendly name exists only in the relay directory
(`relay_directory.Device.name`, `GET /v1/client/devices`), which the machine
chooser fetches and the Activity Monitor's carousel fetches again. A window
holds no copy: nothing records `dev.name` at dial time, and nothing caches an
id -> name map, so a window that was restored at startup — or opened by
`+new-remote-window --device=…` — has never seen the directory at all.

## Details

Two candidate shapes, and the second is probably right:

1. **Record the name at dial time.** `MachineChooser.openRelayWindow` has
   `dev.name` in hand; carry it into `setRemoteMachine` and own it like the
   other strings. Cheap, and wrong on its own: it answers for windows the
   chooser opened and for no others, so the same machine would be named two
   ways depending on how the window was born.
2. **A small persisted id -> name map**, written whenever a directory listing
   is parsed (the chooser and the Activity Monitor both already do it), read by
   `displayName`. That makes the name available to a restored window, to a
   window opened from the CLI, and to the close confirmation, from one store.
   `host_defaults.zig` is the precedent for a per-machine store keyed on the
   device id, and `RemoteMachine.hostDefaultsKey` already computes that key.

Whichever shape: `displayName` stays the ONE derivation. A rename on the relay
should reach the pill at the next listing rather than pinning whatever the name
was when the window opened, and a machine we have never listed must still be
namable — falling back to the device id, which is what happens today.

Mind the borrow contract: `displayName` currently returns a slice of the
window's own strings and its doc says so. A map-backed name changes who owns
that memory.

## Resolution

Shape 2, with the store that already existed: the chooser's remembered-machine
cache (`machine_cache.zig`, T711) IS a persisted, account-scoped id -> name map,
so no second one was added.

- `RemoteMachine.relay` owns a `name`; `displayName` answers `name orelse
  device`, so it still borrows the window's own strings (the borrow contract
  holds) and stays the ONE derivation for the pill, its tooltip and the close
  confirmation.
- `setRemoteMachine` names the machine in Mac's order: the remembered directory
  name for the signed-in account (`machine_cache.loadName`), else the hostname
  the agent sent in its HELLO, else nothing (device id). A carried name — T68's
  "New Window" copies the parent's — wins.
- `Window.adoptRelayNames` renames every open relay window a listing names, and
  refreshes its pill. Fed by all three listing paths: the chooser's async fetch
  (`onDevices`, which is also where the LAUNCH warm lands, so a restored window
  is renamed there), its synchronous fetch, and the Activity Monitor carousel
  (names that filled the fixed buffer are skipped rather than shown clipped).
- The directory name is never the account-less wildcard: `machine_cache.load`
  keeps its account scoping.

Follow-up: T1736 (Mac pins an explicit `+new-remote-window --name=` as the
pill's name; Windows does not yet).

## Validation criteria

- [x] A window opened on a relay device shows that device's friendly name in
      the pill, on both a chooser-opened and a CLI-opened window.
      *`test\win32\remote-pill-relay-name.ps1` arm A (CLI-opened, named from
      the remembered list with no listing this run); a chooser-opened window
      takes the same path, since the chooser writes that list before any
      window is dialed from it.*
- [x] A window restored at startup, whose session predates any directory
      fetch, shows the friendly name once a listing has happened.
      *Arm C: windows opened before any listing are renamed by the chooser's
      fetch without being reopened; the launch warm lands on the same
      `onDevices` path.*
- [x] A device with no known name still shows its id, never a blank pill.
      *Mac's order goes through the HELLO hostname first — arm B shows it, and
      never the id; the id fallback is `displayName`'s `name orelse device` plus
      `machine_cache` unit test "nameFor" (blank names count as none).*
- [x] The close confirmation (T1390) and the pill tooltip name the machine the
      same way, from the same call. *By construction: both call
      `machineDisplayName()` → `RemoteMachine.displayName`, which this change
      edits in place; `close-confirm-idle.ps1` ALL PASS 58, `remote-pill.ps1`
      ALL PASS 19 (incl. section 6 tooltip).*
- [x] Floor green: both zig test lanes, `test-agent`, P1-P3.
      *`floor-lane.ps1 -Lane all` lib/none/win32/agent PASS; ipc-p1 26, p2 20,
      p3 16 ALL PASS; harness lane PASS; 21 due guard harnesses ALL PASS.*

## Progress log

- 2026-09-24 20:09: claimed; work starting.
- 2026-09-24 20:30: design settled: shape 2 using the EXISTING remembered-machine cache (machine_cache.zig, T711) as the id->name store rather than a new one. RemoteMachine.relay gains an owned name; setRemoteMachine resolves it (cache for signed-in account -> HELLO hostname -> null=device id, Mac's order); Window.adoptRelayNames renames open windows from every listing (chooser onDevices incl. launch warm, chooser sync fetch, Activity Monitor carousel). Acceptance test\win32\remote-pill-relay-name.ps1 ALL PASS 15, -NegativeControl red.
- 2026-09-24 21:16: status: in-progress -> done (by cli: David, from powershell, in ghoztty) [commit ccef8a1a1]
