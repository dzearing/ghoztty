---
id: T1728
title: "Installing the MSI by hand over a running Ghoztty shows no Files In Use dialog naming it (author MsiRMFilesInUse under wixl)"
order: null
deps: ["T1208"]
status: "todo"
commits: []
seat: "win"
priority: "P3"
triage-reason: "P3: T1208 already tells the user after the fact on both update paths; this is the before-the-close dialog for the by-hand MSI path only, and it needs wixl (Docker) to validate."
tags: ["polish"]
---

# T1728 - Installing the MSI by hand over a running Ghoztty shows no Files In Use dialog naming it (author MsiRMFilesInUse under wixl)

## Summary

T1208 made the close-and-reopen visible AFTER the fact (tray notice on relaunch, both update paths). The user's original ask was the standard Windows Installer Files In Use dialog BEFORE the close, when the MSI is run by hand. That needs a hand-authored Dialog/Control/ControlEvent table (MsiRMFilesInUse) that survives wixl, read back from the compiled MSI like T1176's actions. Needs wixl (Docker) to validate, so deferred; P3 because the after-notice already tells the user.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
