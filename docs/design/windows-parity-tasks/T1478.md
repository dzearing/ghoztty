---
id: T1478
title: "A stop/resume cycle leaves the loop down while every recovery command reports success"
order: null
deps: []
status: "done"
commits: ["8ac61a801"]
seat: "win"
priority: "P1"
tags: ["fix"]
---

# T1478 - A stop/resume cycle leaves the loop down while every recovery command reports success

## Summary

**Observed on 2026-09-09, in a controller session, with the full command trail
below.** A controller stopped the loop to run the floor lanes without two GUI
lanes fighting for the foreground, then resumed it. The loop stayed down for
seven minutes across three recovery attempts, and **every one of them reported
success**. This is the "a step whose failure mode is nothing happens" shape that
T1292 paid for over three days of silent publish skips.

The sequence, with the times:

1. `09:20` The loop's own session behaved **correctly**: after its turn it ran
   `go-loop-exec.ps1 claim`, got exit 4 (STOPPED by request), and per go.md step
   0 refused to pick a task and refused to `/reset-context` (perpetuating the
   loop is exactly what a stop defeats). It unmarked its window, released the
   lock, printed "the loop is parked, not broken", and went idle. Nothing here
   is a defect.
2. `09:41` `go-loop-exec.ps1 resume` -> `RESUMED stop request cleared` /
   `claim will take the loop again on the next turn`. **Exit 0.** But there is no
   next turn: the only session that would claim is idle at its composer and
   nothing re-triggers it. The message states a future that cannot arrive.
3. `09:41:52` `go-loop-watchdog.ps1 -Once -Force` ->
   `re-entering: no live loop pane, opening a new window (state=free)` /
   `new-window exit=0` / `ACTION new-window`. **Exit 0.** No session started.
   `+new-window --target=main` is idempotent by design (CLAUDE.md: named targets
   that already exist are focused, not recreated), and `main` was still occupied
   by the parked-but-idle session from step 1. So the watchdog focused a window
   and called it a re-entry.

Both watchdog log lines are false in that state. There *was* a live loop pane -
it was idle, not absent - and no new window was opened. `new-window exit=0`
reports the window operation, not a loop coming back, so the one number a caller
would gate on is the one number that cannot see the failure. Health kept
answering `DOWN state=free windows=0` while all three commands claimed success.

**What actually fixed it** was sending the go prompt to the parked pane by hand:

```powershell
ghoztty.exe +send-keys --target=main --enter "read go.md and go"
```

after which the next health poll read `HEALTHY uptime=1m turn=1 state=held`.

So the capability is all present; what is missing is that nothing connects
"the stop flag is cleared" to "the idle session in the loop window needs the
prompt typed into it".

Two candidate fixes, and the second is the one that closes the hole:

- `resume` sends the go prompt itself when it can see a marked-or-known loop
  window holding an idle session. Direct, but `resume` currently touches only
  the lock file and gains a dependency on pane state.
- **`go-loop-watchdog.ps1` learns the third case.** It already distinguishes
  "live pane, working" from "no pane at all"; the missing branch is "pane exists,
  session is idle, lock is free" -> send the prompt rather than call
  `+new-window` and declare victory. That is also the branch that makes the
  watchdog's existing re-entry honest, because the same wrong log line would
  print for any idle-occupant case, not just after a stop.

Note the near-miss that makes this worth a test rather than a one-line patch:
the controller's *first* send used `--name=` (which is `+read`'s flag; send-keys
takes `--target=`) and got an **empty exit code** with no visible error, which
looked exactly like the `--when-idle` stall it was not. A fix that shells out to
`+send-keys` and does not assert the pane actually started working will
reproduce this whole failure one level down.

## In plain terms

Telling the loop to pause and then telling it to start again did not start it
again. Every command said it had worked, so there was nothing to look at: the
pause had left the session sitting there doing nothing, and the "start again"
command only unticked a box. Now "start again" actually pokes the waiting
session back into work, and it refuses to claim success until the loop really is
running again - if it cannot get it going, it says so and tells you what to look
at.

## Goals

- [x] Resuming a paused loop restarts it, instead of promising something that
      cannot happen.
- [x] No command on the recovery path can report success over a loop that is
      still down.
- [x] The supervisor stops saying there is no window when there is one.

## Validation criteria

- [x] With the loop parked by a stop and its session idle in the loop window,
      the documented recovery (`resume`, then the watchdog) brings the loop back
      to `state=held` with no hand-typed prompt. `resume` now runs that forced
      watchdog tick itself, so it is one command rather than two.
      Verified: `test\win32\go-loop-resume.ps1` section D (D1-D5), ALL PASS.
- [x] The watchdog never logs `no live loop pane` or `opening a new window` when
      the target window exists and holds an idle session; the idle-occupant case
      logs what it actually did. Verified: section D (D2, D3) for the found pane,
      and section B (B3-B5) for the remembered-but-closed pane, which now names
      the pane it looked up instead of claiming none existed.
- [x] Recovery is gated on the loop reaching `held` (poll the lock, which is
      what `go-loop-health.ps1` reads), not on `+new-window` or `+send-keys`
      exit codes. Verified: section C (C3-C5) - a `new-window` that exits 0 over
      a loop that never comes back scores exit 5 / RECOVERY UNCONFIRMED - and
      section X5b-X5d of `test\win32\go-loop-guard.ps1`, where a resume that
      cannot recover exits 5 with the remedy.
- [x] An acceptance script in `test\win32\` stages the exact state (stop, let a
      session park and idle, resume) and asserts the loop returns to `held`, with
      a negative control proving it goes red against today's watchdog.
      Verified: `test\win32\go-loop-resume.ps1`, ALL PASS (22 assertions); the
      `-NoPaneHistory` negative control in section C is today's watchdog exactly.

## What landed

- `scripts\go-loop-watchdog.ps1`: `Get-HistoryPane` reads the lock's own
  append-only ledger (`go-loop-history.jsonl`) for the last pane the loop ran
  in, so a released lock no longer means "no pane anywhere". The new-window log
  line names what it looked up and why it fell through. `-WaitForHeldSeconds`
  polls the lock after an action and reports RECOVERED / RECOVERY UNCONFIRMED,
  exit 5 under `-Once`. `-NoPaneHistory` is the negative control.
- `scripts\go-loop-exec.ps1`: `resume` clears the flag, then restarts the loop
  through that forced tick, and exits 5 with `RESUME INCOMPLETE` plus the remedy
  when the loop does not reach `held`. It leaves a loop that is already running
  alone, and `-NoRecover` is the flag-only half.
- `test\win32\go-loop-resume.ps1`: new end-to-end harness, stamped in
  `scripts\guard-due.ps1` as its own guard row.

## Progress log

- 2026-09-12 01:16: claimed; work starting.
- 2026-09-12 01:29 [session d13802b5]: root cause: after a stop, the parked session RELEASES the lock, so nothing in the tree names its pane - the watchdog's only pane source is the lock, so it fell through to +new-window, which focuses an existing --target=main window and exits 0. Fix: (1) the watchdog reads the lock's own append-only ledger (go-loop-history.jsonl) for the last pane and decides about THAT pane; (2) -WaitForHeldSeconds gates the verdict on the lock reaching held, exit 5 when it does not; (3) resume now restarts the loop through that forced tick and exits 5 with the remedy when the loop does not come back, instead of promising a next turn that cannot arrive. New acceptance test\win32\go-loop-resume.ps1: ALL PASS (22), with -NoPaneHistory as the negative control reproducing the 2026-09-09 behavior (new-window, RECOVERY UNCONFIRMED).
- 2026-09-12 02:12: status: in-progress -> done [commit 8ac61a801]
