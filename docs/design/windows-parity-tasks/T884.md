---
id: T884
title: "Remaining delivery/loop harnesses have no guard-due rows (morning-refresh, deliver-windows-build, agent-upgrade, relaunch-guard) - an edit to their scripts obliges nobody to re-run them"
order: null
deps: []
status: "done"
commits: ["7b604c0f8"]
seat: "win"
priority: "P2"
triage-reason: "same T783 gap T531 just closed for upgrade-staleness/upgrade-no-fork, in the neighbouring harnesses; latent until someone edits those scripts, so P2 - but the L24 lesson is that these harnesses go quietly red for months when nothing obliges a run"
tags: ["test", "infra"]
---

# T884 - Remaining delivery/loop harnesses have no guard-due rows (morning-refresh, deliver-windows-build, agent-upgrade, relaunch-guard) - an edit to their scripts obliges nobody to re-run them

## Summary

Found during T531 (2026-08-16). The delivery pipeline had NO guard-due
coverage at all: `scripts\upgrade-ghoztty-windows.ps1` could be edited and
committed without any gate demanding its harnesses re-run, which is exactly
how `upgrade-no-fork.ps1` sat red at L24 (a T883-class blind capture) with
nobody the wiser - it is not in the P1-P3 floor and nothing tied an edit to a
run. T531 added rows for `upgrade-staleness` (covers
upgrade-ghoztty-windows.ps1, launch-upgrade.ps1, delivery-version.ps1) and
`upgrade-no-fork` (covers upgrade-ghoztty-windows.ps1, loop-session.ps1).

The same gap remains for the neighbouring harnesses:

- `test\win32\morning-refresh.ps1` <- `scripts\morning-refresh.ps1`
- `test\win32\deliver-windows-build.ps1` <- `scripts\deliver-windows-build.ps1`
- `test\win32\agent-upgrade.ps1` <- the agent-refresh arming half it covers
- `test\win32\relaunch-guard.ps1` <- the relaunch-guard spawn/watch code

For each: add a `$GuardTable` row in `scripts\guard-due.ps1` (keep each row to
the family the harness is ABOUT - noise is how a gate gets ignored) and a
green-run stamp block in the harness tail (skip-aware where the harness counts
skips; see go-loop-guard.ps1's block for the pattern). Then run each harness
green once to seed its stamp, and commit the stamps.

## In plain terms

We have automated test suites for the scripts that deliver new builds to the
user's machine, but until recently nothing forced anyone to actually run them
after changing those scripts - one suite was quietly broken for a long time
and nobody noticed. The tracking system that catches this now covers two of
the suites; this task extends it to the remaining four.

Resolved 2026-09-19: three of the four were already covered or retired, so the
work here was the last one — the suite that proves the terminal comes BACK
after it restarts its background service. Editing that code now obliges
somebody to re-run that suite.

## Validation criteria

- [x] Each of the four harnesses has a guard-due row whose Covers globs name
      the scripts it is about, and a stamp block that only fires on a clean
      green sweep. — `agent-upgrade` (T1037) and `deliver-verify` (T198/T727)
      already had theirs; `morning-refresh` retired with the morning swap under
      D85, and `install-ownership` + `daily-publish` are its replacements;
      `relaunch-guard` landed here.
- [x] `scripts\guard-due.ps1 check` reports each new guard CURRENT after one
      green seeding run, and DUE after touching a covered file. — seeding run
      `test\win32\relaunch-guard.ps1` = ALL PASS (25 assertions) +
      `STAMPED relaunch-guard (3 files)`; `check` then said
      `GUARD CURRENT relaunch-guard (3 files, stamped 2026-09-19 …)`, and with a
      byte appended to `src\apprt\win32\relaunch_guard.zig` it said
      `GUARD DUE relaunch-guard: … changed src/apprt/win32/relaunch_guard.zig`.
- [x] `test\win32\guard-due.ps1` still ALL PASS. — arm H2 (every row names a
      harness that stamps itself) was RED at HEAD over `chooser-roster-push`
      and is green here. The harness is not ALL PASS: A2/C6/C10 were already
      red at HEAD `3afaf4e73` (proven by re-running against the HEAD copy of
      `guard-due.ps1`), are unrelated to this row, and are filed as T1669.

## Progress log

- 2026-08-20 21:31: T1037 closed the agent-upgrade third of this: guard-due row 'agent-upgrade' (covers test\win32\agent-upgrade.ps1, src\apprt\win32\agent_upgrade.zig, src\apprt\win32\LocalAgent.zig) plus a green-run stamp block in the harness tail, seeded green at 9b0d87463. Still open here: morning-refresh, deliver-windows-build, relaunch-guard.
- 2026-09-19 15:21: claimed; work starting.
- 2026-09-19 15:44 [session 9bb8e07a]: Re-verified the card (CHECK FIRST): of the four harnesses named, three are already resolved - agent-upgrade by T1037, deliver-windows-build by the deliver-verify row (T198/T727), and morning-refresh retired with the morning swap itself under D85 (scripts\morning-refresh.ps1 and its harness no longer exist; install-ownership + daily-publish replaced its row). Only relaunch-guard was left, and it had no row and no stamp block.
- 2026-09-19 15:44 [session 9bb8e07a]: Landed: guard-due row 'relaunch-guard' (covers test\win32\relaunch-guard.ps1, src\apprt\win32\relaunch_guard.zig, src\apprt\win32\job_spawn.zig - job_spawn because arm F is exactly the T524 breakaway that was extracted out of relaunch_guard.zig) plus a green-run stamp block in the harness tail. Seeded green: 25/25 ALL PASS, STAMPED relaunch-guard (3 files). Verified both directions: check says CURRENT after the run, and DUE naming src/apprt/win32/relaunch_guard.zig when a covered file is touched. Also fixed H2 of test\win32\guard-due.ps1 in passing: the 'chooser-roster-push' row had a stamp but its harness had no stamp block, so its CURRENT could only ever have been hand-written; chooser-sessions-push.ps1 now re-stamps on a clean green. Remaining 3 reds in that harness (A2/C6/C10) are pre-existing at HEAD 3afaf4e73 - proven by re-running against the HEAD copy of guard-due.ps1 - and are filed as T1669 (the fixture's uncovered probe file is matched by the wildcard audit rows).
- 2026-09-19 16:18 [session 9bb8e07a]: RESUME STATE (context reset at user request, mid-task): the code work is DONE and UNCOMMITTED in the shared tree - scripts\guard-due.ps1 (new relaunch-guard row), test\win32\relaunch-guard.ps1 (stamp block), test\win32\chooser-sessions-push.ps1 (stamp block, H2 fix), test\win32\relaunch-guard.stamp.json (new, the seeded stamp), docs\design\windows-parity-tasks\T884.md, docs\design\windows-parity-tasks\T1669.md (new). Keep all of it; do not reset the tree. Validation already green over this exact tree: relaunch-guard.ps1 ALL PASS (25) + STAMPED, floor-lane -Lane all = ALL LANES PASS (lib/none/win32/agent), guard-due.ps1 acceptance 97 pass / 3 fail where the 3 are pre-existing at HEAD 3afaf4e73 and filed as T1669. STILL OWED before commit: scripts\floor-lane.ps1 -Lane harness (was running at reset; touching test\win32\*.ps1 makes the whole harness-floor family DUE and validate refuses the commit until it is green) and test\win32\ipc-p1.ps1 / ipc-p2.ps1 / ipc-p3.ps1. Then set-status done + log entry + commit via git-commit-guard.
- 2026-09-19 16:19 [session 9bb8e07a]: Harness floor result (post-reset info): FAIL - 3 of 29, 2 PENDING (asserted-nothing/T1568, skip-visibility/T1123 are the inherited ratchet reds). The 3 reds are build-mode-guard.ps1, caller-anchor.ps1 and persistence-flag.ps1; the latter two say 'GHOZTTY_TEST_ALLOW_STALE' which is the STALE-BUILD guard, and that is almost certainly self-inflicted this turn: the zig-out build is from 15:24 and src\apprt\win32\relaunch_guard.zig was rewritten at ~15:47 by the append/revert probe that proved the guard goes DUE, so its mtime is newer than the binary. NEXT TURN: rebuild (zig build -Dapp-runtime=win32 -Doptimize=Debug), then re-run scripts\floor-lane.ps1 -Lane harness and confirm those three go green before reading them as real. Log: C:\\Users\\David\\AppData\\Local\\Temp\\floor-lane-harness-20260919-160345-559.log
- 2026-09-19 16:21: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-09-19 16:56 [session e3f9e0c5]: Resumed after the mid-task reset; kept the uncommitted work per the RESUME protocol. The three harness-floor reds the last turn flagged were NOT fixed by a rebuild: the DUE-probe appended a byte to src\apprt\win32\relaunch_guard.zig and reverted it, so the content is unchanged, zig build correctly has nothing to do, and the file's mtime stays newer than the 15:24 exe. The freshness gate names that case itself - GHOZTTY_TEST_REBUILD_STALE=1, 'the edit was not in this exe's module graph' - and with it build-mode-guard (55), caller-anchor (27) and persistence-flag (29) are all ALL PASS, and the clearance persists for later runs. Full harness floor then PASS: 29 audits, 0 failures, 2 inherited PENDING (T1568/T1123), STAMPED harness-floor (395 files). ipc-p1 (26) / ipc-p2 (20) / ipc-p3 (16) ALL PASS. chooser-sessions-push.ps1 ALL PASS (19) + STAMPED chooser-roster-push (2 files), which proves the new stamp block end to end. guard-due check now reports only the three advisory rows that cannot run on this box.
- 2026-09-19 17:00: status: in-progress -> done (by relaunch-guard row + stamp block landed; all validation green on the box)
