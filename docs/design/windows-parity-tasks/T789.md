---
id: T789
title: "pane-banner.ps1 has two sections both labelled 6g"
order: null
deps: []
status: "done"
commits: []
seat: "win"
priority: "P2"
triage-reason: "two sections share a label, so a red one cannot be named unambiguously"
tags: ["test", "polish"]
---

# T789 - pane-banner.ps1 has two sections both labelled 6g

## In plain terms

Two different blocks of the banner test are both called "section 6g", so a
note that says "the 6g assertions" points at two places. Nothing is broken for
a user; it wastes the time of whoever reads the test next.

## Summary

`test/win32/pane-banner.ps1` labels two separate blocks `6g`:

- line 618 — `6g. T209 / T204: the chevron is an ICON BUTTON, and hot-tracks`
- line 734 — `6g. T123: table columns size to the PANE, not a fixed 360pt cap`

The label is load-bearing prose: `BannerOverlay.T123_NEUTERED`'s doc comment
and the script's own header both cite "section 6g" as the claim list a
negative control adjudicates, and T283 had to disambiguate them by naming the
task instead ("the T123 table block in section 6g"). The comment at line 238
citing "6g's `banner chevron hover=` oracle" means the OTHER one.

## What to do

Renumber one of them and follow the references. `6h` is already T377's, so the
cheap move is to renumber forward from the chevron block (or split as `6g1` /
`6g2` if a full renumber ripples too far into task files and the log). Then
update every citation: `BannerOverlay.zig`'s `T123_NEUTERED` and
`T377_NEUTERED` comments, the script header, the line-238 stderr note, and
`CLAUDE.md`'s "section 6f2"/"section 6j" neighbours if the letters move.

## Validation criteria

- [x] No two sections in `pane-banner.ps1` share a label - the chevron block is
      `6f3` now, and new section 0 SCORES the rule: `PASS section labels are
      unique (27 sections; duplicates: none)`, red over a copy with `6h`
      renamed to `6g`
- [x] Every in-repo citation of the renamed section points at the right block -
      the three cross-references inside the script, `BannerOverlay.zig`'s two
      T571 chevron-fill comments, `hover-capture.ps1` and `lib/HoverCapture.ps1`
      moved to `6f3`; the T123 citations (`BannerOverlay.zig`'s neutered-control
      doc, the `6g2` and `6h` notes) correctly still say `6g`; `docs/claude/`
      cites only `6f2` and `6j`, neither of which moved
- [x] `pane-banner.ps1` still ALL PASS - 149 assertions, guard restamped
      (2026-09-16); floor lanes lib/none/win32/agent all PASS, harness floor
      PASS, `hover-capture.ps1` ALL PASS (26)

## Progress log

- 2026-09-16 09:45: claimed; work starting.
- 2026-09-16 09:48: CHECK FIRST: defect still real - lines 787 and 957 both labelled 6g. Renamed the CHEVRON block to 6f3 (it follows the 6f/6f2 collapse family, and 6g2/T745 belongs with the T123 table block, so renaming the table half would have split a pair). Updated the live citations: pane-banner.ps1's own three cross-references, BannerOverlay.zig's T571 chevron-fill comments, hover-capture.ps1 and lib/HoverCapture.ps1. Closed task files keep their prose - they are a record of what was true then. Added section 0: the script now scores its own labels for uniqueness, verified against a seeded duplicate.
- 2026-09-16 10:12: floor: ALL LANES PASS (lib/none/win32/agent). pane-banner.ps1: ALL PASS, 149 assertions, guard restamped; new section 0 passes first in the real run. Comment-only BannerOverlay + harness edits left 20 guards DUE - running the harness floor, then hover-capture and tab-tooltip.
- 2026-09-16 10:31: hover-capture.ps1 ALL PASS (26), stamped. tab-tooltip.ps1 2F/18P - NOT this change: its A and E-negative arms assume the tab's default caption is short, and cmd.exe captions the tab with the run's own deep %TEMP% scratch path, so the strip elides it and T556's correct two-line tip fires. Product is right, harness premise is incidental; filed T1613. That guard stays DUE, so the commit takes -NoGuardDue naming T1613.
- 2026-09-16 10:32: status: in-progress -> done
