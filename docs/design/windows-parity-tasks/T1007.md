---
id: T1007
title: "Most acceptance harnesses have no staleness guard, so the next one that drifts goes red for months like the menu test did"
order: null
deps: ["T987", "T996"]
status: "skipped(stale: superseded by T1523)"
commits: []
seat: "win"
priority: "P2"
triage-reason: "Measured in T987: 163 of 225 test/win32 harnesses have no guard-due row, so a harness that stops matching the code it covers goes red - or green-but-wrong - with nothing pointing at it."
tags: ["test", "infra"]
---

# T1007 - Most acceptance harnesses have no staleness guard, so the next one that drifts goes red for months like the menu test did

## Summary

T987 measured it: 163 of the 225 scripts in test/win32 have no guard-due coverage row, so nothing ties them to the code they cover and nothing notices when one stops matching the app. menu-bar.ps1 was exactly that shape - a hardcoded model of the app's menus, one assertion red since T871 renamed a row, on every run for months. Not every harness needs a row (the floor lanes and P1-P3 run constantly, and a gate that fires on unrelated edits is noise T783 warns about), so this is a triage pass, not a bulk edit: pick out the harnesses that (a) mirror a table or model that lives in the source, or (b) are the ONLY proof of a behavior and are not in the P1-P3 floor, and give those a row. List the ones deliberately left uncovered with a reason, so the gap is a decision rather than an oversight.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-22 14:53: T1106 is a paid instance of exactly this: chooser-orphan-badge.ps1 - the ONLY harness that asserts the chooser's 'not in any window' mark - had no guard row, sat 4 FAILURE(S) from the 242-script sweep with nothing obliging anyone to re-run it, and the failure was misread as a broken attach for a day. A row was added in f26f33202. The pattern to generalise: a harness that is the sole coverage of its subject is the one that most needs a row, and 'has a stamp' is not the same question as 'has a row'.
- 2026-08-22 15:36: Second paid instance in two days: test\win32\agent-autostart.ps1 had no guard row either, so its red B6 from the T1094 sweep had nothing obliging anyone to re-run it. It is the sole proof that the HKCU Run value is executable the way winlogon executes it - no lane and no other script ever starts the agent from that string. A row (LocalAgent.zig, session_meta.zig, the harness) plus a bottom-of-run stamp landed with T1108. Same tell as T1106: sole coverage of its subject.
- 2026-09-16 06:32: status: todo -> skipped(stale: superseded by T1523) (by enacting the 2026-09-15 triage proposal, unvetoed: the mechanism shipped as guard-due.ps1 and the remaining row backlog is tracked by T1523)
