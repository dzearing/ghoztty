---
id: T1466
title: "A restored pane is still 1.55x slower than a plain one at bulk output, and closing it needs D94"
order: null
deps: ["T1465"]
status: "skipped(D94: the user chose to leave commands on the whole machine, so 1.75 is the permanent bound)"
commits: []
seat: "win"
priority: "P1"
triage-reason: "the last third of T1458's user report; the cause is measured and the remedy is known, but it trades a build's CPU for output speed and D94 is the user's call on that trade"
tags: ["perf", "fix"]
unblock: "D94 is answered. If option 1 (leave commands on the whole machine), close this skipped(D94) and 1.75 is the permanent bound. If option 2, drop the allowAllCores call in pty_host.zig and re-measure."
---

# T1466 - A restored pane is still 1.55x slower than a plain one at bulk output, and closing it needs D94
## Summary

T1465 measured the cause of the agent-held pane's ingest deficit and shipped the
half of the remedy that costs nothing: the pty holder and the conhost it creates
are kept off the CPU's efficiency cores, which took the burst ratio from
1.84-2.02x of a local pane down to 1.51-1.58x. `test\win32\pane-ingest-ab.ps1`'s
`$relayRatioBound` came down 2.50 -> 1.75 to hold that.

The remaining ~0.5x needs the USER's shell subtree on the performance cores too.
Measured as a one-off experiment (the whole holder tree pinned): **1.05x and
1.14x**, i.e. level with conhost. It is not shipped because process affinity is
inherited by everything the pane starts, so a build run in a persisted pane
would be confined to 16 of this box's 32 logical CPUs - a trade the user has to
make, not the loop. That is **D94**.

## In plain terms

A pane that keeps its session alive still takes about half again as long as an
ordinary one to scroll a big burst of text. We know exactly how to close the
rest of it and we know what it would cost: anything you RUN in that pane would
get about half the machine. The question is out with the user.

## Details

- Do NOT re-investigate the relay, the parser, the message count or the ACKs -
  T1463/T1464/T1465 each measured one of those and each came back negative. The
  reader is 96% blocked in `ReadFile` and its peek finds an empty pipe every
  time; the SOURCE is the whole story.
- The lever is `internal_os.power` (`src/os/power.zig`): `preferPerformanceCores`
  pins the calling process (inherited by its children), `allowAllCores` hands one
  child the full mask back with a soft CPU-set preference. Today `pty_host.zig`
  calls the first for itself and the second for the shell. Option B of D94 is
  simply not making that second call.
- If D94 comes back "leave commands on the whole machine", this task closes as
  `skipped(D94: ...)` and `pane-ingest-ab`'s 1.75 bound is the permanent one.
- ~~A third option nobody has costed yet: pin the conhost the ConPTY spawns
  WITHOUT pinning the shell.~~ **This is already what ships** (checked against
  the code, 2026-09-08). `pty_host.zig` calls `preferPerformanceCores` on
  ITSELF, and `src/os/power.zig`'s header says why that reaches conhost: the
  ConPTY's conhost is a child of the holder and affinity is inherited, so it is
  pinned without ever needing its handle. `allowAllCores` then hands the shell
  the full mask back. So there is no untried middle option - the only thing left
  unpinned is the user's shell subtree, and pinning that IS D94's option 2.
  Nothing here can move without the user's answer.

## Goals

- [ ] D94 is resolved and this task does what it says.
- [ ] If pinned: `pane-ingest-ab -ReleaseSandbox` at or under 1.20 and the bound
      moves with it.

## Validation criteria

- [ ] `test\win32\pane-ingest-ab.ps1 -ReleaseSandbox` three runs, numbers in
      this file.
- [ ] `scripts\floor-lane.ps1 -Lane all` green.
- [ ] Whatever D94 picks, the reasoning is written into `src/os/power.zig`'s
      header so the next person does not re-derive it.

## Progress log

- 2026-09-08: filed from T1465's measurements; blocked on D94.
- 2026-09-08 20:22: claimed; work starting.
- 2026-09-08 20:23: status: in-progress -> blocked(D94: the user's call on trading a build's CPU for output speed) (by verified in code that the third option in Details is already shipped; no path forward exists without D94)
- 2026-09-09 07:37: status: blocked(D94: the user's call on trading a build's CPU for output speed) -> skipped(D94: the user chose to leave commands on the whole machine, so 1.75 is the permanent bound) (by D94 resolved 2026-09-09T14:00:06Z to option o1 (Leave commands on the whole machine, the recommended one); the task's own unblock text names closing it skipped(D94) as the action for that answer, and the allowAllCores call in pty_host.zig therefore stays)
