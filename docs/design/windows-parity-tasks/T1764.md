---
id: T1764
title: "closeNeedsConfirm / ProcessTree comments still say cmd\u0027s cursor reads \u0027at the prompt\u0027 through a running command; T1757\u0027s 133;I makes that false for cmd - re-state which shells the process table is still needed for"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P3"
triage-reason: "Comment accuracy only: src/apprt/win32/Surface.zig closeNeedsConfirm and ProcessTree.hasDescendants explain the process-table override with cmd getting 133;B and no 133;C. Since T1757 (6269cb9af) cmd\u0027s prompt ends in 133;I, so its cursor reads output during a run. The override is still right (stock PowerShell with no integration, remote shells, a shell whose PROMPT the user replaced), but the comments should name those cases. No behavior change."
tags: ["docs"]
---

# T1764 - closeNeedsConfirm / ProcessTree comments still say cmd's cursor reads 'at the prompt' through a running command; T1757's 133;I makes that false for cmd - re-state which shells the process table is still needed for

## Summary

TODO: describe the defect or gap, the evidence, the fix, and the validation.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-09-26 10:38: priority: P1 -> P3 (Comment accuracy only: src/apprt/win32/Surface.zig closeNeedsConfirm and ProcessTree.hasDescendants explain the process-table override with cmd getting 133;B and no 133;C. Since T1757 (6269cb9af) cmd's prompt ends in 133;I, so its cursor reads output during a run. The override is still right (stock PowerShell with no integration, remote shells, a shell whose PROMPT the user replaced), but the comments should name those cases. No behavior change.)
