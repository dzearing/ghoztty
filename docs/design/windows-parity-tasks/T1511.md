---
id: T1511
title: "The 79 acceptance scripts that stamp a guard move onto the shared scorer, so an unwound run stops recording the files as proven"
order: null
deps: ["T1510", "T1513", "T1514", "T1515", "T1516", "T1517"]
status: "todo"
commits: []
seat: "win"
priority: "P1"
triage-reason: "the half of T1510's defect that outlives the red line - a stamp written over an unwound run keeps a guard quiet until the covered files change again"
tags: ["test", "infra"]
---

# T1511 - The 79 acceptance scripts that stamp a guard move onto the shared scorer, so an unwound run stops recording the files as proven

## In plain terms

When one of our test scripts finishes cleanly it leaves a note saying "this
feature was proven against the code as it stands now", and that note is what
stops the loop being nagged to run it again. Seventy-nine of those scripts leave
the note even when the run crashed halfway - so a feature nobody actually tested
gets recorded as tested, and stays that way until somebody edits the code again.
After this lands, only a run that reached its own end leaves the note.

## Summary

T1039 gated the stamp on body completion, and it gated it in the right place:
`scripts\guard-due.ps1 update` runs as a CHILD of the harness and refuses to
write while the inherited `GHOZTTY_TEST_BODY` reads `pending`. What publishes
that variable is the dot-source of `test\win32\lib\TestScore.ps1`, so a script
that never dot-sources it publishes nothing and the child stamps
unconditionally.

T1510 measured the set and put it under a ratchet: `unarmed-stamp` in
`lib\AssertedNothingAudit.ps1`, counted by section C5 of
`test\win32\asserted-nothing.ps1`, which stood at **79** when this was filed.
This task burns that number down to zero. It is the higher-value half of the
conversion because the damage outlasts the run - a red line scrolls away, a
stamp keeps a guard quiet until the covered files change again.

## Direction

Per script, the conversion is three lines, and
`test\win32\viewer-feedback-capture.ps1` is the worked example (T1510's commit):

- dot-source `lib\TestScore.ps1` with the other libs at the top - that alone is
  what ARMS the run;
- `Complete-TestBody` as the last statement the run reaches before the stamp,
  and a top-level `try` with no `catch` either ends in it or grows a `catch`
  that SCORES the throw (`lib\BodyCompleteAudit.ps1` decides which, and it is
  already a hard-zero sweep, so a conversion that gets this wrong goes red);
- the final `if ($script:fail -eq 0) { "ALL PASS ..." } else { ... }` becomes
  one `Write-TestVerdict -Pass ... -Fail ... -Label '...'` call.

Lower C5's ceiling in `asserted-nothing.ps1` as each batch lands - never raise
it. When it reaches 0, add `unarmed-stamp` to `Get-AssertedNothingHardKinds` so
a new script cannot reintroduce one.

Batch them: each converted script must be RUN green on the box, and many of
these are GUI acceptance scripts that take minutes. Several batches, several
turns, is the shape.

**As of batch 10 (2026-09-13) the runway on this box is gone.** Six unarmed
stampers remain and not one of them can reach a green stamping run here at
HEAD, so the burn-down is now waiting on other work rather than on run time:

| script | blocked on |
|---|---|
| `website-windows-download` | T1513 - the gh-pages mirror has drifted, F1 red |
| `ghoztty-cleanup` | T1514 - a box-state SKIP means it can never re-stamp here |
| `tab-tooltip` | T1515 - sections A and E red at HEAD |
| `layout-capture-cost` | T1516 - a wall-clock frame budget that fails under load |
| `relay-account` | T1517 - wedges in its own teardown, never prints a verdict |
| `release-artifacts` | wixl, therefore Docker - the user's call, never the loop's |

The first five are `deps:` on this task, so it stays out of `next` until they
close. `release-artifacts` is the one that may never be reachable from this
seat; its guard is already carried as an ADVISORY by `guard-due.ps1`, and its
teeth are fork-ci compiling the package on every push. When the other five
land, convert all six, take C5 to zero, and add `unarmed-stamp` to
`Get-AssertedNothingHardKinds`.

## Validation criteria

- [ ] `test\win32\asserted-nothing.ps1` reports 0 `unarmed-stamp` scripts.
- [ ] `unarmed-stamp` is in `Get-AssertedNothingHardKinds`, and `-TeethCheck`
      still goes red on a synthesized violator.
- [x] `lib\BodyCompleteAudit.ps1`'s sweep is still at zero findings over the
      whole suite - the conversion brings every one of these files into its
      scope for the first time.
- [x] Every converted script was run on the box and ended green, with its own
      guard stamped by that run. (batch 1: 17; batch 2: 8 more; batch 3: 9 more;
      batch 4: 6 more; batch 5: 6 more; batch 6: 6 more; batch 7: 6 more;
      batch 8: 6 more; batch 9: 6 more; batch 10: the last 3 this box can reach)
- [x] The P1-P3 floor and the four zig lanes still pass. (batches 1-10)

## Progress log
- 2026-09-12 19:12: claimed; work starting.
- 2026-09-12 19:40: batch 1: 18 stamping scripts converted onto the shared scorer (docs-routing, merge-terminology, cleanslate-audit, launch-preflight-audit, verdict-exit-audit, isolation-meta, registration-sites, control-char-scan, build-mode-guard, upstream-remote, fork-identity, ship-workflow, ghoztty-cleanup, log-append, feedback-user-report, whea-report, layout-blob-cross-lineage, parity-tasks-seat). Five needed a scoring catch on a top-level try; four had no pass counter and grew one. All 18 run green on the box. C5 ceiling 79 -> 61, C4 198 -> 180. website-windows-download was converted and REVERTED: its F1 is red at HEAD for an unrelated gh-pages mirror drift, filed as T1513.
- 2026-09-12 20:09: correction to the note above: 17 conversions ship, not 18. ghoztty-cleanup was reverted alongside website-windows-download - it runs GREEN but skips H5 on this box ('the ghost entry is not registered'), and a skipped section deliberately does not stamp, so any edit to it leaves its guard permanently due here. Filed as T1514 (same class as the already-fixed T898). Ceilings: C5 79 -> 62, C4 198 -> 181. Floor green: four zig lanes ALL LANES PASS, ipc-p1/p2/p3 ALL PASS, asserted-nothing both modes, body-complete-audit, and the eight sweep audits my edits made due, all green and re-stamped; guard-due check exits 0. Task stays OPEN for batch 2 (the GUI acceptance scripts, and the two blocked ones once T1513/T1514 land).
- 2026-09-12 20:09: status: in-progress -> todo (by batch 1 of the burn-down landed (17 scripts); the remaining 62 are GUI acceptance runs that need their own turns)
- 2026-09-12 20:12: claimed; work starting.
- 2026-09-12 20:21 [session e626388e-36ce-4dbb-b500-06413145e050]: batch 2 picked: test-filter-guard, build-fresh-guard, zig-repro-t476, deliver-windows-build, agent-shell-integration, crash-stacks, crash-databreak, go-loop-resume - the eight whose guards this box can actually re-stamp. All eight converted (dot-source lib\TestScore.ps1, a passing-assertion counter where there was none, Complete-TestBody before the stamp, Write-TestVerdict as the final line); body-complete-audit still zero findings, asserted-nothing ceilings C5 62 -> 54 and C4 181 -> 173. Now running each one green on the box.
- 2026-09-12 20:52 [session e626388e-36ce-4dbb-b500-06413145e050]: batch 2 landed: 8 stamping scripts converted (test-filter-guard, build-fresh-guard, zig-repro-t476, deliver-windows-build, crash-stacks, crash-databreak, agent-shell-integration, go-loop-resume). Seven of the eight had no pass counter and grew one; deliver-windows-build needed the marker twice because its -PureOnly early exit is the whole body of that mode. All eight run green on the box and re-stamped their own guards (21/42/12/75/91/93/27/22 assertions). Ceilings C5 62 -> 54, C4 181 -> 173; body-complete-audit still zero findings, asserted-nothing green in both modes including -TeethCheck. Floor green: ALL LANES PASS, ipc-p1/p2/p3 ALL PASS, the eight sweep audits my edits made due all re-stamped, guard-due check exits 0. 54 unarmed stampers remain, all GUI acceptance runs - task stays OPEN for batch 3.
- 2026-09-12 20:52: status: in-progress -> todo (by batch 2 of the burn-down landed (8 scripts); the remaining 54 are GUI acceptance runs that need their own turns)
- 2026-09-12 20:54 [session e626388e-36ce-4dbb-b500-06413145e050]: batch 2 shipped as 6e455d967.
- 2026-09-12 20:56: claimed; work starting.
- 2026-09-12 21:29 [session a9dafea0-c1b0-487b-87f4-02ae72be4246]: batch 3: the first nine GUI acceptance runs converted onto the shared scorer - session-layout-preserve, sessions-running-cmd, remote-disconnect, ipc-relay, remote-pill, activity-monitor-remote, chooser-restore-all-remote, remote-reconnect-relay, chrome-theme. All nine RUN GREEN on the box and re-stamped their own guards (19/16/49/35/13/96/47/12/136 assertions). Three needed structural work: remote-pill's verdict and stamp moved out of the try body so the body can END in Complete-TestBody; activity-monitor-remote's stamp moved ahead of the verdict for the same reason and now gates on the count rather than on an exit 1 that is no longer above it; chooser-restore-all-remote's hand-rolled $script:bodyComplete flag replaced by the shared marker. ipc-relay grew a pass counter. Ceilings C5 54 -> 45, C4 173 -> 164; body-complete-audit still zero findings; asserted-nothing green in both modes.
- 2026-09-12 21:39: status: in-progress -> todo (by batch 3 of the burn-down landed (9 GUI acceptance scripts); 45 unarmed stampers remain, each needing its own run time)
- 2026-09-12 21:40 [session a9dafea0-c1b0-487b-87f4-02ae72be4246]: batch 3 shipped as b1ffabd90.
- 2026-09-12 21:42: claimed; work starting.
- 2026-09-12 21:48 [session 21289554-35fa-4d5c-bb71-64cc9e8a95d6]: batch 4 picked and converted: palette-jump, tab-tooltip, chooser-session-sort, chooser-resume, chooser-orphan-badge, relay-account, restore-late-agent, layout-capture-cost, harness-process-leak. Four continue past their top-level try so those tries grew a SCORING catch rather than ending in the marker; relay-account grew a pass counter (its Assert only counted failures). body-complete-audit back to zero findings; ceilings C3 36 -> 31, C4 164 -> 155, C5 45 -> 36. Now running each one green on the box.
- 2026-09-12 22:27 [session 21289554-35fa-4d5c-bb71-64cc9e8a95d6]: batch 4 landed: 6 of the 9 shipped - palette-jump (25 assertions), chooser-session-sort (19), chooser-resume (29), chooser-orphan-badge (17), restore-late-agent (19), harness-process-leak (42). All six run GREEN on the box and re-stamped their own guards. Three were converted and REVERTED rather than shipped unproven, because this box cannot reach a green stamping run of them at HEAD: tab-tooltip (T1515 - sections A and E red, the tip now carries an un-abbreviated title line above the cwd), layout-capture-cost (T1516 - a wall-clock frame budget that failed G4+G5 on one run and G3 on the next) and relay-account (T1517 - reached its last assertion then wedged in its own teardown for 25+ minutes at 4.6s CPU and had to be killed). Ceilings C4 164 -> 158, C5 45 -> 39; body-complete-audit still zero findings; asserted-nothing green in both modes.
- 2026-09-12 22:37: status: in-progress -> todo (by batch 4 of the burn-down landed (6 scripts); 39 unarmed stampers remain, three of them now blocked behind T1515/T1516/T1517)
- 2026-09-12 22:39 [session 21289554-35fa-4d5c-bb71-64cc9e8a95d6]: batch 4 shipped as 8298b3fd0.
- 2026-09-12 22:42: claimed; work starting.
- 2026-09-12 22:56 [session ddd1b7c3-9b0e-4435-b10c-9503f1560a22]: batch 5 converted and RUN GREEN on the box: ipc-when-idle (22 assertions), ipc-version (35), url-scheme (39), clipboard-retry (16), gui-launch-command (29), go-loop-guard (395) - each re-stamped its own guard. ipc-version had hand-rolled the completion marker as $script:reachedEnd and moved onto the shared Complete-TestBody, which its own 'ran to the end' assertion now reads via Test-TestBodyComplete. Ceilings C3 31 -> 29, C4 158 -> 152, C5 39 -> 33.
- 2026-09-12 23:20 [session ddd1b7c3-9b0e-4435-b10c-9503f1560a22]: batch 5 landed: 6 stamping harnesses converted (ipc-when-idle, ipc-version, url-scheme, clipboard-retry, gui-launch-command, go-loop-guard). Five grew a pass counter; ipc-version's hand-rolled $script:reachedEnd moved onto the shared marker and its 'ran to the end' assertion now reads Test-TestBodyComplete. All six run GREEN and re-stamped their own guards (22/35/39/16/29/395 assertions). Ceilings C3 31 -> 29, C4 158 -> 152, C5 39 -> 33; body-complete-audit zero findings; asserted-nothing green in both modes. Floor green: ALL LANES PASS, ipc-p1/p2/p3 ALL PASS, the eight sweep audits my edits made due all re-stamped, guard-due check exits 0. 33 unarmed stampers remain - task stays OPEN for batch 6.
- 2026-09-12 23:20: status: in-progress -> todo (by batch 5 of the burn-down landed (6 scripts); 33 unarmed stampers remain, each needing its own run time)
- 2026-09-12 23:23 [session ddd1b7c3-9b0e-4435-b10c-9503f1560a22]: batch 5 shipped as f8233dd7a.
- 2026-09-12 23:26: claimed; work starting.
- 2026-09-12 23:37 [session 28559a23-d32c-4d16-b792-08b874575513]: batch 6 converted and RUN GREEN on the box: viewer-nav-pin (24 assertions), viewer-narrow-pane (63), viewer-image (40), viewer-composer (30), viewer-feedback (96), viewer-panes (195) - each re-stamped its own guard. All six run leak checks AFTER their top-level try, so every one of those tries grew a SCORING catch and the marker sits immediately before the stamp. Ceilings C4 152 -> 146, C5 33 -> 27.
- 2026-09-13 00:00 [session 28559a23-d32c-4d16-b792-08b874575513]: batch 6 landed: 6 stamping viewer harnesses converted (viewer-nav-pin, viewer-narrow-pane, viewer-image, viewer-composer, viewer-feedback, viewer-panes). Every one of them runs its foreground/browser leak checks AFTER the top-level try, so all six tries grew a SCORING catch rather than ending in the marker, and Complete-TestBody sits immediately before the stamp. All six run GREEN on the box and re-stamped their own guards (24/63/40/30/96/195 assertions). Ceilings C4 152 -> 146, C5 33 -> 27; body-complete-audit zero findings; asserted-nothing green. Floor green: ALL LANES PASS, ipc-p1/p2/p3 ALL PASS, the eight sweep audits my edits made due all re-stamped, guard-due reports no non-advisory guard due. 27 unarmed stampers remain - task stays OPEN for batch 7.
- 2026-09-13 00:00: status: in-progress -> todo (by batch 6 of the burn-down landed (6 viewer harnesses); 27 unarmed stampers remain, each needing its own run time)
- 2026-09-13 00:03 [session 28559a23-d32c-4d16-b792-08b874575513]: batch 6 shipped as 3fee4276c. Also cleared a stale .git/index.lock (written 23:29, no git writer alive) that blocked the first commit attempt.
- 2026-09-13 00:05: claimed; work starting.
- 2026-09-13 00:09 [session beb3aed8-e98f-43e1-a9d0-0f56405be6de]: batch 7 converted: readonly-badge, key-state-pill, menu-f10-binding, split-divider, viewer-worktree-port, stderr-launch-capture. Four grew a SCORING catch (their leak checks run after the top-level try); readonly-badge and key-state-pill already scored their throw. Ceilings C4 146 -> 140, C5 27 -> 21; body-complete-audit zero findings. Now running each one green on the box.
- 2026-09-13 00:38 [session beb3aed8-e98f-43e1-a9d0-0f56405be6de]: batch 7 landed: 6 stamping GUI harnesses converted (readonly-badge, key-state-pill, menu-f10-binding, split-divider, viewer-worktree-port, stderr-launch-capture). Four of them run their foreground-leak checks after the top-level try, so those tries grew a SCORING catch; readonly-badge and key-state-pill already scored their throw and only needed the marker. All six run GREEN on the box and re-stamped their own guards (37/76/14/73/23/20 assertions). Ceilings C4 146 -> 140, C5 27 -> 21; body-complete-audit zero findings; asserted-nothing green. Floor green: ALL LANES PASS, ipc-p1/p2/p3 ALL PASS, the seven sweep audits my edits made due all re-stamped, guard-due reports no non-advisory guard due. 21 unarmed stampers remain (five of them blocked behind T1513-T1517, one behind Docker) - task stays OPEN for batch 8.
- 2026-09-13 00:38: status: in-progress -> todo (by batch 7 of the burn-down landed (6 GUI harnesses); 21 unarmed stampers remain, each needing its own run time)
- 2026-09-13 00:40: took over stranded working-tree paths (dirty since before this turn's claim): docs/design/windows-parity-tasks/T1511.md
- 2026-09-13 00:41 [session beb3aed8-e98f-43e1-a9d0-0f56405be6de]: batch 7 shipped as 1930cb8a2.
- 2026-09-13 00:43: claimed; work starting.
- 2026-09-13 00:49 [session ef8145d9-8f5b-47a0-8488-e8cd2cbe95bf]: batch 8 picked and converted: agent-autostart, gui-postmortem, orphan-notify, update-check, claude-integration, menu-bar. claude-integration and menu-bar run their leak checks after the top-level try so both tries grew a SCORING catch; agent-autostart grew a pass counter; gui-postmortem's verdict derives its passing count from asserted-minus-failures. body-complete-audit zero findings; ceilings C3 29 -> 28, C4 140 -> 134, C5 21 -> 15. Now running each one green on the box.
- 2026-09-13 01:21 [session ef8145d9-8f5b-47a0-8488-e8cd2cbe95bf]: batch 8 landed: 6 stamping harnesses converted (agent-autostart, gui-postmortem, orphan-notify, update-check, claude-integration, menu-bar). claude-integration and menu-bar run their foreground-leak checks after the top-level try, so both tries grew a SCORING catch; agent-autostart had no pass counter and grew one; gui-postmortem's verdict derives its passing count from its assertion counter minus its failures. All six run GREEN on the box and re-stamped their own guards (26/33/13/17/87/81 assertions). Ceilings C3 29 -> 28, C4 140 -> 134, C5 21 -> 15; body-complete-audit zero findings; asserted-nothing green in both modes including -TeethCheck. Floor green: ALL LANES PASS, ipc-p1/p2/p3 ALL PASS, the eight sweep audits my edits made due all re-stamped, guard-due check exits 0 with only the three pre-existing advisories. 15 unarmed stampers remain (five blocked behind T1513-T1517, one behind Docker) - task stays OPEN for batch 9.
- 2026-09-13 01:22: status: in-progress -> todo
- 2026-09-13 01:22 [session ef8145d9-8f5b-47a0-8488-e8cd2cbe95bf]: batch 8 shipped as 73643313b.
- 2026-09-13 01:27: claimed; work starting.
- 2026-09-13 01:31 [session c2ad7ac9-ab1f-47ce-aa3f-063d6cac3ee3]: batch 9 picked and converted: update-progress, update-apply, reset-context, close-confirm-idle, pane-banner, activity-monitor. Four of the six run leak checks after the top-level try so those tries grew a SCORING catch; reset-context and close-confirm-idle end their body in the marker. body-complete-audit zero findings; ceilings C4 134 -> 128, C5 15 -> 9. Now running each one green on the box.
- 2026-09-13 02:21 [session c2ad7ac9-ab1f-47ce-aa3f-063d6cac3ee3]: batch 9 landed: 6 stamping harnesses converted (update-progress, update-apply, reset-context, close-confirm-idle, pane-banner, activity-monitor). Four of them run their foreground-leak checks after the top-level try, so those tries grew a SCORING catch; reset-context and close-confirm-idle end their body in the marker directly. All six run GREEN on the box and re-stamped their own guards (42/52/81/58/139/205 assertions). Ceilings C4 134 -> 128, C5 15 -> 9; body-complete-audit zero findings; asserted-nothing green in both modes including -TeethCheck. Floor green: ALL LANES PASS, ipc-p1/p2/p3 ALL PASS, the eight sweep audits my edits made due all re-stamped, guard-due check exits 0 with only the three pre-existing advisories. 9 unarmed stampers remain (five blocked behind T1513-T1517, one behind Docker); the three this box can still reach are agent-upgrade, session-relaunch-notify and update-real-msi - task stays OPEN for batch 10.
- 2026-09-13 02:21: status: in-progress -> todo (by batch 9 of the burn-down landed (6 scripts); 9 unarmed stampers remain, each needing its own run time)
- 2026-09-13 02:22 [session c2ad7ac9-ab1f-47ce-aa3f-063d6cac3ee3]: batch 9 shipped as 72c43724c.
- 2026-09-13 02:25: claimed; work starting.
- 2026-09-13 02:29 [session 2748e8cd-a12d-4ab0-98db-700878543e2f]: batch 10 picked and converted: agent-upgrade, session-relaunch-notify, update-real-msi - the last three stamping harnesses this box can reach. agent-upgrade's top-level try continues into foreground-leak checks so it grew a SCORING catch; session-relaunch-notify already scored its throw and needed only the marker and the shared verdict; update-real-msi ends its body in the marker and passes its SKIP count to Write-TestVerdict. body-complete-audit zero findings; ceilings C4 128 -> 125, C5 9 -> 6. Now running each one green on the box.
- 2026-09-13 03:12 [session 2748e8cd-a12d-4ab0-98db-700878543e2f]: batch 10 landed: the last three reachable stamping harnesses converted (agent-upgrade, session-relaunch-notify, update-real-msi). agent-upgrade's top-level try continues into the foreground-leak checks so it grew a SCORING catch; session-relaunch-notify had already hand-rolled one and needed only the marker and the shared verdict; update-real-msi ends its body in the marker and passes its SKIP count to Write-TestVerdict, so its 'a skipped run must not stamp' rule is unchanged. All three run GREEN on the box and re-stamped their own guards (139/131/30 assertions). agent-upgrade's FIRST run was 1 FAILURE at N3 (Wait-Dialog 60 timed out on a loaded box) and the immediate re-run of the same bytes was ALL PASS - filed as T1518. Ceilings C4 128 -> 125, C5 9 -> 6; body-complete-audit zero findings; asserted-nothing green in both modes including -TeethCheck. Floor green: ALL LANES PASS, ipc-p1/p2/p3 ALL PASS, the ten sweep audits my edits made due all re-stamped, guard-due check exits 0 with only the three pre-existing advisories. 6 unarmed stampers remain and NONE is reachable from this box at HEAD - five behind T1513-T1517 and release-artifacts behind Docker - so T1511's own burn-down has run out of runway here.
- 2026-09-13 03:13: status: in-progress -> todo (by batch 10 landed (the last 3 reachable scripts); the remaining 6 are now deps-blocked behind T1513-T1517 and Docker, not behind run time)
