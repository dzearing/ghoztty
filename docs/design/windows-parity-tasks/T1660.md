---
id: T1660
title: "The task dashboard takes about a minute to answer /api/data, so the board loads slowly and polls can never keep up"
order: null
deps: []
status: "done"
commits: ["be567b929"]
seat: "win"
triage-reason: "P1: the dashboard is how a human watches the loop, and a 58s payload makes it feel broken"
priority: "P1"
tags: ["perf", "infra"]
---

# T1660 - The task dashboard takes about a minute to answer /api/data, so the board loads slowly and polls can never keep up

## In plain terms

Opening the task dashboard leaves you looking at an empty board for close to a
minute before anything appears, and once it is up the numbers are always a
minute behind. The page asks the server for fresh data every five seconds, and
the server needs about sixty seconds to answer, so it is permanently
answering a question that is already stale - and the machine is busy the whole
time.

## Summary

Measured on 2026-09-18 against the real tracker (1600+ task files):
`GET /api/data` returned in **57.9s** (`Invoke-RestMethod ... -TimeoutSec 120`,
server started with no `GHOZTTY_TASK_DIR` override). The page polls it every
5s (`POLL_MS = 5000` in `scripts\task-dashboard.page.html:461`), so requests
overlap continuously.

The suspect is the git history work `buildPayload` does per commit rather than
the task-file read: `statusesFromDir` (`scripts\task-dashboard.js:488`) runs a
`git grep '^status:' <sha> -- docs/design/windows-parity-tasks` for every sha it
needs a bucket snapshot at, and each of those greps now scans 1600+ files.
`git log --name-only` over the same directory (line 935) is the other candidate.
Profile before fixing - the numbers above are the symptom, not the cause.

Shape of the fix to consider: cache the payload (it can only change when the
task dir or HEAD moves - both cheap to stat), and/or replace the per-sha
`git grep` with a single walk that folds statuses forward.

It also cost `test\win32\parity-tasks-seat.ps1` its green run: section O probed
liveness with `/api/data` on a 5s timeout, so a slow payload read as a server
that never came up. T861 changed that probe to `/` (a true liveness oracle) -
which means the harness no longer notices this slowness at all, and this task is
the only thing tracking it.

## Goals

- [x] `/api/data` answers inside the poll interval, with margin
- [x] the payload the page receives is unchanged
- [x] a standing check that goes red if the time grows back

## What it was

Not the history walk the summary suspected - that is cached. Two things, both
paid on EVERY request:

- `lastTouched()` ran one `git log -1 --format=%at -- <file>` per in-progress or
  blocked task. Each costs ~350ms on this box and there are sixteen of them:
  5.6s of the 7s build. The data was already in `fileDates()`'s single
  `git log --name-only` walk (`modifiedAny`), so the sixteen calls are gone and
  the answer is byte-identical (verified task by task against the old payload).
- `commitList()` re-walked the whole tracker history with bodies, twice
  (~850ms), on every poll. It can only change when HEAD moves, so it is
  memoised on HEAD like `fileDates()` already was.

And the reason the endpoint measured 57.9s while one build took 7s: node is
single-threaded and every git call is `execFileSync`, so a build longer than
the 5s poll queues the next poll behind it and the queue only ever grows. So
the payload is now memoised too, keyed on HEAD + the three content directories'
(count, newest mtime) + the served page version + the calendar day - ~60ms to
compute. The fields that are a function of the clock rather than of content
(`generatedAt`, the loop and watchdog state, the live end of the series, each
in-flight task's `staleMs`) are recomputed on every serve, so a cache hit never
shows a frozen "now".

## Validation criteria

- [x] `GET /api/data` against the real tracker answers in well under the 5s poll
      interval, measured the same way (stopwatch around `Invoke-RestMethod`).
      **57.9s -> 0.55-0.73s warm** (server-side: 2.3s cold build, ~100ms on a
      cache hit, ~350ms on a rebuild). Negative control: the same measurement
      against the pre-fix code on this tree read 7.3-7.8s.
- [x] The payload is still correct: the dashboard renders the same counts,
      series and activity feed as before the change. Verified by diffing
      `--once` output before and after: identical except `pageVersion` (this
      edit) and the in-progress task's `statusSince`, which is `now` by design
      for an uncommitted status. `test\win32\dashboard-dom.ps1` ALL PASS (45).
- [x] A regression check exists that would fail if the payload time grows back
      past the poll interval. `test\win32\task-dashboard.ps1` section G:
      G1 budgets half the page's own `POLL_MS` (read out of the served HTML,
      not duplicated) and would have gone red at the pre-fix 7.3s; G2 proves
      the speed is not staleness - a task file written between two polls is in
      the next payload; G3 proves a cache hit still carries a fresh
      `generatedAt`.

## Progress log

- 2026-09-19 07:24: claimed; work starting.
- 2026-09-19 07:30: root cause: per-request git work, not history. 16 in-progress/blocked tasks each cost a 'git log -1 -- <file>' (~350ms) = 5.6s, plus commitList's two full 'git log' walks (~850ms) on every poll. And the endpoint read far worse than one build (57.9s vs ~7s) because node is single-threaded with execFileSync, so 5s polls queued behind a 7s build forever. Fix: lastTouched folded into the existing single 'git log --name-only' walk, commitList memoised on HEAD, and a payload memo keyed on HEAD + task/decision/digest dir fingerprints + page version + calendar day, with the clock-dependent fields refreshed on every serve.
- 2026-09-19 08:06: status: in-progress -> done (by cli: David, from bash, in ghoztty) [commit be567b929]
