---
id: T952
title: "crash tooling identifies a lane by embedded test names; a build-time lane stamp would make it exact"
order: null
deps: ["T855"]
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
triage-reason: "P2: the T855 check is sound and covered by a harness, so this is exactness rather than a live falsification risk"
tags: ["infra", "test"]
---

# T952 - crash tooling identifies a lane by embedded test names; a build-time lane stamp would make it exact

## Summary

T855 made lane resolution real by reading the zig test names embedded in each candidate binary, which is measured and covered. It is still a SIGNATURE TABLE (scripts/lib/CrashCatch.ps1, LANE_MARKERS): renaming or deleting one of the named tests makes every candidate fail to verify, and the crash tooling then refuses to resolve any binary for that lane. crash-stacks.ps1 section 7 now FAILS rather than skips when that happens, so the drift is caught by a harness rather than in the field - but the exact fix is for the LANE ITSELF to say what it built: floor-lane (or build.zig) writing a small marker beside the binary, or into a manifest, naming lane + optimize mode + filter, with resolution preferring that and falling back to the markers.

## In plain terms

The crash-diagnosis tools work out which of two identically-named test programs
they are looking at by reading a list of test names out of the file. It works,
and a test guards it -- but the list is maintained by hand, so renaming one of
those tests would make the tools stop recognising anything. The build already
knows exactly what it produced; having it leave a note beside the program would
turn a good inference into a fact.

## Goals

- [x] The build writes a lane stamp beside each lane's test binary.
- [x] The crash tooling prefers the stamp and falls back to test names.
- [x] A stamp that disagrees with its binary stops resolution loudly.
- [x] Harness coverage green on real stamped lanes.

## Validation criteria

- [x] The lane's own build leaves a marker naming lane + optimize mode + any
      test filter beside (or alongside) the binary it produced. (`<exe>.lane`
      from src/build/LaneStamp.zig; read back after floor-lane -Lane all for
      none/win32/agent, size+mtime_ns equal to the exe; format pinned by two
      build-helpers unit tests)
- [x] Resolution prefers that marker and falls back to the embedded test names,
      so an older build in the cache still resolves. (crash-stacks 7c2 (a)/(e):
      a renamed-test fixture resolves by stamp; unstamped fixture by markers)
- [x] A marker that contradicts the binary is a loud failure, not a preference.
      (crash-stacks 7c2 (d): size, mtime, malformed, and none-stamp-on-win32
      all CONTRADICTION; resolution refuses rather than using an older build)
- [x] Acceptance coverage in `test\win32\crash-stacks.ps1` alongside the T855
      sections. (section 7c2, ALL PASS 110; real lanes resolve by stamp with
      no marker drift)

## Progress log

- 2026-09-24 00:32: claimed; work starting.
- 2026-09-24 00:37: re-verified: still true (LANE_MARKERS is the only lane evidence). Design: src/build/LaneStamp.zig custom step writes <exe>.lane (lane/exe/optimize/filters/size/mtime_ns) between link and run for ghostty-test (lane=app_runtime) and ghoztty-agent-test (lane=agent); CrashCatch.ps1 Read-LaneStamp + Get-BinaryLaneVerdict prefer it, markers cross-check (Foreign presence or size/mtime/exe mismatch = CONTRADICTION, resolution stops), marker drift reported not fatal; crash-stacks.ps1 section 7c2. Filtered build verified stamp size+mtime_ns match the real exe exactly.
