---
id: T1030
title: "The cappedLuminance hue sweep only ever tests red - its 0..360 loop feeds a 0..1 hue"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "A test that only ever exercises one input: the hue sweep feeds 0..360 into a 0..1 parameter, so the contrast floor T325 landed is proven for red alone"
tags: ["test"]
---

# T1030 - The cappedLuminance hue sweep only ever tests red - its 0..360 loop feeds a 0..1 hue

## Summary

test-coverage hole found while fixing T325: the sweep looks like 72 hues and is one

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## In plain terms

A test that is supposed to check the close button's red hover — and every
other saturated color the chrome can produce — against a brightness ceiling
actually only ever checks one color: red. It looks like it sweeps the whole
rainbow. Nothing is broken for a user today; the point is that the safety net
covering "white must stay legible on a colored fill" is a twelfth the size it
appears to be, so a future change could break green or blue and no test would
notice.

## Where

`src/apprt/win32/color_math.zig`, the `cappedLuminance: the ceiling holds on
the 8-bit color it returns` test. It loops `h` from 0 to 360 in steps of 5 and
passes it to `hsbToRgb`, whose hue parameter is a **0..1 turn** (`hp = h * 6`),
matching `rgbToHsb`'s `h /= 6.0`. Every multiple of 5 makes `hp` an even
multiple of 30, so `x` collapses to 0 and the `else` branch returns pure red at
the loop's brightness/saturation. `h = 0` returns red too.

Found 2026-08-20 while writing T325's own sweep, which was copied from this one
and had the same bug before it was caught.

## Goals

- [ ] The sweep iterates hue as a 0..1 turn, so it really is a hue circle.
- [ ] It still passes (if it does not, that is a real `cappedLuminance` defect
      and gets its own task).
- [ ] A grep for other `hsbToRgb` callers passing degrees comes back clean —
      `color_math.zig`'s `pale_red` (`.h = 5`) is the one other suspect.

## Validation criteria

- [ ] `zig test src\apprt\win32\color_math.zig` green.
- [ ] The `none` and `win32` lanes green via `scripts\floor-lane.ps1`.
- [ ] Asserted in the test itself (or by a printed probe recorded here) that
      the sweep now produces more than one distinct hue.

## Progress log

- 2026-09-09 06:21 [session 328a946b-27c3-4646-a45d-2061c8883c9d]: T676 sweep 2026-09-09: STILL REAL, re-verified at HEAD. color_math.zig:912 is still 'while (h < 360) : (h += 5)' feeding hsbToRgb, whose hue is a 0..1 turn - so the sweep is still red-only. New finding for the same card: the pale_red probe below it (:935) also passes degrees (.h = 5), and the hue check at :938 compares rgbToHsb(dark_red).h - a 0..1 turn - against 5, so 'abs(delta) < 15' passes vacuously for every possible result. Both are the same unit bug and should be fixed together.
