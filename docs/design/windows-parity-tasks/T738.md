---
id: T738
title: "connection.zig \u0027inbound DATA routing\u0027 flakes on a 100k-spin timeout"
order: null
deps: []
status: "done"
commits: []
seat: "win"
priority: "P2"
tags: ["test"]
triage-reason: "load-flake class with T498 and T508; a 100k-spin timeout is a box-load assumption, not a bound"
---

# T738 - connection.zig 'inbound DATA routing' flakes on a 100k-spin timeout

## In plain terms

One test in the standing test suite fails at random. It is not finding a bug —
it gives up waiting after a fixed number of tries, and on a busy machine that
number runs out before the answer arrives. A test that cries wolf is worse than
no test: the next person to see it red spends their turn hunting a defect that
is not there.

## Summary

Seen on 2026-08-11 during T249's floor run:

```
error: 'remote.connection.test.inbound DATA routing: bytes land in the right
channel; unknown is dropped' failed: [tripwire] (warn): untripped point=read
src/remote/connection.zig:3772:34: in drainChannel
```

Line 3772 is `if (spins > 100_000) return error.Timeout;` in `drainChannel`,
which busy-spins on `ch.pop()` with a `std.Thread.yield()` between tries while a
reader thread fills the ring. **A spin COUNT is not a timeout** — it measures
how many times this thread got scheduled, not how long it waited, so what it
actually bounds varies with core count, box load and the scheduler's mood. On a
loaded box the reader thread can lose 100 000 yields' worth of races without
anything being wrong.

Re-running `scripts\floor-lane.ps1 -Lane win32` on the identical tree and
build passed (339 s, ALL LANES PASS), which is what makes this a flake rather
than a regression: same inputs, different verdict.

The fix is to bound the wait by the CLOCK — `std.time.Instant` / `Timer`, a
generous wall-clock budget (a second or two is enormous for a loopback ring) —
and, while in there, to sweep for the same shape elsewhere: any `spins >`,
`attempts >` or `for (0..N)` guarding a cross-thread wait is the same bug
waiting for a busy afternoon.

## Where

- `src/remote/connection.zig` — `drainChannel` (the spin guard) and the
  `inbound DATA routing` test that calls it.
- Sweep `src/remote/` and `src/apprt/win32/` for sibling spin-count waits.

## Validation criteria

- [x] `drainChannel` bounds its wait on wall-clock time, not on a spin count,
      and says so in its error. Verified by reading the code: T472 (f2bb58d8a)
      replaced the 100k-spin guard with `TestDeadline`, a `std.time.Timer`
      budget, and its own unit test proves 200 000 yields cannot spend a live
      budget.
- [x] The sweep is done and every cross-thread wait found is either converted
      or explicitly justified in a comment. Swept `src/remote/` and
      `src/apprt/win32/` for a wait whose bound is an iteration count: three
      were left, all now on the wall clock - `pipe_stream.zig`'s accept thread,
      `BannerOverlay.zig`'s two collapse-animation guards, and
      `pty_host_smoke.zig`'s holder dial retry. Everything else already bounds
      on `Instant`/`Timer`/`milliTimestamp` or on a stall timer that resets on
      progress.
- [x] `scripts\floor-lane.ps1 -Lane win32 -Repeat 3` PASS: win32#1/2/3 all
      PASS on this tree (226s, 226s, 240s), with `-Lane all` green first
      (lib/none/win32/agent) and P1-P3 ALL PASS.

## Progress log

- 2026-08-11 06:18: recurred 2026-08-11 during the T666 floor run: 'remote.connection.test.inbound DATA routing: bytes land in the right channel; unknown is dropped' failed with [tripwire] (warn): untripped point=read on the win32 lane, while the none and agent lanes passed. Same timeout shape this task describes. The box was under no concurrent load at the time - the lane ran alone - so the 100k-spin bound is thin even on an idle box.
- 2026-09-14 21:25: claimed; work starting.
- 2026-09-14 21:40: CHECK FIRST outcome: the connection.zig half was already fixed by T472 (f2bb58d8a) - drainChannel is on TestDeadline/wall clock. What is left is criterion 2, the sweep: converted the three remaining count-bounded cross-thread waits to wall-clock bounds (pipe_stream.zig accept loop, BannerOverlay's two collapse-animation guards, pty_host_smoke's holder dial retry). Floor lanes running.
- 2026-09-14 22:21: status: in-progress -> done
