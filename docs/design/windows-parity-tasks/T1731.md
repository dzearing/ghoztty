---
id: T1731
title: "Walk the first published release that carries the DLL Repair/Cancel action (T1730) with install-walkthrough, no -Msi"
order: null
deps: ["T1730"]
status: "todo"
commits: []
seat: "win"
priority: "P1"
triage-reason: "P1: the last proof that the user's reported Cancel error (T1291/T1730) is gone in the bytes they actually install; depends on a publish, so it cannot run until one exists."
milestone: "M1"
tags: ["test", "fix"]
---

# T1731 - Walk the first published release that carries the DLL Repair/Cancel action (T1730) with install-walkthrough, no -Msi

## Summary

T1730 validated the DLL custom action against the published 1.36.36 package rewritten to the new authoring; only a real wixl build proves the Binary row and type-1 action wixl emits. Once daily-publish ships a win-v release built after T1730, run test\win32\install-walkthrough.ps1 with no -Msi and confirm D3/D3b/D5 ALL PASS.

## Validation criteria

- [ ] `gh release list --repo dzearing/ghoztty` shows a `win-v*` release whose
      commit is at or after T1730's.
- [ ] `powershell -NoProfile -File test\win32\install-walkthrough.ps1` (no
      `-Msi`) walks that release: ALL PASS, including D3 (Cancel = 1602) and D3b
      (no error 1722 in the log).
- [ ] If it goes red on D3/D3b, the MSI's CustomAction / Binary rows are read
      back and the defect is filed against build-msi.sh.
