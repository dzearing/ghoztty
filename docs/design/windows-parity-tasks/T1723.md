---
id: T1723
title: "The dashboard\u0027s All-tasks table says \u0027# is what the loop works down\u0027 but sorts by order alone, not by next\u0027s priority/milestone/order/id"
order: null
deps: ["T1722"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "P2: the dashboard\u0027s queue view disagrees with the real queue; misleading to a watching human, but next itself is right."
tags: ["fix"]
---

# T1723 - The dashboard's All-tasks table says '# is what the loop works down' but sorts by order alone, not by next's priority/milestone/order/id

## Summary

task-dashboard.page.html sorts the queue view by order: then id and ignores priority and milestone, so its head is not what parity-tasks.ps1 next hands out (which ranks priority, then milestone since T1722, then order, then id). Make the default 'order' sort reproduce Sort-TaskQueue, or relabel it, and cover it where the dashboard's other sorts are tested.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
