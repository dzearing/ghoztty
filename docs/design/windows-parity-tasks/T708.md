---
id: T708
title: "toggle_tab_overview does nothing on win32 while hero mode is exactly the overview it should show"
order: null
deps: ["T682"]
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
tags: ["feature"]
triage-reason: "a bound action that does nothing on win32; hero mode already is the overview it should show"
---

# T708 - toggle_tab_overview does nothing on win32 while hero mode is exactly the overview it should show

## Summary

App.zig's .toggle_tab_overview arm returns true and does nothing (details.md records the same on Mac; only the GTK apprt has an overview). T682 made the chord reachable from a focused viewer pane and confirmed the app CLAIMS it - which is now a dead key: a user who binds toggle_tab_overview gets silence from every pane. Meanwhile win32 already ships hero mode, a full-window carousel of every pane with its own thumbnail per tile - which is what a tab/pane overview IS. Wanted: decide whether toggle_tab_overview should drive toggleHeroMode() on win32 (making the bound action mean something, with the palette/menu entry to match), or be documented as deliberately unsupported and dropped from the palette rather than acknowledged. Either answer is better than returning true. Note the seat question: if it maps to hero mode here, Mac should map it to whatever its equivalent is, or the divergence is the defect.

## In plain terms

Ghoztty lets you put any of its actions on a keyboard shortcut. One of them,
"toggle tab overview", did nothing at all: press it and the window sat there,
because nothing on Windows (or on the Mac) had ever been wired to it. The
overview it was asking for already exists here under another name - the
full-window view that shrinks every pane into a strip of live thumbnails with
the selected one blown up beside it. Now that shortcut opens exactly that, from
any pane, on both platforms.

## Goals

- [x] a keyboard shortcut bound to `toggle_tab_overview` opens the pane
      overview instead of doing nothing
- [x] pressing it again closes it - it is a toggle
- [x] it answers from a focused viewer pane too, not only from a terminal
- [x] the same action means the same thing on macOS (platform symmetry)
- [x] no duplicate palette/menu entry: "Toggle Hero Mode" already is that entry

## Validation criteria

- [x] `test\win32\tab-overview-action.ps1` ALL PASS (18 assertions) - the
      chord is a CUSTOM keybind (`--keybind=ctrl+shift+f8=toggle_tab_overview`,
      the action ships with no default), and the oracle is the hero geometry
      hero-mode.ps1 uses: one visible leaf at the hero rect with every hidden
      leaf sized to it.
- [x] claim C drives the chord at the viewer's Chromium input child with
      `toggle_hero_mode` as its positive control, so a dead subject chord
      cannot be mistaken for dead injection.
- [x] `-NegativeControl` (claim A inverted to "the chord does nothing", the
      pre-fix behavior) fails exactly that one assertion and nothing else.
- [x] floor lanes green: `scripts\floor-lane.ps1 -Lane all` (lib, none, win32,
      agent) and P1-P3.

## Progress log

- 2026-09-13 04:52: claimed; work starting.
- 2026-09-13 04:56 [session d6fee52e]: decision: map toggle_tab_overview to hero mode rather than drop it - hero mode IS the pane overview (carousel of every pane, selected one blown up). Landed on BOTH seats: win32 App.zig arm + Window.performViewerBindingAction (viewer path goes straight to toggleHeroMode, window-scoped), and macOS Ghostty.App.swift (out of the known-but-unimplemented fallthrough group, into toggleHeroMode - safe because that helper reads only the target, never the void union payload). No palette/menu change: 'Toggle Hero Mode' already IS that entry, a second one would be a duplicate under a second name.
- 2026-09-13 05:26 [session d6fee52e]: validated: new test\win32\tab-overview-action.ps1 ALL PASS (18 assertions; claims A/B terminal toggle, C viewer path with toggle_hero_mode as positive control), -NegativeControl fails exactly the inverted claim A. floor-lane.ps1 -Lane all = ALL LANES PASS (lib/none/win32/agent); ipc-p1 25, p2 20, p3 16 all pass. First draft of claim C was red because it posted the chord at the GhozttyViewer host instead of the Chromium input child - the control caught it. Filed T1522 (seat mac) for validating the Swift half, which this box cannot compile.
