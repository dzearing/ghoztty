---
id: T1567
title: "Skipped sections went unreported again: 13 acceptance scripts violate the skip-visibility rule"
order: null
deps: []
status: "skipped(duplicate of T1123)"
commits: []
seat: "win"
priority: "P2"
tags: ["test", "infra"]
---

# T1567 - Skipped sections went unreported again: 13 acceptance scripts violate the skip-visibility rule

## Summary

skip-visibility.ps1 is red today: its pending exception list names 2 scripts and 15 now violate, so a skipped section can hide behind an ALL PASS line in 13 scripts nobody has converted. Convert each (count the skip site, name the count on the last line), shrink the pending list to empty, then add a guard-due row for skip-visibility so the audit is change-gated and cannot drift again.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-09-14 10:30: status: todo -> skipped(duplicate of T1123) (by SIMILAR block named T1123, which is the same red B1 assertion; evidence folded into T1123 instead)
