---
id: T734
title: "Restore split-divider\u0027s cross-pane stale-line scan once a composited capture exists"
order: null
deps: ["T275", "T778"]
status: "done"
commits: []
seat: "win"
priority: "P2"
tags: ["test"]
triage-reason: "parked behind a composited capture path; restore the scan once T275-class capture exists"
---

# T734 - Restore split-divider's cross-pane stale-line scan once a composited capture exists

## Summary

T228 retired the wide cross-pane scan and measured why it cannot come back on a background desktop: a PrintWindow capture of the parent keeps every intermediate divider line a drag ever painted (13 runs against a HEALTHY build), because the parent never erases and only repaints the band region, and the GL child never overpaints the parent's backing store in that render. Scoring those pixels needs a composite the parent is not part of. T214 considered route 0 (an app-side GL readback over IPC) and deliberately did not build it, filing it as T275 - so this is T275's consumer, not T214's. When T275 lands a capture that reflects real composition, restore the wide scan from git history (test/win32/split-divider.ps1 before T218 batch 3) and re-measure the 13-run number, which should drop to 1. Until then the expressible half is already asserted (T228): the band is solid at three points along its length, and the panes tile the split across the other axis so the gap is the only parent-visible strip.

## Validation criteria

- [x] The wide scan is back: a full scanline ACROSS both panes counts the
      divider color once, in both axes, after three drags and after three small
      window resizes.
      *How: `Measure-CrossPaneRuns` in `test\win32\split-divider.ps1`, scored on
      the composited capture T778 built. `runs 1` in all four places;
      `split-divider.ps1` ALL PASS, 79 assertions, 2026-09-14.*
- [x] The 13-run number T228 measured is re-measured and drops to 1.
      *How: the same scanline, same run: 12 runs un-composited (T228's number,
      against today's healthy build), 1 composited. Both are printed in the
      assertion label so the difference stays visible.*
- [x] The restored scan can say "more than one" — a counter stuck at 1 cannot
      pass it.
      *How: the un-composited count is asserted `> 1` as a positive control
      beside each drag-case assertion.*

## Progress log

- 2026-09-14 18:08: claimed; work starting.
- 2026-09-14 18:09: CHECK FIRST said so: T275 is done but does NOT unblock this. T275's capture-pane reads back ONE pane's own renderer target, so it can say nothing about the strip of PARENT between two panes - which is exactly what the retired cross-pane scan scored. split-divider.ps1's Measure-GapSolid header names the real blocker: T778, a COMPOSITED window capture. Added T778 to deps and returning this to todo; taking T778 instead.
- 2026-09-14 18:09: status: in-progress -> todo (by dep repair: real blocker is T778 (composited capture), not T275)
- 2026-09-14 19:02: status: todo -> done (by its deliverable landed with T778: the cross-pane scan is restored in split-divider.ps1)
