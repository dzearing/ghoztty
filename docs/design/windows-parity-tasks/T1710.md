---
id: T1710
title: "Re-point or retire viewer-feedback-utf16.ps1 (RichEdit UTF-16 caret arithmetic) now the composer is a web page"
order: 0.025
deps: ["T1703"]
status: "done"
commits: ["9b72ba2ef"]
seat: "win"
priority: "P2"
triage-reason: "split out of T1703 (P2, out of M1): harness re-pointing that unblocks deleting the composer fallback (T1704); no user-visible change"
tags: ["test"]
---

# T1710 - Re-point or retire viewer-feedback-utf16.ps1 (RichEdit UTF-16 caret arithmetic) now the composer is a web page

## Summary

viewer-feedback-utf16.ps1 is ABOUT the RichEdit's UTF-16 caret arithmetic (EM_GETSEL, surrogate pairs beside chips). The web surface reports its caret in UTF-16 code units through the page's snapshot (viewer_feedback_page.zig ImageSpan), so either re-point the arms at the page (type astral text, paste, Backspace, read the document and the host's buffer back) or retire the script and name the in-process test that covers the same arithmetic. Split out of T1703, which landed the pattern on viewer-feedback-alpha.ps1: arm ONE DevTools port per script with Enable-WebViewCdp before the first launch, open the composer, Find-CdpComposer (pass -Exclude for pages an earlier app used), then type with Send-CdpComposerText / Send-CdpKey, paste pictures with Send-CdpComposerImage, and read back with Get-/Wait-CdpComposerText (test\win32\lib\WebViewCdp.ps1). Chords NATIVE owns (Ctrl+Enter, Esc) still go through the band's window messages. Assert Wait-ComposerSurface 'web' instead of pinning 'richedit'. When done the script no longer calls Set-ComposerSurface 'richedit' or reads a RichEdit50W, which is what T1704 needs before it deletes the fallback.

## In plain terms

The test that checks the feedback box handles accented letters and emoji
correctly (pictures landing in the right spot, the cursor ending up in the
right place) was still testing an old, hidden version of the box. It now tests
the real one people type into. Nothing changes for users; it was the last test
standing in the way of deleting the old hidden box.

## Goals

- [x] The accented-text/emoji test drives the feedback box users get
- [x] It can still catch the bug it was written for (proved by breaking it on purpose)

## Validation criteria

- [x] The script no longer asks for the RichEdit fallback and asserts the composer opened on the web surface - `Set-ComposerSurface 'richedit'` gone, `Wait-ComposerSurface 'web'` asserted; no `RichEdit50W` lookup remains in any `test\win32\*.ps1` (grep)
- [x] Every editing arm drives the web page (or is retired with its replacement coverage named) - A types over CDP and reads the document and page caret back; B/C park the caret with the new `Set-CdpComposerCaret` and paste through the page's listener, so the host's UTF-16 -> byte -> UTF-16 round trip decides where the chip and caret land; D Backspaces in the page against the seeded chip span; E sends with the Ctrl+Enter chord message and reads report.json. Teeth: `-BreakUtf16` (GHOZTTY_TEST_BREAK_UTF16=1) reds B, C, D and E (5 failures), exactly the four offset arms docs/claude/testing.md names
- [x] The script runs ALL PASS on the box - `test\win32\viewer-feedback-utf16.ps1` ALL PASS (32), first run; `viewer-composer.ps1` ALL PASS (41) over the WebViewCdp.ps1 change

## Progress log

- 2026-09-23 20:22: claimed; work starting.
- 2026-09-23 20:27: re-pointed onto the web composer: new Get-/Set-CdpComposerCaret in WebViewCdp.ps1; ALL PASS (32); -BreakUtf16 teeth run reds B/C/D/E; viewer-composer ALL PASS (41). No harness pins richedit any more, so T1704 is unblocked once this closes.
- 2026-09-23 21:00: status: in-progress -> done (by cli: David, from powershell, in ghoztty)
