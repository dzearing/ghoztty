---
id: T1720
title: "Mac: build and verify +new-window --name names the first pane (T968 Swift half)"
order: null
deps: []
status: "todo"
commits: []
seat: "mac"
priority: "P2"
triage-reason: "Parity half of a fixed CLI defect; the Swift edit is unverified until the Mac seat builds it."
tags: ["test"]
---

# T1720 - Mac: build and verify +new-window --name names the first pane (T968 Swift half)

## Summary

T968 changed macos/Sources/Features/IPC/IPCServer.swift handleNewWindow so --name without --split registers the window's first pane (async focusedSurface hop), sets GHOZTTY_PANE_NAME to it, and focuses an existing pane of that name instead of opening a window. Written on the Windows seat and never compiled: build it, then check +new-window --name=x followed by +read --name=x and +send-keys --target=x, and a second +new-window --name=x focusing rather than creating.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
