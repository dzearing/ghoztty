---
id: T706
title: "Screenshot capture: the keyboard caret starts on the POINTER\u0027s monitor, not the one the user is reporting on"
order: null
deps: ["T671"]
status: "done"
commits: []
seat: "win"
priority: "P2"
triage-reason: "Small, and only bites a multi-monitor keyboard user - but it makes the keyboard path T671 just shipped start in the wrong place for exactly the person it was built for."
tags: ["polish"]
---

# T706 - Screenshot capture: the keyboard caret starts on the POINTER's monitor, not the one the user is reporting on

## In plain terms

Start a screenshot from the keyboard on a two-monitor desk and the crosshair
can appear on the other screen — wherever the mouse pointer happens to be
parked. Someone working from the keyboard never moves that pointer, so it says
nothing about where they are looking. The crosshair should start on the screen
holding the window they are writing the report about.

## Summary

Noticed while building T671. `RegionSelector.homeMonitor` derives the "home"
monitor from `GetCursorPos`, and both the hint card and (since T671) the
keyboard caret's starting point are placed on it. That is right for a mouse
capture — the pointer IS the user's attention — and wrong for a keyboard one.

## Details

- The composer's owner window is already passed to `begin` as `owner`. The
  monitor containing THAT window is the better answer whenever the gesture
  did not start with the pointer, and is never worse: the pane the report is
  about is on it by definition.
- Options worth weighing: always use the owner's monitor (simplest, and the
  hint card would then also follow the pane rather than the pointer), or keep
  the pointer's monitor for the card and use the owner's only for the caret.
- Nothing here changes the capture itself, which photographs the whole virtual
  screen either way.

## In plain terms addendum

The hint card moves with the caret, for the same reason: both now sit on the
screen holding the pane the report is about, so the whole gesture starts in one
place instead of two.

## Goals

- [x] the home monitor is the owner window's, with the pointer as the fallback
- [x] the hint card follows the same choice, so card and caret start together
- [x] the rule is pure and unit-tested, including the fallbacks
- [x] an on-box run proves it across two real monitors

## Validation criteria

- [x] Unit test (none lane): the home-monitor choice is a pure function of the
      owner rect / pointer point and the monitor list.
      `region_select.zig` gained `homeMonitor` plus three tests — owner beats a
      pointer parked elsewhere, the answer is rebased onto the snapshot for
      negative origins, and an anchor that lands in a gap (or a zero-area owner
      rect, or no monitors at all) still names something real.
      `zig test src/apprt/win32/region_select.zig`: 28/28.
- [x] On-box acceptance: with the pointer parked on a monitor other than the
      one holding the viewer window, the announced caret position at
      `capture=begin` falls inside the viewer window's monitor.
      `test\win32\viewer-feedback-capture.ps1` section J, ALL PASS (92). The
      pointer cannot move on a background desktop, so the arm proves it by
      MOVING THE WINDOW: parked on the left monitor the caret is announced at
      1920,1080 (centre of 0,0 3840x2160); parked on the right it is 5760,1080
      (centre of 3840,0 3840x2160). `source=owner` on both.

## Progress log

- 2026-09-12 17:49: claimed; work starting.
- 2026-09-12 17:53: Owner window now decides the home monitor (region_select.homeMonitor: owner rect -> pointer -> whole virtual screen, monitors enumerated with EnumDisplayMonitors). Hint card follows the pane too - one coherent place, and the pane being reported on is on that screen by definition. 3 new none-lane tests; win32 debug build green.
- 2026-09-12 18:30: status: in-progress -> done
