---
id: T1659
title: "The ViewerPane host-floor unit test fails inside a harness-floor sweep and passes alone, so test-reach-audit reads as red"
order: null
deps: []
status: "skipped(duplicate -\u003e T1645)"
commits: []
seat: "win"
priority: "P2"
tags: ["test", "infra"]
---

# T1659 - The ViewerPane host-floor unit test fails inside a harness-floor sweep and passes alone, so test-reach-audit reads as red

## Summary

test-reach-audit.ps1 section B1 builds and runs the whole win32 zig test lane. In the 2026-09-18 harness-floor sweep that lane came back 5867/5940 passed, 1 failed: 'apprt.win32.ViewerPane.test.host floor: a real controller on a real window, on this box' failed with '[tripwire] (warn): untripped point=read'. suite-run's confirm-pass re-ran test-reach-audit alone and it was ALL PASS, which is the shape go.md calls a harness defect rather than a broken feature. The cost is real: harness-floor then scores RED and does not stamp its guard row, so scripts\parity-tasks.ps1 validate refuses the commit over a lane that is actually green - and the only remedy today is a 24-minute re-sweep and hoping. Find out what the sweep does to that test (a real WebView2 controller on a real window, after twenty-odd audits have run) and either make the test robust to it or give it the isolation it needs.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-09-18 20:08: status: todo -> skipped(duplicate -> T1645) (by filed before reading the SIMILAR block's pointer; T1645 already tracks this flake and T1652 was closed against it for the same reason)
