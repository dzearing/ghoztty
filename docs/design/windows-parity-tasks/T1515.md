---
id: T1515
title: "The tab tooltip puts an un-abbreviated title line above the cwd, so hovering a tab shows the same path twice"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
tags: ["fix", "polish"]
---

# T1515 - The tab tooltip puts an un-abbreviated title line above the cwd, so hovering a tab shows the same path twice

## Summary

test\win32\tab-tooltip.ps1 sections A and E are RED at HEAD, in a run made for T1511 batch 4. The tip text the debug oracle reports is two lines - the tab TITLE, un-abbreviated, then the ~-abbreviated cwd - where A expects the home prefix to read as ~ on the first line and E expects a title that FITS to keep the tip cwd-only. Either the title-elision threshold moved or the title line stopped being conditional; the harness has been green since 2026-08-21 (56e310ef0). T1511's conversion of this script was written and REVERTED rather than shipped unproven, because a red run cannot re-stamp the guard.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
