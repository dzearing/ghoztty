---
id: T856
title: "New acceptance scripts can land with audit-violating shapes because the harness audit sweeps run only when somebody remembers them"
order: null
deps: []
status: "done"
commits: ["bdf71d1bb"]
seat: "win"
priority: "P2"
triage-reason: "test-infra hygiene; real but not user-facing - the audits still exist and any full audit run catches drift"
tags: ["test", "infra"]
---

# T856 - New acceptance scripts can land with audit-violating shapes because the harness audit sweeps run only when somebody remembers them

## Summary

The asserted-nothing ceiling (39) was found exceeded at 41: cli-unknown-flag.ps1 (T489) and window-name-env.ps1 (T492) both landed with uncounted final verdicts, each in a turn that ran its own harness green but never ran test\win32\asserted-nothing.ps1 - the T505 turn caught it only because it happened to run all five audit sweeps. The audits (asserted-nothing, skip-visibility, verdict-exit, exitcode, foreground) are AST/text analyzers that finish in seconds, but nothing at any boundary is obliged to run them: they are not in the P1-P3 floor, and guard-due rows map harness->covered-files, which cannot express 'any NEW script in test\win32'. Options to weigh: a guard-due row whose Covers is test\win32\*.ps1 for a combined fast audit runner; or parity-tasks.ps1 validate running the analyzers directly (they are cheap); or a claim-time report like guard-due check. Whatever the mechanism, the property to hold is: a new or edited acceptance script that violates a sweep fails step 6 validate in the same turn, not N turns later when the ceiling assertion trips for an innocent.

## Validation criteria

- [x] A new or edited script under `test\win32\` makes an audit sweep DUE in the
      same turn, rather than N turns later. Verified by reading the
      `harness-floor` row in `scripts\guard-due.ps1` (bdf71d1bb): its Covers is
      the wide net `test\win32\*.ps1`, which is exactly the expression the
      Summary said guard-due rows could not carry.
- [x] That DUE state refuses the commit. `parity-tasks.ps1 validate` fails on a
      guard-due row (T783), and step 0's `claim` reports the same verdict at the
      top of every turn - both observed green on this turn's claim, which listed
      `GUARD CURRENT harness-floor` and `GUARD CURRENT harness-floor-teeth`.
- [x] The gate has a demonstration that it can fail: `test\win32\harness-floor.ps1`
      is the teeth harness, carried by its own `harness-floor-teeth` guard row
      (stamped 2026-09-18 from 0b22f0a0e).

## Progress log

- 2026-08-15 10:36: priority: P1 -> P2 (test-infra hygiene; real but not user-facing - the audits still exist and any full audit run catches drift)
- 2026-09-18 06:31: status: todo -> done (by daily triage already-fixed sweep: T725's harness-floor guard row (bdf71d1bb) is the mechanism this card asked for) [commit bdf71d1bb]
