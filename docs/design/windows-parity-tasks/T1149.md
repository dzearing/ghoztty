---
id: T1149
title: "No acceptance script can press a shortcut that uses the Windows key, so nothing guards those defaults"
order: null
status: "done"
commits: []
seat: "win"
priority: "P2"
triage-reason: "P2: a coverage hole, not a reported defect - the shipping chords may well work for a person at the keyboard. It becomes P1 the moment the investigation finds the product side is broken."
milestone: "M1"
deps: ["T1129"]
tags: ["test", "infra"]
---

# T1149 - No acceptance script can press a shortcut that uses the Windows key, so nothing guards those defaults

## In plain terms

A lot of Ghoztty's built-in shortcuts use the Windows key — moving a pane
divider is ctrl+win+arrow, moving focus between panes is win+alt+arrow. No
automated test can press those, so if one of them broke, nothing here would
notice until somebody tried it by hand. The first job is to find out whether
they actually work at all.

**Answer (2026-09-24): they work, and now a test presses them.** The original
probe had pressed Windows-key chords that are not bound on Windows at all.

## Summary

Measured while validating T1129 (2026-08-23). The off-desktop test harness
drives keyboard shortcuts by posting `WM_KEYDOWN` with the modifier state faked
through `SetKeyboardState` (`test/win32/lib/TestDesktop.ps1`, `SendChord`).
Every non-super chord resolves this way — `ctrl+shift+e` opened a split in the
probe, and `ctrl+shift+alt+arrow` drove the whole T1129 acceptance. Three
super-modified chords in a row did nothing at all: `win+ctrl+right` and
`ctrl+win+right` (`resize_split`) and `win+alt+left` (`goto_split`), all posted
at the same window, with `SendChord` reporting success and the layout
unchanged.

`ApplyMods` sets `ks[0x5B]` (VK_LWIN) exactly as it sets the ctrl/shift/alt
pairs, and `Surface.zig`'s `keyMods()` reads `GetKeyState(VK_LWIN)`, so on
paper it should work. Two possibilities, and the task is to tell them apart
before fixing anything:

1. **Harness.** Windows does not honour a faked VK_LWIN through
   `SetKeyboardState` the way it honours the other modifiers, so no posted
   chord will ever carry `super`. Then the fix is a different injection route
   for these chords (or an explicit, documented inability, with the affected
   defaults listed).
2. **Product.** `super` never reaches the binding lookup on a real keypress
   either, and every Windows-key default shortcut is dead for users too. That
   is a P0 the moment it is confirmed.

T1129's acceptance works around it by binding the actions under test to
`ctrl+shift+alt+arrow` for that run, which tests what `resize_split` DOES but
not the chord that reaches it.

## Findings (2026-09-24)

**Neither possibility: the probe pressed unbound chords.** `ghoztty
+list-keybinds --default` on the win32 Debug build lists the super-modified
defaults as:

- `super+ctrl+shift+arrow_*` = `resize_split` (not `ctrl+win+arrow`)
- `super+ctrl+[` / `super+ctrl+]` = `goto_split:previous/next`
- `super+shift+arrow_up/down` = `goto_split:previous/next` (hero navigation)
- `super+ctrl+shift+j` = `write_screen_file:copy,plain`

`win+ctrl+right` and `win+alt+left` - the three chords measured on 2026-08-23 -
are bound to nothing, so "nothing happened" was the correct outcome. Pressed
through the same `SendChord` route (`Send-TestKeys -Modifiers ctrl,shift,win`),
the real defaults resolve: the faked VK_LWIN is honoured by `GetKeyState` in
`Surface.getModifiers`, exactly as the paper analysis said.

Not coverable by posted messages: whether the Windows SHELL swallows a chord
before the app ever sees it on a real keyboard. `super+shift+up/down` is
Windows' own stretch-window shortcut - filed as **T1725**.

## Goals

- [x] Tell harness vs product apart by evidence
- [x] An acceptance assertion over the shipping Windows-key defaults
- [x] Correct the comment in split-divider.ps1 that carried the wrong claim

## Validation criteria

- [x] The two possibilities above are told apart by evidence, and which one it
      is is written into this file. (Neither - see Findings; `+list-keybinds --default`.)
- [x] If it is the product: a fix, plus an acceptance assertion that a
      super-modified default resolves. (No product fix needed; the assertion
      landed anyway - four `T1149:` rows in split-divider.ps1.)
- [x] If it is the harness: a chord route that carries `super`, used by at
      least one acceptance assertion over a shipping Windows-key default.
      (The existing `SendChord` route carries it; super+ctrl+shift+left/right
      and super+ctrl+[/] asserted.)
- [x] `test\win32\split-divider.ps1` still ALL PASS. (2026-09-24: ALL PASS, 85 assertions.)

## Progress log

- 2026-09-24 08:29: claimed; work starting.
- 2026-09-24 08:36: root cause: probe pressed unbound chords (win+ctrl+arrow, win+alt+arrow); real defaults super+ctrl+shift+arrow / super+ctrl+[ ] resolve through SendChord. Added T1149 rows to split-divider.ps1 (ALL PASS 85). Filed T1725 (OS-reserved win+shift+up/down).
- 2026-09-24 08:59: status: in-progress -> done (by cli: David, from powershell, in ghoztty)
