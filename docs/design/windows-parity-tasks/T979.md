---
id: T979
title: "An acceptance script that reads a pane\u0027s banner straight out of +list can score a pane that has not published it yet"
order: null
deps: []
status: "done"
commits: ["3f87ffa99"]
seat: "win"
priority: "P2"
triage-reason: "A banner assertion that races the pane's publish is a flake generator across every restore-path script; it reads as a product bug every time it fires."
tags: ["test", "infra"]
---

# T979 - An acceptance script that reads a pane's banner straight out of +list can score a pane that has not published it yet

## Summary

A pane's sticky banner is published from that pane's own IO thread, after the pane already exists and already answers `+list`. So `+list` will happily hand back a leaf whose `banner` field is not filled in yet, and a script that reads it in the same breath as the leaf scores a pane that has not spoken. Not hypothetical: it sent arm E of `test/win32/session-relaunch-notify.ps1` red against a correct build, and the red was believed enough to be filed as a P0 defect (T977) about a restored pane losing its interrupted-session notice. The product was fine - the notice lands 0.8s after the leaf appears.

That script now waits (`Wait-BannerSettle`) and bounds the wait (E4b). Every other harness that reads `.banner` out of `+list` still has the unpolled shape: `agent-integrations.ps1`, `agent-recovery.ps1`, `cli-argv-fidelity.ps1`, `hook-json.ps1`, `pane-banner.ps1`, `pane-id.ps1`, `reset-context.ps1`, `window-name-env.ps1`. Most set the banner themselves through `+set-banner`, a synchronous IPC round-trip and therefore probably safe - but "probably" is the state this task exists to end. Sweep them, and where a read can outrun a publish, give it the same settle-then-score shape rather than a sleep.

## In plain terms

Some of our own tests ask the app "what banner is on this pane?" at the exact moment the pane appears, which is a moment before the pane has had a chance to answer. When that happens the test reports a missing banner and a real defect gets filed against working code - which is what already happened once. Nobody using Ghoztty sees anything from this; it costs engineering turns chasing bugs that are not there.

## Goals

- [x] Enumerate every acceptance script that reads a pane banner out of `+list`
- [x] For each, decide from the code whether the read can outrun the publish
- [x] Give the racy ones a bounded settle-then-score wait, never a sleep

## Validation criteria

- [x] Each named script either re-runs green with a settle wait, or has a recorded reason its read cannot race — the reasons are in the audit below rather than as comments in each script (a comment-only edit re-arms each script's guard and forces a GUI harness re-run for zero behavioral change). `restore-session-dup.ps1` re-ran ALL PASS (17) with its new settle waits.
- [x] No new sleeps: both new waits are deadline-bounded polls (20s / 40s) that fall through to the same assertion, so a missing notice still fails B8/C4.

## Audit (2026-09-26)

Ground truth for "synchronous": `handleSetBanner` (`src/apprt/win32/IpcHandlers.zig`) runs on the GUI thread — IPC handlers are serialized there (`IpcServer.zig`) — and calls `Surface.setPaneBanner`, which stores `banner_text` before `{"success":true}` goes back. So a `+list` issued after a `+set-banner` has returned always sees it.

| Script | Read | Verdict |
|---|---|---|
| `session-relaunch-notify.ps1` | E arms | already settled (`Wait-BannerSettle`, the original fix) |
| `restore-session-dup.ps1` | B7 | polled (`Wait-LoserBanner`) |
| `restore-session-dup.ps1` | B8, C3, C4 | **RACY — fixed.** C waited for the pane's OWN banner, which comes back with the manifest before the IO thread publishes the notice, then scored "no notice in the slot" and read the scrollback once. Now settles on the notice's scrollback copy (bounded) and re-reads the leaves. B8 read the scrollback once after the banner copy; now a bounded poll. |
| `resolve-window-persist.ps1` | C2/C3/D2 | polled (`Wait-Noticed`) |
| `resolve-window-persist.ps1` | A4 | negative control on a healthy launch; a race could only pass it, and A2 (every pane has a session) independently rules out the not-persisted path |
| `pane-id.ps1` | B6/B7 | after a synchronous `+set-banner` |
| `pane-id.ps1` | G2 | hook sets it via `+send-keys` (async) — already polled, 40s |
| `window-name-env.ps1` | A6 | synchronous `+set-banner`, and polled anyway |
| `cli-argv-fidelity.ps1` | D, D2 | synchronous `+set-banner`; D2 also polled (`Wait-For`) |
| `hook-json.ps1` | banner row | polled (20 x 200ms) |
| `pane-banner.ps1` | all | polled (`Wait-Banner`) |
| `reset-context.ps1` | `Banner-Of` (A8..L6) | the helper's `+set-banner` is a foreground call and `Run-Helper` waits for the helper to exit, so it has published before the read; nothing else sets a banner on those panes |
| `agent-recovery.ps1` | I12 | negative ("no give-up banner"); the give-up notice only publishes when retries run out, and I9/I10 have already proven the retry succeeded |
| `agent-integrations.ps1` | — | never reads a pane banner (it tests the banner *script* file); named by mistake |

## Progress log

- 2026-09-26 12:22: claimed; work starting.
- 2026-09-26 12:25: audit done: only restore-session-dup read an async publish unpolled (B8 + C3/C4 scrollback/banner after own-banner restore); fixed with bounded settle polls; run ALL PASS 17. Every other reader is either after a synchronous +set-banner (GUI-thread handler sets banner_text before replying) or already polled.
- 2026-09-26 12:41: status: in-progress -> done (by cli: David, from powershell, in ghoztty)
