---
id: T921
title: "The staleness gate cannot report its own harness going stale"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
triage-reason: "The staleness gate has teeth for every harness but its own, so the one script that can fail a commit is the one nothing re-checks."
tags: ["test", "infra"]
---

# T921 - The staleness gate cannot report its own harness going stale

## Summary

scripts\guard-due.ps1 is the gate every acceptance harness leans on, and its own acceptance is test\win32\guard-due.ps1 - but no coverage row watches scripts\guard-due.ps1, and that harness never stamps, so editing the gate (adding a row, changing the hashing or the verdict) re-arms nothing. Found while adding the docs-routing row in T822. Fix: add a guard-due row (Covers scripts\guard-due.ps1 + test\win32\guard-due.ps1) and give that harness the stamp-on-clean-green block its siblings have; watch the fixture-count arms (B1 asserts 'STAMPED go-loop (5 files)') when touching the table.

## In plain terms

Every test script in this repo is watched by a checker that refuses a commit
when code changed and its test was not re-run. The checker's own test was the
one nobody watched: editing the checker never asked for its test to be re-run,
and that test had quietly gone red three weeks ago without anyone seeing it.
Now the checker watches its own test like every other one.

## Goals

- [x] a `guard-due` row covering `scripts\guard-due.ps1` + `test\win32\guard-due.ps1`
- [x] the harness stamps its row on a clean green run
- [x] fix the arms that had gone red unseen (A2, C6, C10)

## Validation criteria

- [x] `test\win32\guard-due.ps1` ALL PASS (103 assertions) and prints `STAMPED guard-due (2 files)` - run 2026-09-23.
- [x] N1/N2 pin the live row and its coverage; N3 pins the stamp behind `failures -eq 0` - same run.
- [x] the row goes DUE on an edit to a covered file - appended a line to `test\win32\guard-due.ps1`: `check -Guard guard-due` printed `GUARD DUE guard-due` and exited 1; restored, it read CURRENT and exited 0 (2026-09-23).
- [x] harness floor green over the edited scripts - `floor-lane -Lane harness` PASS in 967s, `STAMPED harness-floor (403 files)`. The first run's three reds (persistence-flag, caller-anchor, build-mode-guard) were a stale zig-out; after a Debug rebuild each passed alone. body-complete read CURRENT at claim.

## Progress log

- 2026-09-23 09:21: claimed; work starting.
- 2026-09-23 09:42: row + self-stamp + N1-N3 landed uncommitted; guard-due harness ALL PASS 103 and STAMPED guard-due. Also fixed A2/C6/C10 (red unseen since argv-hazard/unroll-count glob scripts\*.ps1) by scoping to -Guard. Harness floor + body-complete were running in background when user asked for a reset; next turn: re-run them (floor-lane -Lane harness, body-complete-audit), prove DUE teeth by editing test\win32\guard-due.ps1, then commit.
- 2026-09-23 09:44: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-09-23 09:44: resumed: DUE teeth proven (appended a line to test\win32\guard-due.ps1 -> 'GUARD DUE guard-due' exit 1; restored -> CURRENT exit 0). Running floor-lane -Lane harness now, then persistence-flag.
