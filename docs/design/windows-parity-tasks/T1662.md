---
id: T1662
title: "A red zig lane reports an unrelated stderr line as the failure reason, so the real cause is never in the kept output"
order: null
deps: ["T1645"]
status: "done"
commits: ["8712f15a1"]
seat: "win"
priority: "P1"
tags: ["test", "infra"]
triage-reason: "P1: a failure summary that names the wrong line makes every red lane cost turns instead of minutes; this one cost four"
---

# T1662 - A red zig lane reports an unrelated stderr line as the failure reason, so the real cause is never in the kept output

## Summary

When a zig test fails, the build runner prints 'error: <test> failed: <text>' where the text is whatever stderr happened to arrive at that moment - and every test in the binary shares one stderr. floor-lane.ps1's FLOOR FAILURE DETAIL keeps the first six 'error:' lines, so that misattributed line is what survives into the kept output, while the REAL cause (the error return trace naming the same test) carries no 'error:' prefix and is dropped. T1645 is what this cost: four turns and five progress-log entries chased '[tripwire] (warn): untripped point=read' - a line emitted by src/tripwire.zig's own PASSING test - while the actual failure, error.WaitForTimeout at ViewerPane.zig:8237, sat in the same logs unread. Remedy: when Get-LaneFailureDetail sees 'error: <name> failed:', pull the stack-trace frames that name that test out of the same log and print those under it.

## In plain terms

When the test suite goes red, the few lines we keep to explain why can name the
wrong thing entirely. All the tests in one binary share a single output stream,
so the failure line picks up whatever text happened to be printed at that
moment - often a harmless note from a test that PASSED. The real reason is
further down and does not look like an error, so our filter throws it away.
The result is a red run whose kept output points at an innocent line, which is
how one defect cost four turns of chasing.

## Goals

- [x] A failure summary keeps the lines that name the failing test, not the
      lines that happen to match `error:`
- [x] The 2026-09-18 T1645 logs, replayed through the fixed formatter, surface
      `error.WaitForTimeout at ViewerPane.zig:8237` rather than
      `[tripwire] (warn): untripped point=read`

## Validation criteria

- [x] Given a captured lane log in which `error: <test> failed:` carries text
      from a different test, `Get-LaneFailureDetail` prints the error return
      trace frames that name the failing test underneath it.
      Verified twice: against the REAL
      `%TEMP%\floor-lane-win32-20260918-221751-383.log` (the block now carries
      `ViewerPane.zig:9386 in waitFor`, `return error.WaitForTimeout;` and
      `ViewerPane.zig:8237:13 in test.host floor: ...`), and as arms 20-26 of
      `test\win32\floor-lane-verdict-detail.ps1` over the same shape planted in
      a sandbox.
- [x] A lane log with no such misattribution is formatted exactly as it is
      today (no regression in the ordinary case). Arm 27 compares the WHOLE
      block against the literal pre-T1662 text, not a substring, so an extra
      line cannot slip past.
- [x] A negative control proves the new section can be absent: a log with no
      frames for the named test says so rather than printing nothing. Two arms:
      a log whose only trace belongs to a DIFFERENT test (the block must not
      offer `claude_plugin_migration` as this failure's cause, and must say the
      trace is missing), plus the real
      `floor-lane-agent-20260918-222931-510.log`, which genuinely carries no
      trace for the test the runner blamed.

## Progress log

- 2026-09-19 08:09: claimed; work starting.
- 2026-09-19 08:10: Confirmed the shape from the real 2026-09-18 logs. floor-lane-win32-20260918-221751-383.log line 53 is error: 'apprt.win32.ViewerPane.test.host floor: ...' failed: [tripwire] (warn): untripped point=read while the REAL cause sits at lines 292-297: a zig error return trace ViewerPane.zig:9386 in waitFor / return error.WaitForTimeout then ViewerPane.zig:8237 in test.host floor: .... No 'error:' prefix on any of it, so Get-LaneFailureDetail drops all of it. Fix going into scripts\lib\LaneVerdict.ps1: parse the failing test name out of the "failed:" line, find the LAST error-return-trace frame whose symbol is that test, walk backwards over the contiguous 3-line frame groups, and print those frames (caret lines dropped) under the error block.
- 2026-09-19 08:53 [session c294fe4f]: 2026-09-19 09:00 - RESET requested mid-task by the user; the fix is COMPLETE and green, only the final gate is unfinished. State of the tree (uncommitted): scripts/lib/LaneVerdict.ps1 (Get-LaneFailureDetail now returns FailedTest/Trace/TraceTruncated; new Get-LaneFailedTestName and Get-LaneErrorReturnTrace; Format-FloorFailureDetail prints the trace section or says it is absent), test/win32/floor-lane-verdict-detail.ps1 (arms 20-27, now ALL PASS 37 assertions), and this task file with goals and validation criteria ticked. Verified: replaying the REAL floor-lane-win32-20260918-221751-383.log now surfaces ViewerPane.zig:9386 waitFor / return error.WaitForTimeout / ViewerPane.zig:8237 test.host floor, which is exactly what T1645 could not see. floor-lane -Lane all = lib/none/win32/agent ALL PASS. REMAINING, and it is the only thing left: the harness lane. Its first run was 1 FAILURE - control-char-scan.ps1, caused by MY OWN progress note above (a PowerShell backtick-f in -Text became a 0x0c form feed in this file); that byte is patched and control-char-scan.ps1 is now ALL PASS 39 assertions standalone. A second full 'floor-lane.ps1 -Lane harness' was running when the reset landed and was killed by it. NEXT TURN: re-run 'powershell -NoProfile -File scripts/floor-lane.ps1 -Lane harness' (about 17 min; the only expected reds are the three PENDING members asserted-nothing/T1568, skip-visibility/T1123, persistence-flag/T1663), then parity-tasks validate, then commit and push. LESSON worth keeping: never put a backtick-escape sequence inside a -Text argument to parity-tasks.ps1 note.
- 2026-09-19 08:56: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-09-19 08:56 [session 87450dab]: resumed: tree matches the handoff note exactly (LaneVerdict.ps1 + floor-lane-verdict-detail.ps1 + stamps). Only the harness lane re-run remains; starting it now.
- 2026-09-19 09:19: took over stranded working-tree paths (dirty since before this turn's claim): docs/design/windows-parity-tasks/T1662.md, scripts/lib/LaneVerdict.ps1, test/win32/argv-hazard-audit.stamp.json, test/win32/cleanslate-audit.stamp.json, test/win32/command-resolve-audit.stamp.json, test/win32/desktop-launch-audit.stamp.json, test/win32/floor-lane-verdict-detail.ps1, test/win32/floor-lane-verdict-detail.stamp.json, test/win32/isolation-meta.stamp.json, test/win32/job-teardown.stamp.json, test/win32/launch-preflight-audit.stamp.json, test/win32/seam-audit.stamp.json, test/win32/stderr-capture-audit.stamp.json, test/win32/stderr-launch-capture.stamp.json, test/win32/unroll-count-audit.stamp.json, test/win32/verdict-exit-audit.stamp.json
- 2026-09-19 09:21: status: in-progress -> done (by cli: David, from powershell, in ghoztty) [commit 8712f15a1]
