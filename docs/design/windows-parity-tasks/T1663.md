---
id: T1663
title: "A relaunch no longer restores the previous run's window, so session persistence silently does nothing"
order: null
deps: []
status: "done"
commits: []
seat: "win"
priority: "P1"
milestone: "M1"
triage-reason: "P1: session persistence is a headline feature, its acceptance script reproduces the failure alone on a quiet box, and a user who restarts the app loses every pane with nothing on screen saying so"
tags: ["fix", "test"]
---

# T1663 - A relaunch no longer restores the previous run's window, so session persistence silently does nothing

## In plain terms

Close Ghoztty with a couple of panes open and start it again. It is supposed to
bring those panes back - that is what session persistence is for, and it is the
feature the whole `ghoztty-agent` exists to provide. Right now nothing comes
back: you get a fresh empty window, your panes are gone, and the app says
nothing about it. Turning persistence explicitly OFF produces the same result,
so from where a user stands the setting does nothing at all.

## Summary

`test\win32\persistence-flag.ps1` section C2 is red:

```
PASS  C1c the fixture window has 2 panes (got 2)
FAIL  C2a a launch with no persistence flag RESTORES the previous run's
      window (2 panes, got -1)
FAIL  C2b the restored pane is LIVE: input reaches its child and output returns
PASS  C3a --session-persistence=false restores nothing (got -1 pane(s))
PASS  C3b the non-persistent launch comes up with exactly its own window (got 1)
```

`-1` is "no such window at all". C1 builds the fixture fine (a two-pane
`t158-pair` window under `--session-persistence=true`), so the failure is in
the RESTORE, not in the setup. C3 passes vacuously: it asserts that nothing
comes back, and nothing does either way, which is exactly the shape that lets a
restore regression hide behind a green-looking "persistence off" assertion.

Section A - the source sweep that T1491 is filed against - is **green** in these
runs (`A1 no launch leaves persistence unstated (undeclared: 0 of 309)`), so
this is a different defect from that card and T1491 may itself be already
fixed; see the note below.

## What is already established

- **Not load, not isolation.** `suite-run`'s confirm pass re-ran it ALONE on a
  quiet box and it **REPRODUCED** (`[ 2/3] REPRODUCED 2m 19s
  persistence-flag.ps1 2 FAILURE(S) (26 passed)`). That distinguishes it from
  T1572's known false red, which has always gone green alone.
- **Not a stale `zig-out`.** `zig build -Dapp-runtime=win32 -Doptimize=Debug`
  was run immediately before both failing runs, which is the explanation T1571
  offers for this script going red and it does not apply here.
- **Not the T1645 edit.** Ruled out directly: `src\apprt\win32\ViewerWorktreeProbe.zig`
  was stashed, the app rebuilt Debug from a clean `ab37eccf9`, and
  `persistence-flag.ps1` failed with the identical two assertions. So the red
  belongs to the branch head, not to whatever is in flight.
- **It was green at `7ff8ecb6a`** (stamp generated 2026-09-18T23:13:31). The
  only commits between that and `ab37eccf9` are `9876331b5` (T861) and
  `ab37eccf9` itself, and **both are tracker/PowerShell only - neither touches
  product code.** That is the most interesting fact on this card: a pure-docs
  range cannot have caused a product regression.

## What it turned out to be

**Not a product regression, and not the reboot either: the fixture races the
app's debounce.** `App.markLayoutDirty` arms a 250 ms timer and every further
layout change RESTARTS it, and the kill in `Stop-AppOnly` is
`Stop-Process -Force` (TerminateProcess), so there is no shutdown flush. C1 ran
`+new-window`, `+split` and a ~300 ms `+list` poll back to back and then killed
the app - inside one debounce window. `app1.err.txt` from the failing run carries
**zero** `session-layout sync` lines and the manifest never held `t158-pair`, so
the relaunch correctly restored nothing. C2 was measuring a fixture that was
never saved.

Evidence, two probes against the same build that failed:

- a plain launch with 1.2 s / 1.5 s waits persists
  `'window-1'/1 + 't158-pair'/2` every time, with the three `session-layout
  sync` lines to match;
- the same sequence through `Start-OnTestDesktop` (the harness's own launch
  path) does too. So neither the test desktop nor the post-reboot box is
  implicated - only the missing wait.

That also explains the thing the card called its most interesting fact: a
pure-docs commit range cannot cause a product regression, and it did not. A
300 ms poll racing a 250 ms debounce is a coin toss the box's speed decides,
which is why it was green on 09-18 and red on 09-19.

## The fix

In the harness, because the harness is what is wrong:

- **`C1d` asserts the fixture is on DISK before the app is killed** -
  `Wait-Persisted` / `Get-PersistedLeafCount` poll the manifest for the
  `t158-pair` window's two leaves. A write that stops happening now fails
  naming the fixture, instead of three assertions later as a restore that did
  nothing.
- **The manifest path has one owner.** `Get-DebugSessionLayoutPath` in
  `test\win32\lib\CleanSlate.ps1` (the `-debug` stem plus the T691 lineage
  suffix); `Clear-DebugSessionLayout` now calls it instead of inlining it.
- **`C3a` is no longer vacuous**: it ANDs in C2's result, so "nothing came
  back" only counts as the flag working if something would have come back
  without it.

## Validation criteria

- [x] The question above is answered with evidence: neither. The fixture kills
      the app inside the 250 ms layout debounce, so nothing was ever persisted
      to restore - proved by `app1.err.txt` having no `session-layout sync`
      line at all, and by two probes that persist reliably once a wait exists.
- [x] `test\win32\persistence-flag.ps1` is **ALL PASS** on a quiet box, twice
      running, on a freshly rebuilt Debug `zig-out` (`ALL PASS (29)` both runs,
      after `zig build -Dapp-runtime=win32 -Doptimize=Debug`).
- [x] C3 can no longer pass vacuously. Demonstrated rather than argued: a run
      under `GHOZTTY_RESTORE_SKIP=1` (the T620 seam that forces "restore yields
      nothing") scores `FAIL C3a ... C2 restored=False` where the old assertion
      passed - 3 failures instead of 2.
- [x] `scripts\floor-lane.ps1 -Lane harness` scores `persistence-flag.ps1`
      green, and its entry is removed from `$HARNESS_FLOOR_PENDING` in
      `scripts\lib\HarnessFloor.ps1` (the ratchet fails on a stale exception, so
      this is enforced rather than remembered). Run of 2026-09-19 10:18:
      `[18/29] PASS 56.6s persistence-flag.ps1 ALL PASS (29)`, floor verdict
      `HARNESS FLOOR: ALL PASS (29 audits, 2 PENDING)` - the two pending being
      `asserted-nothing.ps1` (T1568) and `skip-visibility.ps1` (T1123), neither
      of them this card's.
- [x] A real relaunch brings back a two-pane window **by hand**, not only under
      the harness: a `t1663-hand` window with two panes, quit with
      **ctrl+shift+q** (the win32 `quit` binding) rather than TerminateProcess,
      the app then confirmed gone (0 windows, `+list` answers `-1`), the
      manifest holding 2 leaves, and the relaunch coming up with the two panes
      back. Closing the windows one at a time is deliberately NOT this gesture -
      that is the user saying they do not want them back, and it correctly
      persists nothing.

## Notes for whoever takes this

- `T1491` ("the persistence sweep's own assertion has gone red") is about
  section **A** of the same script and section A is green now (0 undeclared of
  309). It looks already-fixed; verify and close it naming the commit that
  actually fixed it, rather than folding it into this card.
- `T1572` is the GUI-member-in-a-source-sweep-lane card and is the reason this
  script's reds have been dismissed as noise before. This one is not noise, and
  that is the trap: the same script has both failure modes.

## Progress log

- 2026-09-19 06:45: filed from the T1645 turn's harness-floor sweep. Listed in
  `$HARNESS_FLOOR_PENDING` against this id so the floor reports it on every run
  instead of wedging the gate; the ratchet will fail on a green member, so the
  entry cannot outlive the fix.
- 2026-09-19 09:26: claimed; work starting.
- 2026-09-19 09:36: root cause: NOT a product regression. App.zig debounces the layout write 250ms and every mutation RESTARTS the timer; section C1 ran +new-window, +split and a ~300ms +list poll back to back, then Stop-AppOnly TerminateProcess'd the app - so app1.err.txt carries ZERO 'session-layout sync' lines and the manifest never held t158-pair. The restore then correctly restored nothing. Proved with two probes: a launch with 1.2s/1.5s waits persists 'window-1'/1 + 't158-pair'/2 every time. Fix is in the harness: new C1d waits for the fixture to be ON DISK before the kill (Get-PersistedLeafCount / Wait-Persisted, path via new Get-DebugSessionLayoutPath in lib\CleanSlate.ps1), and C3a now ANDs in C2's result so a dead restore cannot read as a correct refusal.
- 2026-09-19 10:10: TURN ENDED BY USER REQUEST mid-validation - resume, do not restart. The fix is COMPLETE and UNCOMMITTED in the tree: test/win32/persistence-flag.ps1 (new C1d wait-for-persist + non-vacuous C3a), test/win32/lib/CleanSlate.ps1 (new Get-DebugSessionLayoutPath), scripts/lib/HarnessFloor.ps1 (pending entry removed), T1663.md (findings + 3 criteria ticked). Commit message already drafted at temp/t1663-msg.txt. Evidence so far: persistence-flag.ps1 ALL PASS (29) twice; GHOZTTY_RESTORE_SKIP=1 negative control makes the new C3a fail where the old one passed; floor-lane -Lane all = ALL LANES PASS (lib/none/win32/agent). STILL OWED: (1) floor-lane -Lane harness was at 23/29 and NOT finished - re-run it (only known red is skip-visibility.ps1, which is T1123-pending); (2) the by-hand relaunch criterion - scratch script ready at C:/Users/David/AppData/Local/Temp/claude/D--git-ghoztty/6adf2396-2cdd-47dd-98ff-2dfb5093de57/scratchpad/t1663-byhand.ps1 (graceful +close quit, then relaunch, expects 2 panes back); (3) re-run persistence-flag.ps1 once to re-stamp its guard, because the files were line-ending-normalised to CRLF after the stamping run; (4) commit via git-commit-guard and push.
- 2026-09-19 10:10: TURN ENDED BY USER REQUEST mid-validation - RESUME, do not restart. The fix is COMPLETE and UNCOMMITTED in the tree: test\win32\persistence-flag.ps1 (new C1d wait-for-persist + non-vacuous C3a), test\win32\lib\CleanSlate.ps1 (new Get-DebugSessionLayoutPath), scripts\lib\HarnessFloor.ps1 (pending entry removed), T1663.md (findings + 3 criteria ticked). Commit message already drafted at temp\t1663-msg.txt. Evidence so far: persistence-flag.ps1 ALL PASS (29) twice running; a GHOZTTY_RESTORE_SKIP=1 negative control makes the new C3a FAIL where the old one passed; floor-lane -Lane all = ALL LANES PASS (lib/none/win32/agent). STILL OWED: (1) floor-lane -Lane harness reached 23/29 and was NOT finished - re-run it (only red seen was skip-visibility.ps1, which is T1123-pending); (2) the by-hand relaunch criterion - scratch script ready at the session scratchpad as t1663-byhand.ps1 (graceful +close quit, relaunch, expects 2 panes back); (3) re-run persistence-flag.ps1 once to re-stamp its guard, since the files were line-ending-normalised to CRLF AFTER the stamping run; (4) commit through git-commit-guard and push.
- 2026-09-19 10:12: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-09-19 10:18: resumed 2026-09-19: rebuilt Debug win32 (exit 0); persistence-flag.ps1 = ALL PASS (29), guard STAMP UNCHANGED. By-hand criterion DONE with the real user gesture: ctrl+shift+q (the win32 'quit' binding) with a two-pane t1663-hand window open, on an isolated LOCALAPPDATA/pipe suffix. Closing the windows one by one is NOT that gesture - it is the user saying they do not want them back, and correctly persists nothing (first attempt read as a failure for that reason). After the quit the first app is confirmed GONE (0 windows, +list answers -1), the manifest holds 2 leaves, and the relaunch brings the two-pane window back: BY-HAND RESTORE OK. Remaining: floor-lane -Lane harness, then commit.
- 2026-09-19 10:50: status: in-progress -> done (by cli: David, from bash, in ghoztty)
