---
id: T702
title: "connection.zig tests spawn mock-agent threads that are never joined when the body returns early"
order: null
deps: ["T693"]
status: "done"
commits: ["722c201d4"]
seat: "win"
priority: "P2"
triage-reason: "Same use-after-free class as T693 and it corrupts a run that was already failing, so it makes a red lane unreadable; not user-facing on its own."
tags: ["test"]
---

# T702 - connection.zig tests spawn mock-agent threads that are never joined when the body returns early

## Summary

A test that spawns a mock-agent thread and joins it only at the end of the body leaks that thread on every 'try' in between; the thread then reads into a MockAgent the body's defers are about to free - the T693 use-after-free class reached by a different route, and it only fires when the test was already failing. T693 guarded the two sites it touched with a disarming errdefer; 21 spawn sites in the file were not audited.

## In plain terms

When one of these automated tests fails, it should tell you what went wrong.
Right now a failure can instead hang the run or take it down with a crash, so
the message you actually needed never gets printed. Nobody sees this on a good
day — it only bites on the day something is already broken, which is the worst
possible time for the test suite to stop making sense.

## Goals

- [x] Every spawned mock-agent thread in `connection.zig`'s tests is joined on
      the failure path as well as the success path — already true when this task
      was picked up; the sweep landed in `a667007a7` (T536), and all 17
      test-local spawn sites carry a disarming errdefer
- [x] The guard is one line, not five, wherever it repeats — kept as the
      four-line errdefer rather than a helper: the shutdown call inside it is
      per-site (`conn.shutdown()` here, a listener close elsewhere), so a helper
      would only hide the one part that has to be read
- [x] The rule is machine-checked, not remembered: `test\win32\thread-join-audit.ps1`

## Details

The shape T693 fixed twice:

```zig
const ath = try std.Thread.spawn(.{}, InboundAgentCtx.run, .{&ictx});
try conn.start();          // <- any failure here leaks `ath`
_ = try conn.waitHandshake();
ath.join();
```

The guard it used, which must disarm (a second join is UB) and must close the
stream first (the agent thread is parked on a read that nothing else unblocks):

```zig
var ath_joined = false;
errdefer if (!ath_joined) {
    conn.shutdown();
    ath.join();
};
```

Work: sweep the 21 `std.Thread.spawn` sites in `src/remote/connection.zig`,
guard each one whose join is not already unconditional, and factor the pattern
into a small test helper if the repetition earns it.

**What was actually left to do (2026-09-12).** The sweep had already happened:
`a667007a7` (T536) guarded every test-local spawn site in the file, 33 days
after this card was filed and without closing it. Re-verifying by hand cost a
context and produced no code, which is the shape `CHECK FIRST` exists to catch —
and a second hand audit would have been worth exactly as much as the first one,
because nothing re-asks the question. So the deliverable became the validation
this task was owed: `test\win32\thread-join-audit.ps1` plus
`test\win32\lib\ThreadJoinAudit.ps1`, which check the rule over every `.zig`
file under `src`, with `connection.zig` asserted clean and the 31 sites still
open elsewhere held at a per-file baseline (T1505) so a NEW one goes red.

## Validation criteria

- [x] Every spawn site is either guarded or documented as unconditionally
      joined (name each in the progress log) — all 17 named in the 14:18 entry;
      now asserted by section B of the audit rather than by that list
- [x] A deliberately-failed assertion in a guarded test reports the failure
      rather than hanging or crashing (teeth-check at least one site) — planted
      `return error.DeliberateT702TeethCheck` between the spawn and the join in
      the T739 repaint test and ran
      `zig build test -Dapp-runtime=win32 -Dtest-filter="T739: an injected repaint"`:
      `82/83 passed, 1 failed` naming `connection.zig:4344`, no hang, no panic.
      Reverted afterwards (the file is byte-identical to its backup)
- [x] All three zig lanes stay green via `scripts\floor-lane.ps1 -Lane all`
- [x] The audit's own teeth: `-TeethCheck` removes a real guard from
      `connection.zig`, plants an unknown file carrying the defect, and plants
      one under `src\apprt\win32` — all three score red (29 assertions)

## Progress log

- 2026-09-12 14:14: claimed; work starting.
- 2026-09-12 14:18: CHECK FIRST resolved: already fixed at the source. Audited all 17 test-local spawn sites in src/remote/connection.zig (4060,4130,4336,4477,4543,4622,4709,4753,5083,5140,5188,5248,6268,6339,6944,7052,7080) - every one carries a disarming errdefer that shuts the stream down before joining, with no fallible statement between the spawn and the guard. The two non-local shapes are covered too: LifecycleHarness.start stores into h.thread and LifecycleHarness.destroy joins it unconditionally (every test registers defer h.destroy()), and the concurrent-OPEN loop guards a partial spawn with an errdefer over threads[0..spawned]. The sweep landed in a667007a7 (T536), not here. So the deliverable is the VALIDATION T702 was owed: a machine-checked rule, since a hand audit that found nothing is indistinguishable from one nobody re-runs. Prototype scan of src/**.zig finds ~38 test-local spawn sites elsewhere (pipe_stream, tcp_dial, link_control, keepalive, server, inbound_ring, ssh_transport, socket_stream, client_mux, blocking_queue, terminal/search) with the SAME defect - spot-checked pipe_stream.zig:591 and link_control.zig:452, both real. Plan: ship test\win32\thread-join-audit.ps1 + lib\ThreadJoinAudit.ps1, enforce clean over connection.zig, ratchet the rest against a recorded baseline so a NEW unguarded site goes red, and file a follow-up to work the baseline to zero.
- 2026-09-12 14:49: status: in-progress -> done [commit 722c201d4]
