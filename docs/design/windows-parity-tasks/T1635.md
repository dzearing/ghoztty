---
id: T1635
title: "chooser-orphan-badge goes red when it runs immediately after another chooser harness"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "the badge harness goes red for the run before it, which reads as a product regression"
tags: ["test"]
---

# T1635 - chooser-orphan-badge goes red when it runs immediately after another chooser harness

## Summary

Observed 2026-09-17 during T812's guard sweep: run fourth in a back-to-back batch (chooser-resume-remote, then this) it scored 1 FAILURE after 16 assertions; run standalone seconds later it scored ALL PASS (17). The batch was sequential - no overlapped lane - so the suspect is teardown bleed between two chooser fixtures rather than box load: the previous script's agent, its per-user pipe or its sessions.json are still settling when this one's Reset-AgentState runs. Reproduce by running chooser-resume-remote and chooser-orphan-badge back to back in one shell and capturing the FULL output (the batch that caught it kept only the last three lines, which is why the failing assertion is not named here). Fix is whatever CleanSlate is not waiting for; a red flake here reads as a broken orphan badge.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
