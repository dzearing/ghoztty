---
id: T688
title: "Kill-RepoInstances kills once without verifying, in 34 private copies"
order: null
deps: [T158]
status: "done"
commits: []
seat: "win"
priority: "P2"
triage-reason: "A stray instance makes a healthy script report 'GUI died at launch'; the misread costs a triage every time. Harness hygiene, not user-facing."
tags: ["infra", "test"]
---

# T688 - Kill-RepoInstances kills once without verifying, in 34 private copies

## In plain terms

Some of the Windows test scripts sometimes report that the app "died at
launch" when it did nothing of the sort — a leftover copy from the previous
test was still running, so the new one handed its window to the old one and
exited, exactly as designed. The report reads like a crash, and whoever is
watching goes looking for a bug that is not there. This makes the tests wait
until the box is actually clean before they blame the app.

## Summary

`Kill-RepoInstances` is a five-line private helper, copy-pasted into **34**
scripts in `test/win32/` (`Select-String -Pattern 'function Kill-RepoInstances'`).
Every copy kills once, sleeps ~500ms and returns — it never verifies that the
processes are gone. When one survives the sleep:

- the script's own launch finds the pipe already owned, forwards its
  `new-window` and exits (log line: `another instance owns the IPC pipe;
  forwarding new-window`);
- three seconds later `$proc.HasExited` is true and the script prints
  `SETUP FAIL: GUI died at launch`, which reads as a crash and is not one.

Noted while sweeping the suite for T158. Two adjacent facts matter for the fix:

- `ghoztty +list` / `+split` **auto-launch** an instance when none is running,
  so a stray CLI call between the kill and the launch re-creates the very
  process the kill removed. A verify loop has to run immediately before the
  launch, not at the top of the script.
- `lib\CleanSlate.ps1`'s `Stop-RepoGhoztty` is the shared version of this
  helper and has the same shape — kill, sleep, return a count — so the fix
  belongs there and the copies should route through it rather than growing 34
  private loops.

## Details

Route every copy through a shared helper that polls until the matching
processes are gone (path-exact, as today) or a deadline expires, and reports
which pids survived when it gives up. Keep the path-exact filter: matching on
process NAME would take the user's installed release and its live sessions with
it.

## Goals

- [x] The shared kill waits for the processes to actually go, instead of
      sleeping half a second and hoping.
- [x] When one will not go, the setup says which pid is still there and that
      the box is not clean - never `GUI died at launch`.
- [x] The wait is on trial: an acceptance section drives it through a survivor
      that outlasts the old sleep and one that never leaves.

## Validation criteria

- [x] `Stop-RepoGhoztty` (lib\CleanSlate.ps1) polls until zero matching
      processes remain, or fails loudly naming the survivors.
      *Verified: `test\win32\cleanslate-audit.ps1` section D, ALL PASS (29).*
- [x] The 34 private `Kill-RepoInstances` copies call it instead of
      hand-rolling kill+sleep.
      *Already true when this task was claimed - T351 converted all of them
      (now 50). Re-verified: every `Kill-RepoInstances` body in `test\win32\`
      is a single `Stop-RepoGhoztty` call, and `cleanslate-audit.ps1` section B
      is the standing gate that keeps a new private copy out.*
- [x] A negative control proves the loop: hold a repo-path instance alive past
      the old 500ms sleep and assert the helper waits for it rather than
      returning early.
      *Verified: section D2 holds a survivor for 1200ms through the
      `$script:CleanSlateProcQuery` seam and asserts the kill waited (1222ms
      observed, `-ge 1000`); D3 holds one forever and asserts the throw names
      the pid.*
- [x] A sample of the edited scripts still ALL PASS on the box.
      *Verified: `harness-process-leak.ps1` ALL PASS (42) - the one harness
      that exercises `CleanSlate.ps1` against real processes - plus the six
      corpus audits its edit made due, the four zig lanes and P1-P3.*

## Progress log

- 2026-09-12 06:17: claimed; work starting.
- 2026-09-12 06:22: CHECK FIRST resolved: T351 already routed all 50 private Kill-RepoInstances copies through Stop-RepoGhoztty, so the 34-copies half was done before this turn. What was LEFT is the verify: the shared helper still killed once, slept -SettleMs and returned. Fix: Stop-RepoGhoztty now polls (re-issuing the stop each round) until nothing path-exact matches, and THROWS naming the surviving pids at -TimeoutMs (5s default); -SettleMs is measured from the start of the wait so a clean box costs what it did. New Get-RepoGhozttyProcess enumerator behind a $script:CleanSlateProcQuery seam so the acceptance can hold states a real box cannot. cleanslate-audit.ps1 section D: real fake process (D1), a survivor outlasting the old 500ms sleep (D2, waited 1222ms), one that never goes (D3, throws naming the pid). ALL PASS 29 assertions.
- 2026-09-12 06:46: status: in-progress -> done
