---
id: T817
title: "A win32 diff pane has no next/previous-change or unified-vs-side-by-side controls"
order: null
deps: ["T463"]
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
tags: ["feature"]
triage-reason: "P2: a diff renders and scrolls on Windows since T463; these are the affordances that make a long one walkable, and the page half of both already ships"
---

# T817 - A win32 diff pane has no next/previous-change or unified-vs-side-by-side controls

## In plain terms

A diff pane on Windows shows the changes, but you read it by scrolling. On the
Mac the pane's toolbar has two things this one does not: buttons that jump to
the next and previous change, and a switch between the stacked view and the
side-by-side one. Both already exist in the page itself — nothing is calling
them from Windows.

## Goals

- [x] Next / previous change buttons in a diff pane's nav bar, stepping change
      blocks and rolling over into the adjacent file
- [x] A unified ⇄ side-by-side toggle, with the choice remembered across panes
      and sessions the way Mac's `diffViewStyle` preference is
- [x] The nav bar stays PINNED OPEN in a diff pane — already true since
      **T1185** made the bar part of every viewer pane's frame, so this needed
      no rule of its own; re-verified rather than re-implemented

## Evidence

The page half is already bundled and already reachable: `src/viewer/diff.js`
exposes `setStyle` and `nav`, and `viewer.js` re-exports both as
`window.__viewer.setDiffStyle` / `window.__viewer.diffNav`. T463 wired the
CONTENT path (listing + patch) and deliberately left the chrome: `ViewerPane`
holds `diff_style` as an in-session field with no control that changes it, and
`viewer_nav_layout.zig` lays out back / forward / reload / home / address with
no diff arm.

Mac's counterparts: `ViewerView.diffNav` / `diffViewStyle`, and
`ViewerNavigationBar.swift`'s diff controls. The pinning rule is
`ViewerView.navBarPinned` (`sidePanelLayout == .compact || feedbackOpen ||
isDiffMode`).

## Validation criteria

- [x] `none`-lane geometry tests for the added nav-bar controls at 1.0/1.25/
      1.5/2.0 (the design-system rule for every win32 chrome number) — four
      new tests in `viewer_nav_layout.zig` (strip order after Home, the shed
      rule, reachability at every width one pixel at a time, monotonic
      widening) plus the glyph pair's own symmetry test in `icon_button.zig`;
      green in the `none` and `win32` lanes.
- [x] An acceptance arm in `test/win32/viewer-diff.ps1`: section 9, 25
      assertions. Presence + strip order + the tooltip-hit-box rule from the
      bar's own tip log, a NEGATIVE control on a pane showing no diff, then
      real posted clicks — next-change walks off the end of a file into the
      next one, and the layout toggle flips the pane to side-by-side.
- [x] The layout survives a new pane: a diff pane opened AFTER the toggle logs
      `style=split` in its own listing line — the persistence half, asserted
      rather than assumed.
- [x] Floor green on the platform changed: `floor-lane.ps1 -Lane all` (lib,
      none, win32, agent) all PASS, plus every guard `guard-due.ps1 check`
      named for the touched code — the four other viewer suites
      (`viewer-diff-tree`, `viewer-nav-pin`, `viewer-narrow-pane`,
      `viewer-worktree-port`, `viewer-close`), the harness floor, and the
      static audits.

## Progress log

- 2026-09-08 03:23: T639 gave every present nav-bar button a tooltip through a pure viewer_nav_layout.label(Button, Labels); the diff controls this task adds must extend that enum + label (its 'no button is left unlabelled' test fails until they do), and Mac's wording is 'Previous change' / 'Next change' / the split-vs-unified flip.
- 2026-09-17 09:22: claimed; work starting.
- 2026-09-17 09:49: Built: viewer_nav_layout gains prev_change/next_change/diff_style (Mac's strip order, after Home) plus an explicit shed_order so a narrowing diff pane gives up back/forward before change-stepping; two new closed-outline glyphs (diff_split/diff_unified, Fluent DockRight/DockBottom); viewer_diff.Style enum + setDiffStyleCall/diffNavCall; viewer_prefs.loadDiffStyle/saveDiffStyle (%LOCALAPPDATA%\ghoztty\viewer_diff_style); bridge parses diffNavOverflow; ViewerPane.toggleDiffStyle/diffNav/diffNavOverflow with file_tree.adjacentFile walking VISIBLE rows. Goal 3 (nav bar pinned open) was already satisfied by T1185 - the bar is part of every viewer pane's frame.
