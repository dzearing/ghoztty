---
id: T1123
title: "A script violates the skip-visibility rule without being a named exception"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "The rule exists so a skipped assertion can never masquerade as a pass; an unlisted violator undermines every verdict."
tags: ["test"]
---

# T1123 - A script violates the skip-visibility rule without being a named exception

## In plain terms

There is a rule that a check which quietly skips something must say so out loud, so a skipped check can never look like a passing one. One of the checks breaks that rule and is not on the list of known exceptions.

## Summary

`test\win32\skip-visibility.ps1` scored `1 FAILURE(S)` in the T1094 sweep:

```
FAIL  B1 no script violates the rule without being a named exception
```

This is a meta-audit over the whole suite, which makes it more important than its one-line failure suggests: the rule it enforces is what stops a silently-skipped assertion from reading as a pass. While it is red, the suite's own honesty guarantee is not being enforced.

The script must be re-run to name the violator - the sweep log records the verdict but not the offending script.

## Reproduced alone

Re-run on its own with `scripts\suite-run.ps1 confirm` (T1137), against a Debug
`zig-out` at HEAD. Logs: `temp\suite-runs\fwd1\<script>.alone.log`.

- `skip-visibility.ps1` - **reproduced alone: yes** (2026-08-23, `1 FAILURE(S)`). Red on its own too, so the failure is not an artefact of the sweep it was found in.

## Where

- `test/win32/skip-visibility.ps1` section B

## Goals

- [ ] Run it and get the name of the violating script.
- [ ] Fix that script to declare its skip visibly - do NOT simply add it to the exception list, which would defeat the rule.
- [ ] Add it to the exceptions only if there is a real reason, recorded here.

## Validation criteria

- [ ] the violating script is named here
- [ ] it declares its skip visibly, or its exception is justified in writing
- [ ] `test\win32\skip-visibility.ps1` is ALL PASS

## Progress log

- 2026-08-22: filed by T1094's full acceptance sweep (242 scripts, forward order, Debug build at 9a764deea). Evidence quoted above is from that run's log in `temp\suite-runs\fwd1\`.
- 2026-08-23 12:30: reproduced alone: YES (2026-08-23, T1137's confirm pass against a Debug zig-out at HEAD): skip-visibility.ps1 (1 FAILURE(S)). Red on its own as well as in the sweep, so the failure is not an artefact of what ran before it - the triage this task carries stands. Log: temp\suite-runs\fwd1\<script>.alone.log.
- 2026-09-14 10:30 [session fc225df2]: still red 2026-09-14 (T725's harness-floor probe): B1 now names 13 unlisted violators - build-cache, build-fresh-guard, daily-publish, ghoztty-cleanup, install-maintenance, install-prepare, install-restart, ipc-remote, msg-timer-ids, tab-strip, update-apply, viewer-close, viewer-feedback-capture - on top of the 2 already pending (overlay-zorder, split-divider). Two shapes: 'uncounted' (a skip site that records nothing) and 'unreported' (skips happened but the ALL PASS line names no skip count). T725 carries skip-visibility in the harness floor as PENDING against this task; when it goes green, add its guard-due row so it cannot drift again.
