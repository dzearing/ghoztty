---
id: T1733
title: "win32 caption buttons (... / minimize / maximize / close) show no tooltip, where native Windows caption buttons name themselves"
order: null
deps: ["T1417"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "Windows-native polish: every native caption button names itself on hover; ours are custom-drawn and silent. Not M1 - no Mac counterpart (traffic lights carry no tips), so not a parity gap."
tags: ["polish"]
---

# T1733 - win32 caption buttons (... / minimize / maximize / close) show no tooltip, where native Windows caption buttons name themselves

## Summary

T1417 gave the caption band a tooltip host (the strip's control, TipSubject). The custom-drawn '...' and the three caption slabs still show nothing on hover; Windows' own caption buttons show Minimize / Maximize / Restore Down / Close. Add a pure caption_layout tooltip(button, zoomed) text fn (none-lane tested), extend TipSubject, and extend remote-pill/caption-bar acceptance the way T1417's section 6 does (Send-TestRawMessagePair hover+timer).

## Validation criteria

- [ ] Hovering "...", minimize, maximize (restore when zoomed) and close each
      shows its native-worded tooltip; the text comes from one pure function
      with none-lane tests.
- [ ] The pill and tab tips still pass `remote-pill.ps1` / `tab-tooltip.ps1`.
- [ ] An acceptance section scores the arm + show lines, with a negative.

