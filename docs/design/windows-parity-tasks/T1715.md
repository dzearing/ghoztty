---
id: T1715
title: "go-loop-guard is red: BB17 turn-stale re-entry and the L duplicate-window assertions fail"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P1"
tags: []
---

# T1715 - go-loop-guard is red: BB17 turn-stale re-entry and the L duplicate-window assertions fail

## Summary

test\win32\go-loop-guard.ps1 is red at fc3e1fd76: 5 FAILURE(S), 416 passed (run 2026-09-23 18:40 by the start-go controller, GHOZTTY_TEST_ALLOW_STALE=1 because the exe was stale from in-flight work; the failing sections drive scripts, not the exe). BB17 (a held lock whose turn has not completed is re-entered anyway) most likely regressed by ca0e12a7f (T1711: the watchdog now leaves a turn alone while it waits on its own background job), which may exempt a turn past -TurnStaleMinutes; decide whether the bg-wait exemption must yield to the turn clock, then fix code or fixture. L2, L6, L9, L11 (duplicate-window section: marks its own window, unmarked window untouched, names the primary, primary told in its own pane) - unknown whether regression or interference from the live loop window during the run. Not caused by fc3e1fd76 (health-script only; every section that runs go-loop-health.ps1 passed).

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
