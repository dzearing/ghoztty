---
id: T1606
title: "The tab tooltip now carries a title line it should not, and its guard could not see the change that caused it"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
milestone: "M1"
priority: "P2"
triage-reason: "A tooltip showing a line it should not is visible on every tab hover, and its guard could not see the change."
tags: ["fix", "test"]
---

# T1606 - The tab tooltip now carries a title line it should not, and its guard could not see the change that caused it

## Summary

test\win32\tab-tooltip.ps1 is 2 FAILURE(S) / 18 passed on 2026-09-16 (arms A 'the home prefix reads as ~' and E 'a title that fits keeps the tip cwd-only'). The oracle text is now two lines - the raw 'C:\Users\David\AppData\Local\Temp\ghoztty-tabtip-NNNN\tipA713' followed by the correctly ~-abbreviated cwd - so the T556 elided-title line is firing for a tab whose title the test assumes fits. Either the default tab title became the full cwd path (product) or the fixture's window is narrower than it was (test); decide from the code, do not just widen the window. SECOND, SEPARATE DEFECT and the reason this went unnoticed: the tab-tooltip guard covers only src\apprt\win32\tab_tooltip.zig and the script, so it read CURRENT from a 2026-08-21 stamp (56e310ef0) the whole time - whatever sets the tab title lives elsewhere and is outside the Covers list. Fix the coverage as well as the red, or the next title change is invisible in exactly the same way. Found by T786, whose only edit to that file was a comment block.

## In plain terms

Hovering a tab briefly showed the folder twice - once as a long raw path, once
shortened with `~`. That was already fixed on 2026-09-16. What remained is why
nobody noticed for a month: the automatic check that says "this test needs
re-running" was only watching the tooltip's own file, not the two files that
decide what the tab's title is and whether it got cut short. It now watches
both, so the next change there asks for the tooltip test to be re-run.

## Goals

- [x] The duplicated folder line is gone (fixed by ec17471fb under T1622)
- [x] The tab-tooltip guard covers the files that decide the tip's title line

## Validation criteria

- [x] Defect 1 already fixed: ec17471fb (T1622) made `tipTextTitled` drop a title that only restates the location; `test\win32\tab-tooltip.ps1` ALL PASS (29 assertions) on the debug build at 17b8923ef.
- [x] Coverage: the `tab-tooltip` row in `scripts\guard-due.ps1` now covers `src\apprt\win32\tab_strip_layout.zig` (elision verdict) and `src\termio\stream_handler.zig` (untitled-pane title-from-pwd rule). Verified by `guard-due.ps1 check` reporting `GUARD DUE tab-tooltip ... new src/apprt/win32/tab_strip_layout.zig, new src/termio/stream_handler.zig` before the re-run, and STAMPED (4 files) after it.
- [x] `test\win32\guard-due.ps1` ALL PASS (113); body-complete, argv-hazard, unroll-count audits ALL PASS.

## Progress log

- 2026-09-25 13:29: claimed; work starting.
- 2026-09-25 13:35: defect 1 already fixed by ec17471fb (T1622); tab-tooltip.ps1 ALL PASS 29. Coverage fix: guard row now covers tab_strip_layout.zig + termio/stream_handler.zig (Window.zig still excluded as noise); guard read DUE then re-stamped.
