---
id: T1507
title: "A remote machine on a skewed protocol version is back to reading as unreachable (arm 4b of ipc-remote is red)"
order: null
deps: []
status: "done"
commits: []
seat: "win"
priority: "P1"
tags: ["fix"]
---

# T1507 - A remote machine on a skewed protocol version is back to reading as unreachable (arm 4b of ipc-remote is red)

## Summary

The app classifies a proto-version skew as DialFailed, so the CLI says 'failed to reach' - T628's wrong answer, back

## In plain terms

When the machine you are connecting to runs a different version of Ghoztty, the
error tells you the machine could not be reached. So you go and check the box,
the network and whether its agent is running - all four of which are fine - when
the only thing that can fix it is updating one side. T628 removed exactly this
answer; it is back.

## Goals

- [x] `+new-remote-window` against an agent on an incompatible protocol version
      says `incompatible Ghoztty version on <host>:<port>` again
- [x] It does NOT say `failed to reach`
- [x] Arm 4b of `test\win32\ipc-remote.ps1` is green, and is demonstrated to go
      red on the broken build
- [x] And arm 4b can no longer report a slow agent start as this defect

## Details

### Measured 2026-09-12, Debug build, `test\win32\ipc-remote.ps1`

```
== 4b: a PROTOCOL SKEW says so, instead of reading as unreachable (T628)
  PASS skewed agent is running
  PASS skewed dial exits nonzero
  FAIL error names an incompatible version
  PASS error names the endpoint
  FAIL error does NOT blame reachability
    skew error was: failed to reach 127.0.0.1:54663: the agent is not running
                    or not reachable
```

Reproduced twice in a row. **Not caused by the T704 change** that was in the
tree at the time: that touches only the agent's per-shell wsl argv
(`windowsCommandArgs` in `src\remote\agent\pty_child.zig`) and nothing on the
handshake or classification path.

The skewed agent really is reachable, so the misclassification is not the
harness failing to produce the state: a second agent started with
`GHOZTTY_AGENT_INSTANCE` forked and `GHOZTTY_AGENT_PROTO_VERSION=0` while
sharing the first's `GHOSTTY_AGENT_LOCK` was measured ALIVE and accepting TCP
on its port (both ports answered `Connect`), so the app dialed a live listener
and still answered `failed to reach`.

### Where to look

`IpcHandlers.zig:620` asks `dial_failure.classify(err) == .incompatible`, and
`classify` only answers that for `error.ProtocolIncompatible`. That error is
minted in exactly one place - `tcp_dial.classifyHandshakeError` - and only when
BOTH halves hold:

```zig
error.Incompatible => if (peer_proto != null) error.ProtocolIncompatible
                      else error.HandshakeFailed,
```

So one of two things is true and the first job is to say which:

1. `waitHandshakeTicking` is no longer returning `error.Incompatible` for a
   version mismatch (it returns a timeout or a generic failure first), or
2. `conn.peerProtoVersion()` is null at that point - the peer's HELLO facts are
   read AFTER `shutdown()`/`joinPump()` precisely so they are available, and
   something on that path stopped recording them.

`log.warn("IPC new-remote-window: dial failed ... err={}")` in `IpcHandlers` has
the answer; on a Debug build it is in the app's log sink
(`%LOCALAPPDATA%\ghoztty[-debug]\ghoztty.log`). Get that error name first,
then fix, then teeth-check by reverting the fix and watching arm 4b go red.

## Validation criteria

- [x] The `err=` the app logs for a skewed dial is named in this task's progress
      log (which of the two branches above it was) - **neither**: the app logs
      `err=error.ProtocolIncompatible`, captured by pre-launching the debug app
      with `-RedirectStandardError` (see *What was actually measured*)
- [x] `test\win32\ipc-remote.ps1` is ALL PASS, arm 4b included - six
      consecutive green runs, the last one on the reverted (fixed) build
- [x] Arm 4b demonstrated RED on the pre-fix build (teeth check): with
      `classifyHandshakeError` collapsed back to `error.HandshakeFailed`, the arm
      reproduces the reported failure verbatim -
      `skew error was: failed to reach 127.0.0.1:53507: ...` - while the new
      wire-level precondition stays green, which is exactly the pin
- [x] `zig build test -Dapp-runtime=win32` green (the unit tests over
      `dial_failure` and `classifyHandshakeError`) - via
      `scripts\floor-lane.ps1 -Lane all`

## What was actually measured (2026-09-12, T1507 turn)

Neither of the two branches the card proposed. The app path is correct on this
build:

```
warning(win32_ipc): IPC new-remote-window: dial failed host=127.0.0.1 port=61045
                    err=error.ProtocolIncompatible
CLI: incompatible Ghoztty version on 127.0.0.1:61045: update one side
```

(The app's stderr had to be captured by a bespoke repro that pre-launches the
debug build with `-RedirectStandardError`, because a Debug build has no file log
sink at all - filed as **T1508**.)

A fresh-process dial to the same skewed agent, via `remote-test-client`, also
returns `ProtocolIncompatible`. The original red run is not reproducible: six
harness runs since have been green.

What WAS wrong is the arm's precondition. It slept two seconds and asserted only
`-not $skewAgent.HasExited`, which says nothing about whether the agent had
bound its port or could answer a HELLO - so an agent that starts slowly (a
freshly built exe being scanned, a loaded box) produces `failed to reach` and the
arm reports it as a T628 regression. The card's own note that the skewed agent
"really is reachable" was measured in a *separate* run, not in the failing one.

The arm now measures the state it is testing, before dialing:

1. the port accepts a TCP connection (polled, 25s budget), and
2. a dial from a FRESH PROCESS comes back `ProtocolIncompatible`.

(2) establishes the skew on the wire independently of the app, so when the app
then disagrees the failure is pinned on the app rather than on the box - the
teeth check demonstrates exactly that split. The skewed agent also gets its own
`GHOSTTY_AGENT_LOCK` instead of sharing the first agent's, so its startup no
longer depends on what the other agent is doing with that file.

## Related

- T628 - filed and fixed this same wrong answer (`db642c66f`). Its reasoning
  about why "check the network" is the harmful default transfers directly.
- T704 - the turn that observed the red arm; its own change is unrelated.

## Progress log

- 2026-09-12 16:39: claimed; work starting.
- 2026-09-12 17:17 [session b6457780]: Root cause pinned by measurement, not the two branches the card guessed. The APP path is correct on this build: with the debug app's stderr captured, a dial to a proto-0 agent logs err=error.ProtocolIncompatible and the CLI prints 'incompatible Ghoztty version on <host>:<port>'. Six harness runs since have all been green; the 2026-09-12 red run is not reproducible. What WAS wrong is arm 4b's precondition: it slept 2s and asserted only '-not HasExited', which says nothing about whether the skewed agent had bound its port or could answer a HELLO - so a slow agent start reads as 'failed to reach' and reports as a T628 regression. The arm now measures the state it is testing (TCP accept + a fresh-process dial that must come back ProtocolIncompatible) and gives the skewed agent its own GHOSTTY_AGENT_LOCK instead of sharing the first agent's.
- 2026-09-12 17:44: status: in-progress -> done
