---
id: T1703
title: "Re-point the six viewer-feedback harnesses from the RichEdit fallback to the web composer"
order: 0.02
deps: ["T1702"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "split out of T937 (P2, out of M1): harness re-pointing that unblocks the fallback\u0027s deletion; no user-visible change"
tags: ["test"]
---

# T1703 - Re-point the six viewer-feedback harnesses from the RichEdit fallback to the web composer

## Summary

viewer-feedback.ps1, -capture, -images, -carousel, -utf16 and -alpha call Set-ComposerSurface 'richedit' (test\win32\lib\ComposerSurface.ps1) and drive the hidden control with Send-TestControlText / Send-TestControlKey / Get-TestControlText. Move every editing arm (undo, quote formatting, send, image chips, carousel, UTF-16 caret, capture) onto the web surface through T1702's driver, or retire an arm in favour of named in-process coverage (the ViewerPane.zig host floor test). When this lands no script asks for the fallback, which is what lets T937 delete it. Split out of T937.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
