---
id: T716
title: "The widened parity sweep reveals 81 shared-core commits since June that no Windows work item cites"
order: null
deps: ["T685"]
status: "done"
commits: ["0d33f3f1b"]
seat: "win"
priority: "P2"
milestone: "M1"
tags: ["infra"]
triage-reason: "first full run of the widened T685 sweep; unmapped shared-core commits are the main-intake lag made visible, same class as T684"
---

# T716 - The widened parity sweep reveals 81 shared-core commits since June that no Windows work item cites

## In plain terms

We keep a list of every change the Mac side makes, so nothing a Mac user got
silently never arrives on Windows. Until now that list only watched the Mac
app's own folder, so changes to the shared machinery underneath both apps were
never checked. With the watch widened, 81 changes made since June turn out to
have no Windows work item naming them. Most of them are almost certainly
already covered - they built the shared agent that Windows runs today - but
"almost certainly" is exactly what this list exists to replace.

## Summary

T685 widened `scripts\parity-sweep.ps1` from `macos/` + `src/viewer/` to
`macos/` + `src/` (minus `src/apprt/win32/` and `src/apprt/gtk/`). The daily
intake range is clean, so the gate is usable from today forward - but sweeping
back to 2026-06-01 (`680a07ed3..origin/main`) now reports **81 unmapped of 349**
(39 in June, 40 in July, 2 in August). Reconciling them is deliberately NOT part
of T685: the tool change and the historical audit are different jobs, and mixing
them would have made neither reviewable.

The set is dominated by `src/remote/` (173 file touches across the 81) - the
WP1-WP4 remote/agent build-out, whose Windows half is what `ghoztty-agent.exe`
already is. So most of these are expected to resolve as **no-parity-owed**
citations rather than new tasks. The ones to look at hardest are the shared
non-agent commits: `src/termio/` (19), `src/apprt/` (14), `src/Surface.zig` (8),
`src/cli/` (6), `src/config/` (3) - that is the class that produced T604.

Method: work the list from the sweep's `-Format markdown` output; for each
commit either file a Windows task or write a one-line no-parity-owed reason
into a citation section (any `docs/design/windows-parity*` file makes it mapped,
since the reference index is keyed on the sha). Note that this supersedes
T684's "every Mac change merged since June is accounted for" claim under the
wider definition - say so where that claim lives.

## Goals

- [x] Every one of the 81 is either a filed Windows task or a written
      no-parity-owed citation naming its sha
- [x] `parity-sweep.ps1 -Range 680a07ed3..origin/main` exits 0
- [x] T684's coverage claim is amended to the widened definition

## Validation criteria

- [x] `powershell -NoProfile -File scripts\parity-sweep.ps1 -Range 680a07ed398f064d106b6ea476f43ce0d143d21a..origin/main` exits 0 and prints SWEEP CLEAN — verified 2026-09-14: 368 commits, 368 mapped, 0 unmapped (81 before the change)
- [x] Every task filed out of this sweep carries a `triage-reason:` naming the commit it came from — vacuous: no task was owed. All 81 are already ancestors of this branch, so every one resolved as a no-parity-owed citation
- [x] `powershell -NoProfile -File test\win32\parity-sweep.ps1` still ALL PASS — verified 2026-09-14, ALL PASS

## Progress log

- 2026-09-14 02:42: claimed; work starting.
- 2026-09-14 02:49 [session 56b06cc6-198b-4caf-8dff-9470cd1bbf98]: Widened sweep re-run over 680a07ed3..origin/main: 368 commits, 81 unmapped. Key finding that decided the dispositions: all 81 are already ancestors of HEAD (checked one by one), so none is code the Windows build lacks - the question is follow-up owed, not porting. Grouped into 8 dispositions and cited in docs/design/windows-parity-sweeps.md; the four shared non-src/remote commits with user-visible surface (219d6554d rename shortcut, a5119273d +list --tty/--when-idle, 2b71512a5 remote-pwd panic, ed98b22fe CommandCore) were each verified against this branch's code rather than reasoned about. Sweep now exits 0 SWEEP CLEAN. T684's macos-only coverage claim amended in place.
- 2026-09-14 03:12: status: in-progress -> done [commit 0d33f3f1b]
