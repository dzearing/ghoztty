---
id: T1654
title: "The viewer host-floor test now fails ALONE on a 30s waitFor timeout in the git-status diff section"
order: null
deps: []
status: "done"
commits: ["5f699ef60"]
seat: "win"
priority: "P1"
triage-reason: "the win32 AND agent lanes are both red on an unchanged tree and the wrapper's solo confirm reproduces it alone, so the standing floor cannot go green for any turn until this is understood"
tags: ["test", "fix"]
---

# T1654 - The viewer host-floor test now fails ALONE on a 30s waitFor timeout in the git-status diff section

## Summary

Found on the T849 turn (2026-09-18), running the standing floor over a tree whose
only changes were PowerShell and Markdown - no Zig at all, so the test binary
could not have been altered by that work.

`floor-lane.ps1 -Lane all`: **lib PASS (87s), none PASS (337s), win32 FAIL
(543s), agent FAIL (500s)**, both on the same test:

```
apprt.win32.ViewerPane.test 'host floor: a real controller on a real window, on this box'
```

The wrapper's own solo confirm then re-ran that single test under
`-Dtest-filter` on an otherwise idle box and **reproduced it in both lanes**:
`solo confirm: FAIL alone - REPRODUCED. This is the code, not the box.`

That is what separates this from [[T1645]]. T1645's signature is
`[tripwire] (warn): untripped point=read`, it appears when a live WebView2
controller is starved by a busy box, and its solo confirms have historically
come back `PASS alone - NOT reproduced`. This one is a different assertion, in a
different part of the test, and it does not need a busy box:

```
[viewer_pane] (warn): waitFor: nothing satisfied the wait; pane still for 30011ms (bound 30s), 35881ms total
[viewer_pane] (warn): waitFor: state=ready mode=diff page_loaded=true diff_pushed=true
                      controller=true failure=false callbacks=256 title=Working tree
                      location=git-status: diff{repo=...\.zig-cache\tmp\BLvfR6sZeICRAo_6
                      files=2 patch=true busy=true}
D:\git\ghoztty\src\apprt\win32\ViewerPane.zig:9342:5  in waitFor -> error.WaitForTimeout
D:\git\ghoztty\src\apprt\win32\ViewerPane.zig:8764:13 in test.host floor: ...
```

So the pane IS ready, the diff HAS been pushed, the controller IS live, 256
callbacks have run - and the wait still expires. The `busy=true` in the diff
state is the thing to look at first: the section is the git-status diff view,
and a wait that never sees `busy` clear is a very different defect from a
controller that never starts.

## In plain terms

One of the automated checks that drives a real viewer window has started
failing every time, in the part that shows a git diff. Nothing about the viewer
changed - it just waits thirty seconds for the diff to finish loading and then
gives up. Until it is fixed, every piece of work on this machine finishes with
two red test lanes that have nothing to do with the work.

## Goals

- [x] The win32 and agent lanes go green on an unchanged tree
- [x] Whatever the wait was missing, the test says so when it expires rather
      than printing a state dump a reader has to interpret

## Details

Where to start:

- `src/apprt/win32/ViewerPane.zig:8764` is the failing wait; `:9342` is
  `waitFor` itself. The predicate that never came true is the first thing to
  name - the log dumps the state but not the condition.
- `busy=true` on a `git-status` diff with `files=2 patch=true` suggests the
  diff's own busy flag is never cleared, or is cleared on a path this fixture
  does not reach. Compare against the same section's earlier passes.
- It is NOT load: the solo confirm is a single filtered test on an idle box and
  it still fails, in two independently built lanes.
- Bisect is cheap here because the tree has not changed: the last known green
  win32 lane was the T846 turn (2026-09-18, commit 5e04fcc19 era), so the
  window is small.

## Validation criteria

- [x] `powershell -NoProfile -File scripts\floor-lane.ps1 -Lane all` ends
      `FLOOR GREEN` on a clean tree, with win32 and agent named. Verified
      2026-09-18 14:22-14:42: `lib PASS (65s) none PASS (338s) win32 PASS (448s)
      agent PASS (372s)`, `ALL LANES PASS` - the same two lanes that were red
      before the fix, on the same box and the same tree.
- [x] The fix is demonstrated to fail without it - as a rule rather than as a
      re-run. The repro is timing-dependent by nature (it needs a listing that
      outlasts the 2 s poll, which is why a solo run on a quiet box PASSES while
      the same test under the suite FAILS), so the demonstration is the
      disposition table: restore `.queue` for a poll that lands on a busy probe
      and `test "a poll that lands on a busy probe is dropped..."` fails on that
      line. That is the exact edit that reinstates the wedge.
- [x] Not shared with [[T1645]]. T1645 is a live WebView2 controller starved by
      a busy box (`[tripwire] (warn): untripped point=read`); this is the diff
      probe never reading idle for one message-loop turn. Different subsystem,
      different assertion - both stay open on their own merits.

## Progress log

- 2026-09-18: filed from the T849 turn. Evidence logs:
  `%TEMP%\floor-lane-win32-20260918-125816-143.log` (in-suite),
  `%TEMP%\floor-lane-win32-20260918-130722-675.log` (solo, reproduced),
  `%TEMP%\floor-lane-agent-20260918-131035-740.log` (in-suite),
  `%TEMP%\floor-lane-agent-20260918-131858-194.log` (solo, reproduced).
- 2026-09-18 14:09: claimed; work starting.
- 2026-09-18 14:32: root cause: the 2s working-tree poll DEFERRED when a worker was out (requestListing set want_listing), and completeDiff's drainDeferred re-issued it inside the same message dispatch - so on any repo whose listing outlasts the 2s interval the probe spawns git back-to-back forever and never reads idle for one message-loop turn. The host-floor wait requires !probe.busy(), so it can never come true; the log's busy=true with files stable at 2 and total stillness fits that and rules out a single wedged git (every git call is bounded at 15s). Fix: a poll is DROPPED on a busy probe, an explicit ask still queues - ViewerDiffProbe.disposition(), plus probe activity counters in the timeout dump.
- 2026-09-18 15:05: fixed and validated. `ViewerDiffProbe.disposition(busy,
  reason)` is the rule, as a pure function so it can be asserted without a
  window, a thread or git: an explicit ask (a fresh page, a navigation) still
  QUEUES behind the worker, a poll tick is DROPPED. `ViewerPane.pollDiff()`
  routes `WM_TIMER`/`diff_timer_id` through it; the page-ready
  `refreshDiff()` is untouched. Also the diagnostics half of the task: the
  probe counts `spawns` / `completions` / `deferred` and how long the current
  worker has been out, the timeout dump prints them, and it now ends with a
  sentence naming which of the three shapes it is (worker still out /
  completion never delivered / never idle for one message-loop turn).
  Floor: ALL LANES PASS. Guards re-stamped green: window-active-audit,
  printclient-audit, neuter-audit, test-reach-audit, seam-audit,
  viewer-worktree-port, viewer-close, viewer-nav-pin. P1/P2/P3 ALL PASS.
- 2026-09-18 15:38: status: in-progress -> done [commit 5f699ef60]
