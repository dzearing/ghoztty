---
id: T1522
title: "Validate on the Mac seat that a bound toggle_tab_overview opens hero mode"
order: null
deps: ["T708"]
status: "todo"
commits: []
seat: "mac"
priority: "P2"
triage-reason: "T708 landed the Mac half unvalidated - this seat has no Swift toolchain, so the symmetric change has never been compiled"
tags: ["test", "feature"]
---

# T1522 - Validate on the Mac seat that a bound toggle_tab_overview opens hero mode

## Summary

T708 mapped toggle_tab_overview to hero mode on both seats: win32 is covered by test/win32/tab-overview-action.ps1 (18 assertions, terminal + viewer paths), and the Mac half is a dispatch change in macos/Sources/Ghostty/Ghostty.App.swift - GHOSTTY_ACTION_TOGGLE_TAB_OVERVIEW left the known-but-unimplemented fallthrough group and now calls toggleHeroMode(app:target:). It has never been compiled or run: this seat has no Swift toolchain. Wanted: build the Mac app, bind the action (keybind = ctrl+shift+f8=toggle_tab_overview), and confirm it opens the hero carousel from a focused pane and closes it again. Confirm specifically that nothing reads the action payload: toggle_tab_overview carries a VOID payload, which is the whole reason that fallthrough group exists, and toggleHeroMode is safe only because it reads the target alone.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
