---
id: T876
title: "TestDesktop child-class filter is an exact case-sensitive compare - \u0027static\u0027 silently matches nothing; make it case-insensitive or fail loudly on zero matches"
order: null
deps: []
status: "done"
commits: ["11cba158f"]
seat: "win"
priority: "P2"
triage-reason: "Harness footgun, not user-facing; costs a debugging round each time it bites (bit T871 for one full run)"
tags: ["infra", "test"]
---

# T876 - TestDesktop child-class filter is an exact case-sensitive compare - 'static' silently matches nothing; make it case-insensitive or fail loudly on zero matches

## In plain terms

The GUI test toolkit lets a script ask for a window's child controls by
class name, but the name must match Windows' spelling exactly — asking for
"static" instead of "Static" quietly returns an empty list instead of an
error. A test then fails with confusing symptoms far from the real cause.

## Summary

`TestDesktop.ps1`'s C# `Children()` (and `Tops()`) compare the
`GetClassNameW` result with `==` — exact, case-sensitive. During T871 a new
helper passed `-Class 'static'`/`'button'`; every enumeration came back
empty, and 18 assertions failed with symptoms ("visible: ") that pointed at
the dialog rather than the filter. The older scripts dodge this by
enumerating `-Class '*'` and regex-filtering with `-match` (case-
insensitive), which is why the trap had never fired before.

Fix: make the class compare `OrdinalIgnoreCase` in `Children`/`Tops` (class
names are not case-significant to Win32's own `FindWindowExW`), or at
minimum have `Get-TestControls`/`Get-TestChildWindows` warn when a non-`*`
filter matches zero children of a window that HAS children. Prefer the
compare fix; it matches Win32 semantics.

## Validation criteria

- [x] `Get-TestControls -Class 'static'` and `-Class 'Static'` return the
      same rows against a live dialog (exercised by an existing GUI script
      or a small self-test in the lib).
      Verified: new assertion in `test\win32\test-desktop-harness.ps1`
      ("Get-TestControls matches a class case-insensitively (Edit=1
      edit=1)") against the live rename dialog; harness ALL PASS (78).
- [x] The T871 helpers in `test\win32\claude-integration.ps1` can drop
      their case-sensitivity comment once the compare is insensitive.
      Verified: the note at L200 now states the filter is case-INSENSITIVE
      and records that it used not to be.
- [x] `test\win32\claude-integration.ps1` still ALL PASS.
      Verified: ALL PASS (87), guard stamp `claude-integration` refreshed.

## Progress log

- 2026-09-19 14:01: claimed; work starting.
- 2026-09-19 14:01 [session 2d5cc63f-e61e-4311-9b82-d9644f0cddaf]: CHECK FIRST verified: the compare fix already landed as T687 (f51fc234b, 2026-09-12) - ClassMatches() in TestDesktop.ps1 is OrdinalIgnoreCase and all four enumerating finders share it; test-desktop-harness.ps1 carries five case-insensitivity assertions. What is LEFT is the validation this card names by its own wrapper (Get-TestControls) and the now-FALSE comment in claude-integration.ps1 telling future authors the filter is case-sensitive.
- 2026-09-19 14:42 [session 2d5cc63f-e61e-4311-9b82-d9644f0cddaf]: RESET MID-TASK at user request. State: code work COMPLETE and validated, NOT yet committed. Uncommitted: (1) test\win32\test-desktop-harness.ps1 - new Get-TestControls case-insensitivity assertion after the T687 block; (2) test\win32\claude-integration.ps1 L200 - replaced the now-FALSE 'EXACT, case-sensitive' note. Evidence already on the box: floor-lane -Lane all = ALL LANES PASS; test-desktop-harness.ps1 = ALL PASS (78), new assertion green at 'Get-TestControls matches a class case-insensitively (Edit=1 edit=1)', guard stamped; claude-integration.ps1 = ALL PASS (87), guard stamped. REMAINING: harness floor (-Lane harness) was mid-run - 2 of 29 members red (asserted-nothing C5 unarmed-stamp ratchet 6->9, skip-visibility B1) and BOTH are INHERITED: every named violator (build-cache, caption-bar, daily-publish, ghoztty-cleanup, install-*, ipc-remote, msg-timer-ids, tab-strip, update-apply, viewer-close, viewer-feedback-capture) is a script this task never touched. Next turn: file tasks for those two ratchet reds so they report PENDING, then validate + commit through git-commit-guard.
- 2026-09-19 14:44 [session 2d5cc63f-e61e-4311-9b82-d9644f0cddaf]: Harness floor finished AFTER the reset was armed: LANE harness PASS in 1008s, STAMPED harness-floor (395 files). The two red members (asserted-nothing C5, skip-visibility B1) were reported PENDING against already-filed tasks, so they do NOT fail the floor and no new tasks are owed for them. NOTHING now blocks the close: on RESUME, tick the three validation criteria, add the log entry, run validate, and commit the two uncommitted test files through git-commit-guard. All four zig lanes, both named harnesses and the harness floor are green over this exact tree.
- 2026-09-19 14:46: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-09-19 14:46 [session 25fad1c8-08b2-4061-98b3-e7f85b15a659]: Resumed the stale claim. Verified from git status/diff that the two uncommitted files are exactly the work the prior turn described: test-desktop-harness.ps1 gains the Get-TestControls case-insensitivity assertion, claude-integration.ps1 L200 replaces the now-false 'EXACT, case-sensitive' note. All evidence the prior turn recorded is stamped on disk (claude-integration, test-desktop-harness, harness-floor guard stamps). Ticked the three validation criteria and closing.
- 2026-09-19 14:48: status: in-progress -> done (by cli: David, from powershell, in ghoztty) [commit 11cba158f]
