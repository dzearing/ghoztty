---
id: T727
title: "deliver-windows-build.ps1 has no -VerifyOnly: nothing audits the install locations between deliveries"
order: null
deps: ["T198"]
status: "done"
commits: []
seat: "win"
priority: "P2"
tags: ["infra", "test"]
triage-reason: "the 2026-08-10 debug-build-in-the-portable state went unnoticed for 17 hours; a read-only audit is how it gets noticed in seconds"
---

# T727 - deliver-windows-build.ps1 has no -VerifyOnly: nothing audits the install locations between deliveries

## In plain terms

There is now a script that delivers a build to the Desktop portable and the
network share and then checks its own work. What there is no way to ask is
"never mind delivering - what is sitting in those places RIGHT NOW?". That
question would have answered in two seconds on 2026-08-10, when both portable
copies had been holding a debug build for most of a day and nobody could tell
without reading file sizes by hand.

## Summary

T198 added `scripts\deliver-windows-build.ps1`, which verifies every file it
writes (length + mtime, `+version`, PE subsystem) and the portable zip's entry
set. All of that verification only runs as part of a DELIVERY. There is
`-DryRun`, but it deliberately writes nothing and therefore checks nothing
either.

Add `-VerifyOnly`: run the verification pass alone against every configured
location - no copies, no backups, no zip rebuild - and report per location
whether its binaries answer the expected commit, whether their PE subsystems are
right, and whether the published zip's entry set still matches the manifest
rule. Exit 0 when everything is what it should be, 1 when a location is present
and wrong, and SKIP an unreachable one exactly as the delivery path does.

## Details

Two things that make this worth its own task rather than a flag added in
passing:

- **What is "expected" when nothing is being delivered?** The delivery path
  takes the commit from the staged exe. A standalone audit more usefully takes
  it from `git rev-parse --short HEAD`, or from whichever location is NEWEST, or
  from an explicit `-ExpectedCommit`. Picking wrong makes the audit cry wolf on
  a box that is deliberately a delivery behind.
- **Where it gets called from.** The candidates are `morning-refresh.ps1` (a
  once-a-day audit line in its log), the go-loop watchdog, and the dashboard.
  The failure this exists to catch is silent and long-lived, so it needs a
  reader that runs without anyone asking - a flag nobody invokes is not an
  improvement over today.

The vector by which a debug `ghoztty.exe` and `ghoztty-agent.exe` reached both
portable locations on 2026-08-10 was never identified: the upgrade log for that
day shows only `zig-out-release` staging, and no script in the repo names those
directories except `upgrade-ghoztty-windows.ps1`. The most likely explanation is
a hand-run copy from an earlier turn. That is precisely the kind of vector a
periodic audit catches and a delivery-time check cannot.

## Validation criteria

- [x] `deliver-windows-build.ps1 -VerifyOnly` reports every configured location
      without writing a single byte (proved by comparing mtimes across the run).
      Section G3 compares path/length/mtime of every file under the sandbox
      before and after the audit; G1/G2/G4 are the reporting.
- [x] It exits 1 against a location seeded with a mismatched build, and names
      which location and what it found. G7-G9 (a Debug exe dropped into a
      location) and G10/G11 (`-ExpectedCommit deadbee`); G12/G13 do the same for
      a published zip carrying a file the manifest excludes.
- [x] It exits 0 with a SKIP line for an unreachable location. G5/G6.
- [x] Some unattended reader calls it, and its verdict is visible without
      running it by hand. `scripts\deliver-audit.ps1` runs it once a day from
      `go-loop-exec.ps1 claim`; `go-loop-health.ps1` reports `deliver=` from the
      watermark it leaves. G14-G22, including the health line reading it back.
- [x] Arms added to `test\win32\deliver-windows-build.ps1`, green. ALL PASS
      (97 assertions), 2026-09-14.

## What "expected" turned out to mean

The Details section above asked the design question and it decided the shape:
the default yardstick is NOT staging. A box deliberately a delivery behind would
fail a staging comparison every time, and a check that cries wolf gets ignored
inside a week. The default is internal consistency instead - every location
agrees with the newest one, the exe and the .com inside a directory agree, the
agents and sign-in bakes agree, subsystems are right, and the zip obeys the
manifest rule - which is exactly what 2026-08-10 broke and is silent about being
one delivery old. `-ExpectedCommit <sha>|HEAD` is how the stricter question gets
asked deliberately.

The audit is REPORTED from the claim and never refuses it: what it names is
fixed by a delivery, which needs a release build, so a gate there would wedge
the loop over a state no turn can clear. For the same reason `deliver=wrong`
does not degrade `go-loop-health.ps1`.

## Progress log

- 2026-09-14 11:34: claimed; work starting.
- 2026-09-14 11:54: built -VerifyOnly as a read-only audit pass (internal consistency by default: every location agrees with the newest, exe/com agree inside a location, agents and sign-in bakes agree, subsystems right, published zip against the manifest rule; -ExpectedCommit <sha>|HEAD asks the stricter question). Unattended reader: scripts\deliver-audit.ps1, once a day from go-loop-exec claim, watermark at %LOCALAPPDATA%\ghoztty\deliver-audit.json, reported as deliver= on go-loop-health. Its first real run scored 7 problems - filed T1569.
- 2026-09-14 12:31: status: in-progress -> done
