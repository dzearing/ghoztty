---
id: T968
title: "+new-window --name=X is silently ignored, so a scripted pane target never exists"
order: null
deps: []
status: "done"
commits: ["0156cf2d6"]
seat: "win"
priority: "P2"
milestone: "M1"
triage-reason: "A documented CLI flag that does nothing means every scripted pane target silently misses, and the CLI surface is the parity contract."
tags: ["fix"]
---

# T968 - +new-window --name=X is silently ignored, so a scripted pane target never exists

## Summary

A script that names a pane at creation gets exit 0 and no registration; the next +send-keys/+read --target fails with 'target not found' and nothing says why. Found while writing test\win32\holder-soak.ps1 (T909).

## In plain terms

When you open a new terminal window from a script you can ask for it to be
given a name, so later commands can talk to it. The name is thrown away without
a word, so the very next command answers "that doesn't exist" and the script
looks broken for no visible reason.

## Details

`docs/claude/cli.md` draws the distinction deliberately: `+new-window
--target=<name>` registers a WINDOW, `+split --name=<name>` registers a PANE.
But `name` is a field on the shared `VerbArgs` struct
(`src/apprt/ipc/args.zig`), so `+new-window --name=X` parses cleanly, exits 0
and registers nothing — the failure surfaces one command later at
`+send-keys --target=X` as `target 'X' not found`.

Either honour it (register the new window's pane under that name, which is what
the flag reads as) or refuse it as an unknown flag for this verb. Silence is the
one option that is wrong. Whichever way it goes, both platforms move together
and `docs/claude/cli.md` says which it is.

## Resolution

Honoured, on both frontends. Re-verified on claim: both the win32 handler and the
Mac `IPCServer.handleNewWindow` used `--name` ONLY for the inline `--split` pane
(and, on the Mac alone, a `--view` window's pane), so a plain terminal window
dropped it. The rule is now one pure function, `newWindowFirstPaneName`
(`src/apprt/ipc/args.zig`), mirrored in Swift:

- no `--split` -> `--name` names the window's FIRST pane (terminal or viewer),
  and `GHOZTTY_PANE_NAME` in that pane is the name (the window's `--target`
  used to double as it, and still does when no `--name` is given);
- with `--split` -> it names the inline split pane, unchanged;
- idempotent like `+split --name`: with no `--target`, a live pane already
  under the name is FOCUSED rather than a second window opened, and the win32
  reply carries the T135 note naming the dropped flags.

This also closed a smaller Windows-only gap: `+new-window --view=... --name=X`
never registered the viewer pane, which the Mac did.

## Validation criteria

- [x] `+new-window --name=X` registers a pane targetable as `X` - sections 5/6
      of `test\win32\ipc-target-exists-note.ps1` (`+read --name`, `+send-keys
      --target`, idempotent re-issue with note, no second window): ALL PASS (21).
- [x] The chosen behavior is the same on macOS and Windows - same rule in
      `IPCServer.swift`; compiling and running it is the Mac seat's (T1720).
- [x] `docs/claude/cli.md` (Naming + the `+new-window` synopsis), `+new-window
      --help` and `+read --help` document it; unit test
      `newWindowFirstPaneName` in the none lane.

## Progress log

- 2026-09-24 03:36: claimed; work starting.
- 2026-09-24 03:51: re-verified still real on both frontends (--name only named the --split pane). Fix: newWindowFirstPaneName rule in args.zig + win32 handler + Swift mirror; idempotent focus on an existing pane name. Acceptance sections 5/6 in ipc-target-exists-note.ps1 ALL PASS (21). Filed T1720 (seat mac) for the Swift half.
- 2026-09-24 04:22: status: in-progress -> done (by cli: David, from powershell, in ghoztty) [commit 0156cf2d6]
