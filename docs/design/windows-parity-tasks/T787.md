---
id: T787
title: "pane-banner T377 wrapped-line ink probe fails under a T204_NEUTERED build"
order: null
deps: []
status: "done"
commits: ["bf1286d91"]
seat: "win"
priority: "P2"
triage-reason: "an ink probe fails under a neutered build, so the negative control and the assertion disagree"
tags: ["test"]
---

# T787 - pane-banner T377 wrapped-line ink probe fails under a T204_NEUTERED build

## Summary

With T204_NEUTERED = true (the negative-control build), pane-banner.ps1's T377 probe reported 'the wrapped line really fills the content column (rightmost ink 196, column ends 687)' - FAIL - while the same probe reads 674 on the shipped build. The neuter only moves GLYPHS to the leading edge of their button box (icon_button.glyphTarget) and, since T282, only changes which buttons light a fill; neither should touch wrapped banner TEXT. So either the neuter has a wider blast radius than it claims, or the probe is timing-sensitive and this was a flake. Reproduce by flipping the flag, rebuilding -Dapp-runtime=win32 -Doptimize=Debug and re-running the script; if it is real, the neuter's documented list of what it breaks is wrong, and if it is a flake the probe needs a settle or a retry. Found while landing T282, which is what made the neutered run observable at all.

## In plain terms

A test that photographs the status banner and checks its text reaches the
right-hand edge of the card once read a quarter-width line and called the
banner broken. The banner was fine; the photograph was taken before the new
text had finished being laid out. Nobody sees this in the product - what it
cost was a day of doubt about whether a test result meant anything - so the
fix is to make the test wait for a picture that has stopped changing, and to
say so loudly when it never gets one.

## Goals

- [x] Decide from evidence whether the neuter really reaches banner text
- [x] Stop the probe from measuring a half-settled frame
- [x] A probe that cannot settle FAILS, naming every reading it took
- [x] The retry cannot hide a real defect

## Validation criteria

- [x] `T204_NEUTERED = true`, rebuilt `-Dapp-runtime=win32 -Doptimize=Debug`:
      the T377 probe reads `rightmostInk=640`, exactly what the shipped build
      reads, and the run's only failures are the five chevron-hover control
      assertions the neuter exists to break. So the neuter's documented blast
      radius is right and the 196 reading was a stale frame.
- [x] Code agrees: `BannerOverlay.contentWidthFor` reserves the chevron column
      from `icon_button.Metrics.init(scale).target`, which the neuter does not
      touch - it only moves `glyphTarget` and `lightsFill`.
- [x] Hardened probe, shipped build: `ALL PASS (148 assertions)`, with
      `[settled after 2: 640/0 640/0]` in the INFO line.
- [x] Hardened probe, neutered build: same `[settled after 2: 640/0 640/0]`,
      so the settle is not what was making the neutered run differ.
- [x] The retry does not settle the VERDICT: with `T377_NEUTERED = true` the
      probe settles on the first stable frame (`685/21 685/21`) and the
      reserved-column assertion still FAILS (`21 ink px`).
- [x] Floor lanes (`floor-lane.ps1 -Lane all`) green (lib/none/win32/agent) and
      `-Lane harness` green, re-stamped over 382 files - after it caught the
      first cut of this change moving the `# argv-audit:` marker off the
      `+set-banner` call it explains (`argv-hazard-audit.ps1` B2/B3).

## Progress log

- 2026-09-16 03:25: claimed; work starting.
- 2026-09-16 03:42: Not blast radius: contentWidth() reserves the chevron column from icon_button.Metrics.target, which T204_NEUTERED does not touch (it only moves glyphTarget and lightsFill). Reproduced with T204_NEUTERED=true, -Doptimize=Debug: the T377 probe read rightmostInk=640, the same as the shipped build, with only the five expected chevron-hover control failures. So the 196 reading was a stale/unsettled frame. Hardened the probe: it now measures a frame that is both different from the one the previous banner left and identical across two consecutive captures, and FAILS with the whole reading sequence when it never settles.
- 2026-09-16 04:39: status: in-progress -> done [commit bf1286d91]
