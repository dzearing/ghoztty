---
id: T902
title: "Startup job-escape detection is heuristic (first-job flags OR pane lineage); an exact agent-job membership probe would close the nested-killer blind spot"
order: null
deps: ["T675"]
status: "done"
commits: ["bb1d6159c"]
seat: "win"
priority: "P2"
triage-reason: "The heuristic covers the measured field shapes; the blind spot (a killer job nested behind a benign first job, with no pane lineage) has no observed occurrence yet."
tags: ["infra", "fix"]
---

# T902 - Startup job-escape detection is heuristic (first-job flags OR pane lineage); an exact agent-job membership probe would close the nested-killer blind spot

## In plain terms

Ghoztty now moves itself out of a process group that would take it down with
the background service. It decides whether it is in danger from two clues —
what Windows reports about the first group it joined, and whether it was
started from inside a Ghoztty pane. Those clues cover every case seen so far,
but a case is constructible where both clues miss and the app would still die.
This task replaces the clues with a direct question: "am I inside the
service's group, yes or no."

## Goals

- [x] The agent creates its PTY job under a name both sides derive identically
- [x] The app asks `IsProcessInJob` against that job instead of guessing
- [x] A sandbox agent can never end up sharing the dev agent's job
- [x] An agent that does not expose a name leaves behavior exactly as it was

## Summary

T675's `job_escape.shouldEscape` fires on `in_job AND (first-job flags carry
KILL_ON_JOB_CLOSE OR pane lineage via $GHOZTTY_PANE_ID)`. Two soft spots,
both documented in `job_escape.zig`:

- **Nested-killer blind spot**: with nested jobs the NULL-handle
  `QueryInformationJobObject` answers only the FIRST job a process joined. A
  kill-on-close job behind a limitless compat job, on a launch with no pane
  lineage (e.g. from another terminal whose own job sits first), is invisible.
  Measured while building T675's harness: a real pane chain answered `0x0`
  while genuinely inside the agent's `0x2000` job.
- **Lineage over-fire**: a pane-launched app escapes even if the agent's job
  is somehow absent from its chain — harmless (one wasted re-exec) but
  imprecise.

The exact probe: get a handle to the AGENT's PTY job and ask
`IsProcessInJob(self, that_job)` directly. There is no public enumerate-my-jobs
API, so the handle has to come from the agent. Candidate mechanisms, none
validated:

- **Named job object** — `pty_child.zig` creates the job with a lineage-derived
  name; the app opens it by name. MUST incorporate the same per-instance
  isolation as the agent pipe (`GHOZTTY_AGENT_INSTANCE`), because
  `CreateJobObjectW` with an existing name OPENS the existing job — a harness
  agent sharing the dev agent's job name would merge their kill domains, which
  is worse than the blind spot.
- **Handle duplication** — the agent advertises its job handle value in
  `port.json`; the app opens the agent with `PROCESS_DUP_HANDLE` and
  duplicates it. No namespace risk, but the info file is a compatibility
  surface (docs/claude/sessions.md rules apply) and a stale file must degrade
  to the current heuristic, never to a wrong answer.

Whichever lands, the heuristic stays as the fallback for an old agent that
does not expose its job.

## Validation criteria

- [x] The app's startup probe answers membership against the agent's actual
      job, not a heuristic, when a current agent is running - `job_object.inNamedJob`
      opens the agent's PTY job by name and calls `IsProcessInJob`; section E of
      `test\win32\job-escape-startup.ps1` shows the app escaping on that signal
      alone, with its own probe line recording that both heuristics missed
- [x] A harness agent's job can never merge with the dev/installed agent's -
      the name carries the same lineage identity the single-instance guard
      enforces one-agent-per, and `pty_child.createNamed` REFUSES an existing
      name (ERROR_ALREADY_EXISTS -> anonymous job + warn) rather than joining it.
      Sections F and G2 are the negative tests; `pty_job_name.zig`'s unit tests
      cover the composition
- [x] Old-agent fallback: the probe is positive-only, so an absent or
      unopenable name leaves the T675 heuristic deciding - section F is exactly
      that state (agent_job_member=null), and sections A-D re-ran green
- [x] `test\win32\job-escape-startup.ps1` extended with sections E, F and G;
      ALL PASS (38 assertions), and `floor-lane.ps1 -Lane all` green on all four
      zig lanes

## Progress log

- 2026-09-20 05:17: claimed; work starting.
- 2026-09-20 05:18: CHECK FIRST done: job_escape.shouldEscape is still the T675 heuristic (in_job AND (kill-on-close flags OR pane lineage)); no exact probe exists. Defect real. Design: named job object (task option 1), NOT the port.json handle-duplication path - the info file is a compatibility surface and a stale file degrades to a wrong answer, while a name derived from the SAME lineage identity the single-instance guard already uses cannot be stale. New pure module src/remote/pty_job_name.zig composes Local\GhozttyAgentPtyJob-local[-debug][-<GHOZTTY_AGENT_INSTANCE>]; pty_child.zig creates the job under that name and REFUSES to adopt an existing one (ERROR_ALREADY_EXISTS -> anonymous job + warn), which is the no-merge guarantee. The probe is POSITIVE-ONLY: exact membership true forces the escape, false/unknown never vetoes the heuristic, because a false negative is the app dying mid-refresh and a false positive is one wasted re-exec.
- 2026-09-20 05:24: Built. src/remote/pty_job_name.zig (new, pure, unit-tested in the none lane via main_ghostty's explicit import): composes Local\GhozttyAgentPtyJob-local[-debug][-<instance>]. pty_child.zig creates the PTY job under that name and refuses to adopt an existing one (ERROR_ALREADY_EXISTS -> CloseHandle + anonymous job + warn). job_object.inNamedJob opens the name with JOB_OBJECT_QUERY only and asks IsProcessInJob. shouldEscape gains agent_job_member as a POSITIVE-ONLY signal that outranks in_job too (the NULL-handle membership query is blind to the same nested chain). Acceptance: sections E (nested-killer blind spot: limitless outer job + named killer nested inside + no pane lineage; asserts both heuristics MISSED from the app's own probe line, and that it escaped anyway) and F (E's negative control and the isolation proof: the identical shape with the killer named for a DIFFERENT lineage - the app must not claim membership and must not escape). guard-due's job-escape-startup row now covers pty_job_name.zig and pty_child.zig.
- 2026-09-20 06:20: Validated on box. floor-lane -Lane all: lib/none/win32/agent all PASS. test\win32\job-escape-startup.ps1 ALL PASS (38 assertions) with the three new sections green - E shows the app's own probe line reading 'flags=0x0 pane_lineage=false agent_job_member=true' and escaping on that alone, F the same shape under another lineage reading agent_job_member=null and NOT escaping, G the real agent creating the job under exactly the name the app opens. Two findings along the way: (1) the harness called Test-GhozttyIsolatedBuildMode with -Exe when it takes -Mode, which bound nothing, answered false for a Debug build and composed '-local' where the app composes '-local-debug' - E then measured a job that did not exist; there is an E-0 assert on the build mode now. (2) a WMI-created launcher (Win32_Process.Create) lands in SESSION 0, and the job name lives in the per-logon-session Local\ namespace, so E's app could not see a job this script created - E/F/G launch through Start-Process instead, documented in the section. Section B flaked red three times in seven runs (app.err.txt zero bytes) and does so with the pre-change HEAD copy of the script too, so it predates this work: filed as T1680.
- 2026-09-20 06:50: status: in-progress -> done (by cli: David, from bash, in ghoztty) [commit bb1d6159c]
