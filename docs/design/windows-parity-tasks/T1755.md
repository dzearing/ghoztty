---
id: T1755
title: "On Mac, check that a restore never hands one session to two panes, and that the pane refused it says so"
order: null
deps: ["T1687"]
status: "todo"
commits: []
seat: "mac"
priority: "P2"
tags: ["fix"]
triage-reason: "P2: parity half of T1687; the Windows fix is user-visible restore behavior, so the Mac must be checked for the same hole."
---

# T1755 - On Mac, check that a restore never hands one session to two panes, and that the pane refused it says so

## Summary

Mac half of T1684/T1687. Windows now refuses a second pane a session the same restore already gave another pane (it opens a fresh shell with a fresh pane id) and paints a 'Session restored elsewhere:' notice in the banner slot (when free, T422) and the scrollback. Verify whether the Mac local-agent restore can replay one session into two panes (a manifest naming it twice); if it can, add the one-pane-per-session rule, the fresh pane id, and the same notice wording (src/termio/session_notice.zig formatFor(.restored_elsewhere) is shared core, so the text is already there - the Mac apprt only has to set RemoteBackend.session_restored_elsewhere). If it cannot, close with the evidence.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
