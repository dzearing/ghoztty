---
id: T893
title: "No external trigger can drive a config reload: no IPC action, no CLI path, menu returns in-process"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
tags: ["infra", "test"]
triage-reason: "testability + automation gap found while validating T557: a whole class of config-reload behavior (onConfigChange effects) has no externally drivable entry, so acceptance harnesses cannot score it"
---

# T893 - No external trigger can drive a config reload: no IPC action, no CLI path, menu returns in-process

## In plain terms

There is no way for a script or agent to tell a running Ghoztty window
"re-read your config file". A person can click Reload Configuration in the
menu or press the shortcut, but automation cannot — and neither can our own
tests, which means everything that happens on a config reload (fonts,
colors, tooltip themes, dividers) goes unverified by the harnesses.

## Goals

- [x] `ghoztty +reload --config` reloads a running instance's configuration
      from disk, with no keyboard and no menu
- [x] The same flag exists on macOS, in the same change
- [x] `--config` beside `--target` is refused with one shared string
- [x] `tab-tooltip.ps1` section F scores the config-reload trigger it had to
      skip

## Summary

Found while validating T557. The win32 app reaches `reload_config` three
ways, all interactive: the menu bar (command comes back in-process from
`TrackPopupMenuEx` with `TPM_RETURNCMD` — never a postable `WM_COMMAND`),
the keybind, and the viewer chord. There is no IPC action for it
(`IpcHandlers.zig` has none; `+reload` is the VIEWER reload verb, T390),
so T557's harness section F could only score the `WM_SETTINGCHANGE` half
of the tooltip reset — the `onConfigChange` half is one adjacent line but
externally unreachable.

Options for the fix (whoever picks this up decides): an IPC
`reload-config` action surfaced as a CLI flag or verb. NOTE the standing
rule: the CLI surface is identical on both platforms — if a verb/flag is
added it lands on macOS in the same change or a `seat: mac` twin task is
filed with it.

## Validation criteria

- [x] A script can trigger a hard config reload of a running instance
      (headless, no keyboard/menu) — `ghoztty +reload --config`, proved on
      the background test desktop by `tab-tooltip.ps1` F2
- [x] `test\win32\tab-tooltip.ps1` section F regains the config-reload arm
      using it — F2 (the reload drives the `tab tooltip reset` oracle) and
      F3 (`--config --target` is refused); ALL PASS (29 assertions)
- [x] Mac has the same trigger, in this same change
      (`IPCServer.handleReload`), AND a `seat: mac` task is filed to compile
      and run that Swift, which this seat cannot build: **T1672**
- [x] Floor lanes + P1-P3 stay green — `floor-lane -Lane all` ALL LANES
      PASS; P1/P2/P3 ALL PASS (26/20/16); plus `cli-unknown-flag` (110) and
      `viewer-panes` (195), the two suites that own the surfaces touched

## Progress log

- 2026-09-19 21:10: claimed; work starting.
- 2026-09-19 21:11 [session 75adea25-6c9e-49f4-bdde-9c205adc3c52]: re-verified still true at HEAD: verb_flags.reload carries only --target, IpcHandlers dispatch has no config action, handleReload is viewer-only; mac IPCServer handleReload likewise. Design: +reload --config = app-wide HARD config reload (Config.load from disk + core updateConfig), --target and --config mutually exclusive, both platforms in this change (win32 performAction(.app,.reload_config,.{.soft=false}); mac AppDelegate.reloadConfig(nil) on the main queue). Acceptance: tab-tooltip.ps1 section F regains the config-reload arm.
- 2026-09-19 21:37 [session 75adea25-6c9e-49f4-bdde-9c205adc3c52]: built + validated. Shipped: shared parseVerbArgs gains a valueless --config; verb_flags.reload accepts it; win32 handleReload routes it to performAction(.app,.reload_config,.{.soft=false}); mac IPCServer.handleReload routes it to AppDelegate.reloadConfig(nil) on the main queue; --config + --target refused with one shared string (apprt.ipc.args.reload_config_target_error). Docs: cli.md + both live SKILL.md copies (kept byte-identical). Tests: 2 new none-lane unit tests (parseVerbArgs --config, verb_flags valueless flag), tab-tooltip.ps1 section F gained F2 (config reload drives the tabTipReset oracle - the arm T557 had to skip) and F3 (the refusal). Green: floor-lane -Lane all ALL LANES PASS; tab-tooltip ALL PASS (29); ipc-p1/p2/p3 ALL PASS (26/20/16); cli-unknown-flag ALL PASS (110); viewer-panes ALL PASS (195). Mac Swift is unbuilt here - filed T1672 (seat: mac) to compile and run it.
