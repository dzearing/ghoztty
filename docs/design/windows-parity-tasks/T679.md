---
id: T679
title: "tab strip rim is brighter than the banner card\u0027s now that the card is elliptical"
order: null
deps: ["T124"]
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
triage-reason: "A tone mismatch on one rim, visible only side by side against the banner card."
tags: ["polish"]
---

# T679 - tab strip rim is brighter than the banner card's now that the card is elliptical

## Summary

T124 made the banner/TOC card's specular rim a real elliptical gradient lit
from a point half a card-height above the card (Mac's `GlassCardBackground`).
The tab chiclet still reads `banner_card.RIM_TOP`/`RIM_BOT` as the endpoints
of a straight vertical ramp (`tab_shape.rimAlpha`), so its top edge is lit at
the full 0.28 while the card's brightest pixel is now ~0.18. Two pieces of
chrome that T206 deliberately gave the SAME rim are now visibly different.

## In plain terms

The little highlight along the top edge of a tab is brighter than the one
along the top edge of a banner card, even though they are supposed to look
like the same material. Nothing is broken — it just makes the tabs read as
slightly shinier than the card sitting a few inches below them.

## Details

`tab_shape.rimAlpha`'s own comment states the approximation and its premise:
"a tab is short enough that a linear ramp between the same two endpoints is
indistinguishable". That premise held while the card's ramp ALSO started at
`RIM_TOP`; it does not now, because `RIM_TOP` is a stop at the ellipse's
center, which sits above the card and is never reached by any pixel of it.

Two candidate fixes:

- Evaluate `banner_card.rimAlpha` against the TAB's own rect, so the tab is
  lit by the same overhead ellipse (cohesive by construction, the T206 rule).
  Costs a `sqrt` per rim pixel of the tab strip.
- Retune the tab's linear endpoints to the values the card's ellipse actually
  paints at its top and bottom edges. Cheaper, but it re-introduces exactly
  the hand-matched pair of numbers T206's shared constants exist to avoid.

Held out of T124 deliberately: it changes tab visuals, which is a different
surface with its own acceptance coverage (`test/win32/tab-strip*.ps1`).

## Validation criteria

- [x] The tab's rim alpha at its top edge and the card's rim alpha at its top
      edge agree to within a level or two, asserted in the `none` lane.
      (`tab_shape.zig` test "the tab's rim and the card's agree at the same
      relative point (T679)": 0.1794 vs 0.1796, asserted within 1/255.)
- [x] `tab_shape.rimAlpha`'s doc comment no longer claims an approximation it
      is not making — it now describes the shared ellipse and records why the
      old premise died. The module header and `banner_card.RIM_TOP`'s comment
      were rewritten with it.
- [x] Tab-strip acceptance scripts stay ALL PASS.
      (`test/win32/tab-strip.ps1`: ALL PASS, 71 assertions.
      `test/win32/pane-banner.ps1`: ALL PASS, 139.)
- [x] The whole floor is green over the change: `floor-lane.ps1 -Lane all`
      ALL LANES PASS (lib/none/win32/agent), `ipc-p1/p2/p3` ALL PASS, and the
      nine acceptance audits the harness edits made due are green, so
      `guard-due.ps1 check` exits 0.

## Progress log

- 2026-09-12 04:45: claimed; work starting.
- 2026-09-12 05:00: fix: tab_shape.rimAlpha now evaluates banner_card's rim ellipse/gradient over the TAB's rect (option 1, the T206 cohesion rule) instead of a straight RIM_TOP->RIM_BOT ramp; banner_card exposes rimEllipse/rimGradient so renderTab hoists the ellipse per tab and the row term per row, paying a sqrt only on rim pixels. Tab top-center rim now 0.179 vs card's 0.180 (was 0.28 vs 0.18). Added none-lane tests: agreement with the card at the same relative point, horizontal falloff, and a mirror of tab-strip.ps1's two on-screen rim thresholds. The acceptance script's side-gradient scan had to move strictly inside the tab's right edge: at the card's real alpha (~0.07 at half height) the rim is dimmer than the strip beside it, so an outboard scan read the strip on both rows.
- 2026-09-12 05:34: validated: floor-lane all four lanes PASS; tab-strip.ps1 ALL PASS (71) and pane-banner.ps1 ALL PASS (139); ipc-p1/p2/p3 ALL PASS; nine test-harness audits re-run green so guard-due exits 0. One real consequence found by A/B-ing a side build of HEAD: the selected tab's flare tips used to be lightened by the brighter rim to within ~2 levels of the strip, so tab-strip's chiclet scan LOST them; with the card's rim they read as tab and the run is legitimately 3px wider than the published rect at 125%. Reworked that comparison into two assertions - the scan must find every published chiclet (inward <= 2px) and may not run wider than the paint reaches (flare radius + 1px on the selected tab, 2px elsewhere) - and added ChromeGeometry's FlareR metric for it.
