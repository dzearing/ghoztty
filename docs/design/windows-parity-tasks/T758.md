---
id: T758
title: "A banner\u0027s dot-relative autolinks keep the cwd they were parsed with; Mac re-resolves on render"
order: null
deps: []
status: "done"
commits: ["098baa106"]
seat: "win"
priority: "P2"
triage-reason: "parity gap in banner autolinking: Mac re-resolves dot-relative paths on render, win32 freezes the parse-time cwd"
tags: ["polish"]
---

# T758 - A banner's dot-relative autolinks keep the cwd they were parsed with; Mac re-resolves on render

## In plain terms

A banner can carry a path written relative to the pane's folder (`.\out\
build.log`). Ghoztty turns that into a real path when the banner text is
set. If you then `cd` somewhere else, the link still points where it pointed
when the banner was written — even though the same text, re-read now, would
mean somewhere else. On a Mac it follows the `cd`. Nobody has reported this;
it is the loose end T539 left.

## Summary

T539 gave `banner_markdown` an autolink pass whose `PathContext` (`cwd`,
`home`) is supplied by `BannerOverlay.setText` and baked into the link
target at PARSE time. Mac's parser takes `cwd` on every render pass
(`SurfacePaneBanner`, `parseInline(_:cwd:)`), so a pane that `cd`s re-resolves
the same banner text against the new directory — CLAUDE.md's Autolinking
section says exactly that ("resolve against the pane's current working
directory **at render time**").

Absolute paths, UNC paths and URLs are unaffected — they need no context at
all. This only touches `.\`, `..\` and `~\` forms, and `~` does not move.

## Where

- `src/apprt/win32/BannerOverlay.zig` — `setText` is the only place the
  context is read; the blocks are cached in the overlay's arena until the
  text changes.
- `src/apprt/win32/banner_markdown.zig` — `PathContext`, `autolink`.
- The cheap shape is to re-parse when the pane's cwd changes (the `.pwd`
  action already lands in `Surface.setPwd`), not to re-parse on every paint:
  the overlay repaints far more often than the cwd moves, and parsing on the
  paint path is what the cached blocks exist to avoid.

## Goals

- [x] The same banner text, untouched, resolves against the directory the
      pane is in NOW
- [x] Nothing re-parses for a banner that carries no dot-relative link
- [x] A shell that reports nothing (`cmd.exe`) is covered, not just the ones
      with shell integration

## Validation criteria

- [x] A banner set to `.\a.txt` in one directory, then a `cd` in that pane,
      resolves to the NEW directory — section 6k of
      `test\win32\pane-banner.ps1`, read from the Ctrl+click viewer `url`:
      `…\ghoztty-t758-a\t758.md` before the move, `…\ghoztty-t758-b\t758.md`
      after it, with the banner source asserted unchanged in between
- [x] A banner with no relative link does not re-parse on a cwd change — the
      absolute-path control in 6k: no `banner cwd re-resolve` line in the
      app's log across the same `cd`. `banner_markdown.dependsOnCwd` is what
      decides, unit-tested in the `win32` lane over every autolink form
- [x] Absolute/UNC/URL links are byte-identical before and after a cwd change
      — same control, same section: the viewer url still names directory A
      after the pane has moved back into it
- [x] The negative control `T758_NEUTERED` restores the pre-fix behavior and
      fails exactly the two 6k assertions that are about the MOVE (measured
      2026-09-15: 2 FAILED / 146 passed), so those assertions test the fix
      rather than the harness

## Progress log

- 2026-09-15 04:08: claimed; work starting.
- 2026-09-15 04:12: root cause confirmed: BannerOverlay.setText bakes PathContext.cwd into link targets at parse time and nothing re-parses on a cd. Fix: banner_markdown.dependsOnCwd (pure, conservative superset of the .\/..\ autolink rule) + BannerOverlay.cwd_dependent/refreshForCwd + Surface.setPwd re-parses only when the cwd actually MOVED (shell integration reports it every prompt) and only for cwd-dependent banners. Explicit [text](url) links need a scheme, so they never depend on cwd.
- 2026-09-15 04:34: built; added banner_markdown.dependsOnCwd unit tests and section 6k of test\win32\pane-banner.ps1 (one banner, clicked from two directories, plus an absolute-path control that must neither move nor re-parse). docs/claude/cli.md autolinking section now states the win32 mechanism.
- 2026-09-15 05:19: validated: all four floor lanes PASS, harness floor PASS, test\win32\pane-banner.ps1 ALL PASS (148 assertions) including the new section 6k. Negative control T758_NEUTERED measured: exactly 2 FAILED / 146 passed - the two 6k move assertions and nothing else. Guards re-stamped: pane-banner, docs-routing, window-active-audit, close-confirm, remote-disconnect, harness-floor. Filed T1586 (empty cached pwd makes a dot-relative link point at the drive root).
- 2026-09-15 05:34: status: in-progress -> done [commit 098baa106]
