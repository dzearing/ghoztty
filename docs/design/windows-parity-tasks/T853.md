---
id: T853
title: "test-binary-soak.ps1\u0027s two \u0027return , procs\u0027 sites make an EMPTY worker list count as 1 at @() call sites - audit callers for the phantom-element trap"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
triage-reason: "P2: PS5.1 phantom-element audit is harness hygiene with no user-visible impact"
priority: "P2"
tags: ["fix", "test"]
---

# T853 - test-binary-soak.ps1's two 'return , procs' sites make an EMPTY worker list count as 1 at @() call sites - audit callers for the phantom-element trap

## Summary

Found during T494: PS5.1 'return , (array)' wraps the array as ONE pipeline object, so an @() call site sees Count 1 even when the array is EMPTY (the element is the empty array). In CacheHeal.ps1 this manifested as a phantom cache-corruption detection until fixed (commit 3e84827aa). scripts\test-binary-soak.ps1 lines ~483 and ~568 use the same 'return , $procs' shape; if any caller wraps in @() or counts the result, zero started workers could read as one (the element being an empty array), which would misreport load or skip the kill sweep. Audit each caller: either the comma is load-bearing (caller assigns directly, no @()) and deserves a comment saying so, or it is the trap and needs the plain-return + @()-at-call-site pair. Validation: targeted assertions in test\win32\test-binary-soak.ps1 covering the zero-workers path.

## In plain terms

The soak tool can hold the machine busy with background workers while it hunts
for a rare crash, and it tells you how many workers it started and how many of
them died. Those two numbers were wrong: a run in which nothing started said
"one", and a run in which several workers crashed said "one" as well, with all
the crash lines mashed into a single unreadable line. Anyone reading the soak's
summary to decide whether a crash is real was reading a count that could not go
above one.

## Goals

- [x] the worker helpers report the real number of workers - zero when none
      started, N when N did
- [x] a soak in which two load workers crash says `worker-crash=2` and names
      both, one line each
- [x] the harness asserts all of that, including the zero case
- [x] the rest of the repo is audited for the same shape

## Validation criteria

- [x] `test\win32\test-binary-soak.ps1` is ALL PASS (88 assertions), with the
      eight new assertions of section 3e failing before the fix and passing
      after - the pre-fix run is the negative control
- [x] the zero-started-workers path is asserted directly on the helpers (lifted
      out of the script with the PowerShell parser), because it needs
      `Start-Process` to fail and cannot be staged through the soak's own
      command line
- [x] end to end, a two-crashing-worker soak reports `worker-crash=2` and two
      named lines (before: `worker-crash=1` and `w0 1: <both lines>`)
- [x] `scripts\floor-lane.ps1 -Lane all` green
- [x] `scripts\harness-floor.ps1` green (this task edits a `test\win32\` script)

## Progress log

- 2026-09-18 15:44: claimed; work starting.
- 2026-09-18 16:17: root cause: all three worker helpers used 'return , \' while every call site wrapped the call in @(); the comma wrapper survives @(), so the call site counted 1 regardless. Measured on this box: @(f) is Count 1 for an empty, a one-element and a two-element comma-protected return alike. Fix = plain return at the three sites (the call sites already wrap). Eight new assertions in test\win32\test-binary-soak.ps1 section 3e, red before / green after; the end-to-end one caught the live shape (worker-crash=1 and a mangled 'w0 1: <both lines>' for two crashed workers). Filed T1656 for the repo-wide audit rule, since unroll-count-audit knows this shape (B5/B6) but deliberately indexes past it.
