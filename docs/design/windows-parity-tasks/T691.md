---
id: T691
title: "Convert the win32 acceptance suites off kill-the-debug-lineage onto GHOZTTY_AGENT_INSTANCE"
order: null
deps: ["T167"]
status: "done"
commits: ["15a4d3520"]
seat: "win"
priority: "P2"
triage-reason: "Every persistence-touching suite still kills the box's debug agent to run - rude to the loop's own panes, and it serializes the suite. T167 built the knob that makes it unnecessary; this spends it."
tags: ["test", "infra"]
---

# T691 - Convert the win32 acceptance suites off kill-the-debug-lineage onto GHOZTTY_AGENT_INSTANCE

## In plain terms

Our automated tests currently have to shut down the background helper that is
keeping the developer's own terminal sessions alive, just to run. It works, but
it means a test run can interrupt real work, and two test runs can never happen
at the same time. The mechanism that removes the need for that now exists; this
is the job of actually switching the tests over to it.

## Goals

- [x] A run can own an agent lineage without killing anybody else's
      (`Set-GhozttyTestAgentLineage`, `lib\AgentLineage.ps1`)
- [x] A run's processes are ATTRIBUTABLE - the agent and, since this task, its
      ConPTY holders name their lineage on their own command line
- [x] A run's FILES are attributable too: the session manifest follows the
      lineage the way the agent state dir already did
- [x] `Stop-RepoGhoztty -ScopeToLineage` kills only what this run owns, and
      REFUSES rather than widening when it cannot tell
- [x] Two converted persistence suites run concurrently and both pass
- [ ] Every persistence-touching suite is converted (tranche 1 here; the rest
      are dep-linked follow-ups)

## Summary

T167 added `GHOZTTY_AGENT_INSTANCE=<suffix>`: one env var that gives a sandbox
its own agent lineage (guard, lock, heartbeat, state dir, pipe, autostart value,
`+sessions`). What it did NOT do is spend it — every existing suite still opens
with some version of

```powershell
Get-CimInstance Win32_Process -Filter "Name='ghoztty-agent.exe'" |
    Where-Object { $_.CommandLine -like '*local-agent-debug*' } |
    ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

which kills whatever debug agent is holding the loop's own panes, and which is
also why two acceptance scripts can never run concurrently.

## Details

The work, per suite:

1. Give the run a per-run lineage (`"<suite><PID>"`, ≤ 24 chars) and set
   `$env:GHOZTTY_AGENT_INSTANCE` alongside the `LOCALAPPDATA` /
   `GHOSTTY_LOCAL_AGENT_BIN` / `GHOZTTY_PIPE_SUFFIX` block the suite already has.
2. Narrow the teardown from "kill every zig-out agent" to "kill the pids this
   script started" (the pattern `agent-instance-lineage.ps1` uses).
3. Where a suite was silently non-persistent all along, its assertions will
   change — that is the point, and each such change needs reading rather than
   accepting. `upgrade-no-fork.ps1`'s explicit "the sandbox pane really is
   agent-backed (port.json written)" assert is the shape every converted suite
   should carry.

Candidates (anything that starts an agent or asserts on persistence):
`agent-pipe.ps1` (also drops its `--force-replace` + `GHOSTTY_AGENT_HEARTBEAT`
hack), `agent-session-cap.ps1`, `agent-recovery.ps1`, `agent-upgrade.ps1`,
`agent-open-refused.ps1`, `agent-autostart.ps1`, `agent-shell-integration.ps1`,
`agent-user-env.ps1`, `agent-job-escape.ps1`, the `session-*` family,
`upgrade-no-fork.ps1`, and the `chooser-*` scripts that need a live roster.

Worth doing as one sweep rather than one-suite-at-a-time: the value only arrives
when NO suite kills the lineage, since one that still does takes every
concurrent run down with it.

## Validation criteria

- [x] No script under `test\win32\` kills an agent it did not start — for a
      CONVERTED script. `Stop-RepoGhoztty -ScopeToLineage` takes only the agents
      and holders whose command line names this run's lineage plus the app pids
      the harness recorded launching, and REFUSES rather than widening when it
      cannot scope both halves (`agent-lineage-suites.ps1` A7-A12, B1-B3). The
      crude `'*local-agent-debug*'` predicate the task was filed against survives
      in one place only, `upgrade-no-fork.ps1:586`, which T1495 converts.
- [x] Each converted suite still passes standalone — `session-open.ps1` ALL PASS,
      `session-close.ps1` ALL PASS, run alone on 2026-09-12
- [x] Two persistence-touching suites run CONCURRENTLY and both pass —
      `agent-lineage-suites.ps1` section D starts both at once and reads each
      one's own verdict line: `D1 session-open.ps1 ... D1 session-close.ps1 ...
      D2 BOTH concurrent suites finished green`
- [x] A debug agent started before the run (standing in for the loop's own) is
      still alive and still holding its sessions afterwards — section C, with
      `-TeethCheck` (the pre-T691 unscoped kill) turning C2 red and nothing else
- [x] Floor: `floor-lane.ps1 -Lane all` ALL LANES PASS; P1-P3 ALL PASS; all 24
      guards that the touched files made due, re-run green

## Progress log

- 2026-09-12 07:57: claimed; work starting.
- 2026-09-12 08:00: survey: the crude kill the task describes is almost gone already - only upgrade-no-fork.ps1:586 still greps '*local-agent-debug*'. What remains is the REAL blocker: lib\CleanSlate.ps1 Stop-RepoGhoztty kills EVERY process whose ExecutablePath is the repo zig-out agent, so a concurrent run's agent (and the loop's own dev agent) dies with it - 47 scripts route through it. Plan for this turn: (A) make a pty holder attributable to its lineage (its spawn command line carries only a session-id temp path today, so a lineage-scoped kill would leave holders orphaned); (B) new lib\AgentLineage.ps1 minting GHOZTTY_AGENT_INSTANCE for debug-lineage scripts plus Get-GhozttyAgentStateDir so the ~60 hardcoded 'ghoztty\local-agent-debug' paths derive instead; (C) narrow Stop-RepoGhoztty to the current lineage; (D) acceptance proving an outside agent survives and two converted suites run CONCURRENTLY; (E) convert a first tranche and file follow-ups for the rest - 47 GUI suites cannot be re-validated honestly in one turn.
- 2026-09-12 09:17: landed: (1) src - a pty holder's spawn spec now carries the lineage in its FILE NAME (%TEMP%\ghoztty-ptyhost-<lineage>-<sid>.json), which is the only thing about a holder that is visible from outside it, and the session manifest follows the lineage the way the agent state dir already did (session-layout-debug-<lineage>.json); both unset-reproduce the legacy names byte for byte, with unit tests. (2) harness - new test\win32\lib\AgentLineage.ps1 (mint a run-unique lineage, derive the state dir, decide whether a command line is ours) and Stop-RepoGhoztty -ScopeToLineage, which REFUSES rather than widening when it cannot scope both halves. (3) session-open.ps1 and session-close.ps1 converted as the worked examples. Its own -TeethCheck arm caught a real bug before ship: piping Get-RepoGhozttyProcess straight into Where-Object hands the filter the whole ', @()' array as ONE object, so the scoped kill matched nothing and every other arm scored it a pass. Filed T1494/T1495 (the remaining ~57 suites, in two tranches) and T1496 (run the suite concurrently once they are all converted).
- 2026-09-12 09:19: status: in-progress -> done [commit 15a4d3520]
