---
id: D94
title: "Should a restored pane\u0027s own commands give up half the CPU to keep its output fast?"
task: "T1465"
kind: "assumption"
status: "resolved"
created: "2026-09-09T02:51:47Z"
assumed: "Shipped the half that has no downside: the session-keeper and the console plumbing now stay on the fast cores, which took a burst of output from 1.94x slower than a plain pane to 1.55x. The user\u0027s own commands were left on the whole machine, so nothing got slower."
options:
  - key: o1
    label: "Leave commands on the whole machine (Recommended)"
    pros:
      - "a build or test run in a restored pane is exactly as fast as it is anywhere else"
      - "the output speed-up already landed is kept"
      - "nothing about the machine can surprise you"
    cons:
      - "a pane whose session is being kept alive still takes about half again as long as a plain one to scroll a big burst of output"
      - "the gap the original report was about is narrowed, not closed"
    mitigation:
      - "the speed-up covers the terminal\u0027s own work, which is what most output actually waits on"
  - key: o2
    label: "Put everything in the pane on the fast cores"
    pros:
      - "a restored pane finally matches the terminal Windows ships with, output for output"
      - "the thing that was reported as feeling heavy stops feeling heavy"
    cons:
      - "anything you RUN in that pane - a compile, a test suite, an install - is confined to half the machine and takes noticeably longer"
      - "the same command is then faster in a pane whose session is not being kept alive, which is a strange thing to discover"
    mitigation:
      - "none that keeps both; the confinement is inherited by everything the pane starts"
answer: "o1"
answeredAt: "2026-09-09T14:00:06Z"
note: null
---

# D94 - Should a restored pane's own commands give up half the CPU to keep its output fast?

## Why this needs a call

A pane that keeps its session alive across restarts runs your shell inside a separate helper process, and Windows decides on its own to run helper processes on the slower half of the CPU. That is why printing a lot of text in such a pane has been taking about twice as long as in a plain one - the thing the user reported. Moving the terminal's own plumbing off the slow half recovered a good part of it with no cost to anything. Recovering the REST means moving your commands there too, and the fast half is only a third of the machine's total capacity - so the same build that takes four minutes in an ordinary pane would take noticeably longer in a restored one. That is a trade between how fast text appears and how fast your work finishes, and which one matters is not ours to assume.

## Resolution

Chosen: Leave commands on the whole machine (Recommended)

Resolved 2026-09-09T14:00:06Z.
